# Formal Limits

> Transformer 表达力 / 复杂度 / 信息论上界。

## 核心问题与 NTP 假设

NTP-mech 阵营长期依赖 TC⁰ / constant-depth circuit 上界来论证"transformer 在某些任务上有架构级表达能力上界"。本 topic 跟踪：(a) 新的 hard impossibility / lower bound；(b) 把"理论上界"转成"可在部署前估计的实操参数"的工作；(c) 用现实（softmax、低精度、CoT）放宽假设后这些上界还剩多少。

## 关键论文 (chronological)

| 日期 | 论文 | 主要论点 | NTP 归类 (mech/cap/pseudo) | 链接 |
|---|---|---|---|---|
| 2026-05-26 | STARS — Stability-driven Recurrent Scaling (Yang et al.) | LoopLM 的 latent-reasoning 崩塌等价于 Jacobian 谱半径 >1；Jacobian Spectral Radius Regularization + 随机 loop 深度采样 ⇒ test-time depth 单调饱和而非崩塌；C1 (fixed-depth forward-pass) 在 trained-stable looped 模型上需替换为 *converged depth* | counter-evidence (扩 C1 而非否决) | [2605.26733](../papers/paper_notes/2026-05-29-2605.26733-stars-looped-stability.md) |
| 2026-05-26 | NITP: Next Implicit Token Prediction | 在 NTP 之上叠加用浅层激活作下一 token 的稠密 self-supervised target；9B MoE MMLU-Pro +5.7%；论证 NTP one-hot 留下的 hidden-state 自由度可被 auxiliary 对潜空间的几何约束修掉，几何欠约束属 objective-engineering 层 | counter-evidence (削弱"NTP 表征几何不可救"mech 论调) | [2605.24956](../papers/paper_notes/2026-05-28-2605.24956-nitp-next-implicit-token-prediction.md) |
| 2026-05-21 | Lost in Tokenization (Graph Trade-offs) | 不同 tokenization (spectral/RW/adjacency) 对同一图函数诱导多项式级 depth gap；表达力 = (architecture, tokenization) 联合属性 | mech (extends C1) | [2605.22471](../papers/paper_notes/2026-05-27-2605.22471-graph-tokenization-tradeoffs.md) |
| 2026-05-18 | MHA as NW Ensemble | MHA = H 个 NW kernel 估计器的结构化 ensemble；variance reduction 由 head decorrelation (principal angles) 主导 | mech (soft, framework) | [2605.20271](../papers/paper_notes/2026-05-27-2605.20271-mha-as-nw-ensemble.md) |
| 2026-05-08 | Fresh-Symbol Classification (Logistic Theory) | 在 kernel logistic regime 下把 transformer 学到的 template 分类器分解为 ideal classifier + 词汇 overlap 扰动；NTP readout 强制 token-name 依赖使该分解失效 | mech (boundary clarification) | [2605.07120](../papers/paper_notes/2026-05-27-2605.07120-fresh-symbol-classification.md) |
| 2026-05-21 | Deterministic Horizon (Guo) | 由层数与嵌入宽度决定的推理深度上界 H* ∈ [19,31]，超出后训练无法恢复 | mech | [2605.23024](../papers/paper_notes/2026-05-26-2605.23024-deterministic-horizon.md) |
| 2026-05-19 | Measure-Theoretic Reasoning (Zhang et al.) | OT/Wasserstein 框架下，RoPE 保 shift invariance；Dyck-k → TC⁰ depth lower bound | mech | [2605.19944](../papers/paper_notes/2026-05-26-2605.19944-measure-theoretic-reasoning.md) |
| 2026-05-18 | Low-Prec Softmax + (Summarized) CoT (Brösamle & Eckstein) | softmax + 低精度 + log-grow depth 仍 Turing-complete；summarized CoT 把 size 从 log-time 降到 log-space | counter-evidence | [2605.18079](../papers/paper_notes/2026-05-26-2605.18079-lowprec-softmax-cot.md) |

## 当前共识 / 争议

- **共识**：在 no-CoT、固定生成长度下，TC⁰ 类 depth 下界仍是公认的硬限制；2026-05 两篇新工作把它从"渐近"做到"可测量"。
- **争议**：一旦允许 CoT（即使是 summarized / latent CoT），上界基本被打开到 Turing-complete 级；这意味着"NTP-mech 推理深度上界"这一表述必须**强行区分 no-CoT vs CoT-augmented 两个 setting**，否则容易陷入伪命题。
- 仍未澄清：Deterministic Horizon 的 H* 是否在加 CoT 之后退化为"每条 CoT step 一个 horizon 周期"的复合上界。
- **新增 (2026-05-27)**：tokenization 选择本身是 depth 决定因素（Lost in Tokenization）——这意味着 NTP-mech 候选 C1 的参数集应从 (L, d) 扩为 (L, d, tokenization)；BPE 之于 LLM 可能就是某些"机制级失败"的真因，而非模型本身。同时 (Fresh-Symbol Classification) 指出 **NTP 的 readout 强制 token-name 依赖** 是 fixed-label 实验与 NTP 之间的本质 gap。

## 关键证据线 (chronological, 长程视角)

把本 topic 的源头拉回到 circuit complexity 而不是 LLM 时代，可以看清"TC⁰ 上界"不是 2020 后的新发现，而是 1984 年就已存在的硬数学事实被重新挂到 transformer 上。

- **1984，Furst–Saxe–Sipser**："Parity, Circuits, and the Polynomial-Time Hierarchy"（FOCS 1981 / MST 1984）证明 PARITY ∉ AC⁰。这是一切现代 "transformer 不能数奇偶" 论证的远端祖先；TC⁰ 比 AC⁰ 多了 threshold 门，但 PARITY ∈ TC⁰ 也只是 *uniformly* 在 depth O(1) 可解，常数本身相当大。
- **2019-06，Hahn**："Theoretical Limitations of Self-Attention in Neural Sequence Models" [arxiv:1906.06755](https://arxiv.org/abs/1906.06755)。第一篇把 hard-attention / soft-attention 的形式上界直接挂到 PARITY 和 Dyck-2 上的工作。Hahn 的关键 trick 是用 Lipschitz / influence bound 论证 fixed-depth transformer 无法解决任意长度的 PARITY——这一论证在 2026 仍是 mech 阵营 citing 最高频的"原始引用"。
- **2021-06，Merrill–Sabharwal–Smith**："Saturated Transformers are Constant-Depth Threshold Circuits" [arxiv:2106.16213](https://arxiv.org/abs/2106.16213)。把 saturated attention 正式塞进 TC⁰。该论文是"TC⁰ wall"这个比喻被锁死的关键节点。
- **2022-02，Chiang–Cholak**："Overcoming a Theoretical Limitation of Self-Attention" [arxiv:2202.12172](https://arxiv.org/abs/2202.12172)。第一份正面 counter-evidence：通过 layer-norm + position-aware attention，PARITY 在某种 setting 下可以被学到——但需要任务相关的 positional bias，不是 free lunch。
- **2022-07，Merrill–Sabharwal**："The Parallelism Tradeoff: Limitations of Log-Precision Transformers" [arxiv:2207.00729](https://arxiv.org/abs/2207.00729)。把"log precision + 固定 depth"作为现实假设，证明这样的 transformer 仍落在 TC⁰ 内。这是把渐近上界往"现实可部署模型"上贴的第一步。
- **2022-10，Liu et al.**："Transformers Learn Shortcuts to Automata" [arxiv:2210.10749](https://arxiv.org/abs/2210.10749)。揭示了一种 mech 阵营常忽视的现象：transformer 不学正确的 automaton，而学一个 shortcut——这个 shortcut 在 train distribution 内贴近真值、在 long-horizon 上系统性失败。这是后来"length generalization"研究线的起点之一。
- **2023-05，Feng et al.**："Towards Revealing the Mystery behind Chain of Thought" [arxiv:2305.15408](https://arxiv.org/abs/2305.15408)。给出"CoT 把 transformer 的表达力扩张到 P/poly 一类"的形式构造；与 Merrill–Sabharwal 的 CoT-as-Turing-tape 论证 (2310.07923) 互为镜像。
- **2023-06，Sanford–Hsu–Telgarsky**："Representational Strengths and Limitations of Transformers" [arxiv:2306.02896](https://arxiv.org/abs/2306.02896)。引入 *q-sparse averaging* 任务，证明 transformer 有它做不到的 strict separation——这是少数把 lower bound 给到 *task family* 而非 single problem 的工作。
- **2023-10，Merrill–Sabharwal**："The Expressive Power of Transformers with Chain of Thought" [arxiv:2310.07923](https://arxiv.org/abs/2310.07923)。正式版："log CoT steps ⇒ L (log-space)，poly CoT ⇒ P"。这一对 bound 让此后所有 "no-CoT vs CoT" 的形式讨论都必须显式标注 CoT 长度。

判断：从 1984 到 2023 这条线最值得记住的不是"transformer 弱"，而是 **它的表达力等级强依赖 (precision, attention saturation, CoT length, positional encoding) 这四个旋钮**。任何不指明四旋钮就谈 "TC⁰ 上界" 的论断都是 under-specified——这也是 2024 之后这一支文献被批评 *cherry-picking assumptions* 的根本原因。

## 经验验证线 — TC⁰ 之墙的实测史 (chronological)

上一节是 1984–2023 的**理论**血脉；本节追的是另一条更短但更脏的线：在真实 transformer / 真实数据集上**到底有没有看到那堵墙**。这条线的特点是几乎每一篇都既被 mech 派引用、又被 cap 派引用——因为同一组数字在两种 framing 下读出来意思完全相反。

- **2020-09 — Bhattamishra–Ahuja–Goyal**，*On the Ability and Limitations of Transformers to Recognize Formal Languages* ([arxiv:2009.11264](https://arxiv.org/abs/2009.11264))。这是第一篇系统在 PARITY / Dyck-1 / Dyck-2 / Shuffle-Dyck 上 train + test 小 transformer 的工作。结论很硬：固定深度的 vanilla transformer 在 PARITY 上**根本学不会**（test accuracy 在长度外推时直接掉到 chance），Dyck-1 可以但 Dyck-2 边界处崩。Hahn 2019 的形式论断第一次在实验上拿到匹配信号。
- **2022-07 — Delétang et al. (DeepMind)**，*Neural Networks and the Chomsky Hierarchy* ([arxiv:2207.02098](https://arxiv.org/abs/2207.02098))。把 Transformer / LSTM / Stack-RNN / Tape-RNN 同台跑 Chomsky 阶梯任务。结果：Transformer 在 *regular* (PARITY) 与 *context-free* (Dyck) 上长度外推都不行，而带显式 stack/tape 的 RNN 可以。这一对照是 \"架构归纳偏置\" 论的最强实验底座之一；mech 派把它当 TC⁰ wall 的实测，cap 派则反驳说\"没让模型用 CoT 就不公平\"。
- **2022-07 — Anil et al. (Google)**，*Exploring Length Generalization in Large Language Models* ([arxiv:2207.04901](https://arxiv.org/abs/2207.04901))。在 ≤8B 规模 PaLM 上测加法 / parity / 变长 reasoning 的长度外推，证明 **scratchpad/CoT 即使配 fine-tune 也无法把 length-extrapolation 救到训练长度的 2× 以上**——这是\"光加 CoT 不够，还得加正确的 position encoding\"这一后续共识的起点。
- **2023-05 — Ruoss et al. (DeepMind)**，*Randomized Positional Encodings Boost Length Generalization* ([arxiv:2305.16843](https://arxiv.org/abs/2305.16843))。证明只要把 position id 在训练时按某个 prior 随机化，多个 Chomsky-阶梯任务的长度外推可显著扩张。**这是对 Bhattamishra 2020 / Delétang 2022 的部分反例**：墙的位置高度依赖 positional encoding 的选择，所谓\"architecture-level 上界\"在不变 PE 下成立，换 PE 后斜率改变。
- **2023-10 — Zhou et al.**，*What Algorithms Can Transformers Learn? A Study in Length Generalization* ([arxiv:2310.16028](https://arxiv.org/abs/2310.16028))。RASP-L 视角：能被短 RASP-L 程序刻画的任务族，transformer 一般能 length-generalize；不能被刻画的（如 PARITY）就外推不动。这条 RASP-L 假说至今没被严格证伪，是\"为什么有的任务过墙、有的不过\"目前唯一带预测力的经验法则。
- **2024-02 — McLeish et al.**，*Transformers Can Do Arithmetic with the Right Embeddings* ([arxiv:2405.17399](https://arxiv.org/abs/2405.17399))。Abacus embedding：给每个数字 token 注入 position-within-number 信号，transformer 在 100-digit 加法上 length-extrapolation 大幅改善。又一例：墙的硬度由 tokenization × PE × 任务三者联合决定，不是单纯 depth 函数。
- **2024-06 — Sanford et al.** *Transformers, Parallel Computation, and Logarithmic Depth* ([arxiv:2402.09268](https://arxiv.org/abs/2402.09268)) [unverified ID]。 把 *k-hop induction* 任务证明需要 Ω(log k) 层；与 Sanford–Hsu–Telgarsky 2023 的 task-family lower bound 配对，第一次给出\"任务难度 → 最小 depth\"的可计算映射。
- **2026-05 — Lost in Tokenization** ([2605.22471](../papers/paper_notes/2026-05-27-2605.22471-graph-tokenization-tradeoffs.md))。在 graph 表达力上把 \"tokenization 旋钮\" 推到与 (L, d) 并列的位置——前面所有 PE 修正都可视为 tokenization 旋钮的特例。

判断：把这条经验线和上一节理论线并排看，事实是 **TC⁰ wall 在 (vanilla PE, no CoT, fixed depth) 这个最严的角落里被反复实测到，但在它周围的每一个旋钮——PE 随机化 (Ruoss)、数字 embedding (McLeish)、CoT 长度 (Anil 推到 Merrill–Sabharwal 形式版本)、tokenization (2605.22471)——上都至少各被打开一次**。这意味着 NTP-mech 派若想守住\"TC⁰ wall 在工程上可证伪\"这一立场，必须把 falsification 条件写成\"四旋钮全部锁死\"的局部命题，而不是\"transformer 做不到 X\"的全局命题——后者经验上已经死了至少四次。

## 当前最强的 mech 候选

| ID | 候选机制 | 形式陈述 | Falsification 条件 |
|---|---|---|---|
| C-FORM-1 | no-CoT depth wall | fixed-depth, log-precision transformer 在 input length n 上的可解任务严格 ⊂ uniform TC⁰ | 找到一个 n-scaled task，fixed-depth log-precision transformer 在 generalization gap → 0 意义下 solve 它 |
| C-FORM-2 | CoT-length 等级 | 给定 CoT 长度 k(n)，可解问题类 = TIME[k(n) · poly] ∩ NTP-realizable | k(n)=O(log n) 时若能解出 NP-complete 实例 family，则证伪 |
| C-FORM-3 | tokenization-as-depth | 同一函数族在不同 tokenization 下的最小 depth 差为多项式级，故 depth 上界必须写成 (L, d, tokenization) 三元组 | 找到 tokenization 不变的 depth lower bound family（Lost in Tokenization 2605.22471 反方向） |

C-FORM-1 与 C-FORM-2 是当前文献的最大公约数；C-FORM-3 是 2026-05 之后才被 take seriously 的，仍处于"形式上自洽但实测证据少"的状态。

## 反例与上界突破

- **Chiang–Cholak 2022** (上引)：PARITY 在合适 positional bias 下可学，说明 Hahn 2019 的 lower bound 是 *architecture+input-encoding* 联合的，而非纯 architecture 的。
- **Brösamle–Eckstein 2026-05** (本页表中 2605.18079)：低精度 softmax + log-grow depth 仍 Turing-complete；summarized CoT 把空间从 log-time 进一步压到 log-space。这一结果直接 weaken 了 Merrill–Sabharwal 2207.00729 的 "log precision = TC⁰" 推论的实际杀伤力——只要允许 depth 随 n 缓慢增长，TC⁰ wall 就不再是 wall。
- **Liu et al. 2022 shortcuts**：反方向 counter-evidence——即使理论上某任务在 transformer 表达力内，训练得到的解也可能是 *shortcut* 而非 correct mechanism；这意味着"理论可表达"与"NTP 训练能学到"之间存在第二道 gap，不能用 expressivity 论证替代 learnability 论证。

## 表达力 vs 可学性 — 第二堵墙 (chronological)

上面所有上下界都讨论的是**表达力 (expressivity)**：固定架构 / precision / depth / CoT 长度下，*存在不存在*一组参数使 transformer 计算目标函数。但 NTP 给定的不是\"存在性\"，而是\"在 next-token cross-entropy 这一个目标 + SGD/AdamW 这一个优化器 + i.i.d. 训练数据这一个 sample 通道下，能不能被学到\"。这两件事之间有一段被 mech 派长期忽视的 gap——它本身就构成第二堵墙，且**其位置常常比 TC⁰ 墙更靠前**。把这条线单拉出来：

- **2022-10 — Liu et al. shortcuts** ([arxiv:2210.10749](https://arxiv.org/abs/2210.10749))（上节已引）。第一次清晰展示：在表达力*内*的 automaton 任务，transformer 学到的是 shortcut 而非正确机制；training loss → 0，OOD length acc → chance。表达力够 ≠ NTP 训练能学到。
- **2023-09 — Berglund et al. Reversal Curse** ([arxiv:2309.12288](https://arxiv.org/abs/2309.12288))。\"A is B\" 训过，\"B is A\" 测不出。这件事**不是表达力问题**——逆映射在 1 层 transformer 表达力内就能写——而是 NTP gradient 对方向 asymmetric。Reversal curse 是\"第二堵墙\"最干净的样例。
- **2023-05 — Allen-Zhu & Li Physics-of-LM 系列**（[arxiv:2305.13673](https://arxiv.org/abs/2305.13673) 等）。在合成传记数据上把\"参数有没有存住事实\"和\"模型能不能 retrieve 它\"分离开测；结论：knowledge storage 容量约 2 bits/param，但 retrieval 受 *训练时是否出现过该 query template* 强约束。表达力 ≫ 学得到的子集。
- **2024-02 — Power et al. grokking 复盘 / Nanda mech-interp**（[arxiv:2301.05217](https://arxiv.org/abs/2301.05217) modular addition mech；[arxiv:2201.02177](https://arxiv.org/abs/2201.02177) Power et al. 原版）。grokking 现象说明：**就算解在表达力内**，NTP loss 也可能在到达正确电路前先固化到一个 memorization 解；从 memorization 切到 generalization 需要 weight decay × 数据量 × 训练步数三者的特定窗口。这意味着 \"transformer can in principle do X\" 和 \"GPT-style training run will produce a transformer that does X\" 是两个不同命题。
- **2024-04 — Zhang et al. Towards Best Practices of Activation Patching** [unverified ID]。回到 mech-interp 工具自身：activation patching 找到的\"算法子电路\"在 NTP 训出的模型里**密度极低**——大部分参数处于既非 memorization 也非可读算法的中间相。表达力上界与实际占据的解空间之间存在容量浪费。
- **2024-06 — Bordelon–Cengiz–Pehlevan**，*A Dynamical Model of Neural Scaling Laws* ([arxiv:2402.01092](https://arxiv.org/abs/2402.01092))。把 scaling law 显式建成 SGD 在特征学习相图上的轨迹；推论：data scaling exponent 由 *数据本身 spectrum* 决定，与表达力上界几乎无关。这条线为 \"为什么 dense scaling 撞墙不是撞 TC⁰ 墙\" 提供了第一份机制级解释。
- **2025-XX — Length-generalization 综述线**（Anil 2207.04901 → Zhou RASP-L 2310.16028 → Abacus 2405.17399 → 后续 looped-transformer 工作 [unverified bundle]）合在一起读，给出 \"learnability frontier\" 的当代经验描述：能被短 RASP-L 程序刻画 ∩ 配对正确 PE/embedding ∩ 训练分布覆盖该 program 的输入子流形，三者交集才是 NTP 真正学得到的子集，**严格小于** Merrill-Sabharwal CoT-augmented 上界给的可表达类。
- **2026-05-26 — Korchinski, Favero, Wyart (EPFL), *Learn from your own latents and not from tokens — a sample-complexity theory* ([arxiv:2605.27734](https://arxiv.org/abs/2605.27734))**。在 *depth-$L$ probabilistic CFG* 这一可解析生成模型上严格计算三组 sample complexity：token-level SSL / supervised 需 $\exp(\Omega(L))$ 样本恢复 latent 树；*单层* latent-prediction SSL (data2vec / JEPA 类) 只需 $O(1)$-in-$L$ 样本；多层 hierarchical stacking 不带渐近改进。这是第二堵墙第一次拿到 *跨范式* 形式分离——不是 (objective, optimizer, data spectrum) 四旋钮在 NTP 内部的 learnability 退化，而是 NTP loss 这一个 *paradigm 选择* 本身在 PCFG 世界类上把 sample complexity 推到指数。**对 C-FORM-4 的影响**：C-FORM-4 此前的实验 anchor 只有 reversal curse 与 shortcut learning 两条 (均 paradigm-内 learnability)，2605.27734 把 anchor 列表扩到 **三条 + 第一条形式定理 anchor**——它是 C-FORM-4 \"learnability gap 存在\" 命题在受限世界类下的 *sharp 量化版* (而非证伪)。但 anchor 与命题之间须明确边界：(α) 2605.27734 的分离在 PCFG 假设 *强* 成立时才 sharp，真实文本/视频是 ambiguous CFG + long-range constraint，token-SSL 在 7B+ 规模 *没有* 经验上表现出 $\exp(\Omega(L))$ 样本饥饿 (反而是 Chinchilla 量级 *线性* scaling) — 这是 C-FORM-4 falsifier \"在多项式步数内被学到\" 在 *真实数据* 上几乎已经成立的反向论据；(β) 形式分离的 *方向* 与 C-FORM-4 一致 (NTP-paradigm 在某些世界类下 learnability 严格弱于 latent-SSL paradigm)，但 *强度* 受 PCFG 假设软化后未知。**不开新 sub-candidate**：2605.27734 已在 [`../survey/ntp_survey.md`](../survey/ntp_survey.md) §10 登记为 C-WM-5 (LeJEPA identifiability) evidence-base 第二支柱 (上一支柱: LeJEPA stationary-noise identifiability 定理)；落到 formal 域只作 C-FORM-4 evidence-base 第三 anchor (前两 anchor: reversal curse 行为侧 + Bordelon 2402.01092 dynamical model 机制侧) — *不* 升级独立 mech candidate (跨范式分离仅 PCFG 受限成立, 强度不足以独占 candidate 槽)，*不* 改 C-FORM-4 falsifier 文字 (paradigm-外分离与 paradigm-内 learnability gap 是不同命题, falsifier 须保持 paradigm-内界定避免读者把 \"用 JEPA 替换 NTP\" 误算 C-FORM-4 证伪)。**互引** ([`world_model.md`](world_model.md) §C-WM-5 LeJEPA-identifiability)：world_model 侧把 2605.27734 作 *identifiability + sample-complexity* 双轨理论支柱中的 sample-complexity 一支；formal 侧把同一条 anchor 作 C-FORM-4 evidence-base 第三 anchor — 同一 paper 在两侧 *服务不同 candidate* 的处理与 [Chess-WM §formal × world_model 互引](world_model.md) (commit `ab19e5d`) 同型，是 daily harvest 三连 blockquote (世界类 / state-tracking / video-action) 之外 *jepa-sample 第四颗子弹* 的 formal-域落点，补齐 2026-05-30 daily 五论文中此前唯一只挂 §10 未落 topic 的一条。

判断：把这条线和上文表达力线并排看，最诚实的图景是 \"NTP 真正学到的能力\" 嵌在两道边界之间——
1. **外圈**是 (depth, precision, CoT, tokenization) 联合决定的 expressivity 上界（TC⁰ 墙的现代版）；
2. **内圈**是 (objective shape, optimizer bias, data spectrum, PE/embedding choice) 决定的 learnability 上界。
两道墙的*相对位置*随四旋钮组合而异，但**对 frontier scale 的真实瓶颈，2024-2026 几乎所有实测证据指向内圈先撞**——reversal curse、length-extrapolation 崩塌、knowledge retrieval template 依赖、shortcut learning，没有一个是\"transformer 表达不了\"。这意味着 NTP-mech 派若要论证\"架构级天花板\"，必须在 *learnability* 而非 *expressivity* 这一侧拿出 lower bound；目前为止还没有一份这样的形式工作存在（仅有 Bordelon 2402.01092 的 dynamical model 给了部分图像）。该 gap 即是 NTP 形式化论证下一阶段的真正前线。

新增候选机制：

| ID | 候选机制 | 形式陈述 | Falsification 条件 |
|---|---|---|---|
| C-FORM-4 | NTP-learnability gap | 存在表达力可达的任务族 F，使任何 vanilla NTP 训练流程（cross-entropy + AdamW + i.i.d. sampling）在多项式步数内都不能将 OOD generalization error 压到 ε 以下 | 给出一个属于 F 的具体任务，并展示一组超参 + 训练数据使其在多项式步数内被学到（length-generalization 边界即潜在反例区） |

C-FORM-4 与 C-FORM-1/2 互补：前三个候选锁 expressivity 上界，C-FORM-4 锁 learnability 上界。Reversal curse 与 shortcut learning 是它当前最具体的实验 anchor。

## 与其它 topic 的接口

- → [reasoning](reasoning.md)：C-FORM-2 是 reasoning 页 C-REAS-1（CoT-as-scratchpad）的形式底座；二者证伪条件互为充分必要。
- → [scaling_limits](scaling_limits.md)：C-FORM-1 是 scaling 页 C-SCALE-1（架构级 compute ceiling）的 *上界* 来源；scaling 法则只能在 C-FORM-1 之内运作。
- → [world_model](world_model.md)：C-FORM-3 的 tokenization 旋钮直接影响 world_model 页 C-WM-1（closed-world bottleneck）的有效 horizon。
- → 样章 [N2-the-tc0-wall](../samples/N2-the-tc0-wall.md)：本 topic 的 chronology §1984→2023 即 N2 §1-§2 的素材底；N2 负责把它叙事化。

## MoE / 动态 depth / early-exit — "per-token compute" 假设的当代裂缝 (2022–2026)

上文所有 TC⁰ 风格 lower bound——Hahn 2019、Merrill–Sabharwal 2106.16213 / 2207.00729、Sanford 2402.09268——都共享一条隐藏假设：**每个 output token 的计算量是 (L · d²) 这一个常数**。在 dense decoder-only transformer 上这一假设几乎默认成立；但 2022 之后 frontier model 的实际架构早已不在这一假设的舒适区。把这一道裂缝单独抽出来，因为它直接决定上一节 C-FORM-1 / C-FORM-2 在 2026 frontier scale 下的"现实射程"。

- **2022-01 — Du et al. (GLaM) ([arxiv:2112.06905](https://arxiv.org/abs/2112.06905))**。1.2T 参数 MoE，每个 token 只激活 ~96.6B。这是第一次在 production scale 上把 \\\"sparsely-activated transformer\\\" 形式塞进 NTP pipeline——但形式上**激活 expert 数 k 是一个 input-dependent 离散变量**，使得 \\\"per-token compute = const\\\" 在 token 级不再成立。Merrill–Sabharwal 风格的 TC⁰ 嵌入需要重新做：threshold gate 的 fan-in 在 MoE-router 路径下与 expert 数线性挂钩。
- **2024-03 — Fedus / Switch / MoE 综述 + DeepSeek-MoE ([arxiv:2401.06066](https://arxiv.org/abs/2401.06066))**。Fine-grained expert (256 experts top-8) 把 routing decision 的离散熵推到 log₂(C(256,8)) ≈ 38 bits/token——一个不可忽略的 input-conditioned bit budget。若把 router 视为 *额外的 attention layer*，C-FORM-1 的 (L, d) 参数应扩为 (L, d, log E) 三元组。**形式工作的现状：没有公开论文把 MoE-augmented transformer 的 expressivity class 形式刻画过**；mech 派与 cap 派都默认沿用 dense bound，这是 2026-05 文献里一条公开的赤字。
- **2022-07 — Schuster et al. (CALM) ([arxiv:2207.07061](https://arxiv.org/abs/2207.07061))**。confidence-based early-exit：per-token layer 数 L(t) 是 input-conditioned。Merrill–Sabharwal 的 "fixed L" 假设在 CALM-style 推理里不成立——形式上等价于把 depth 从常数升为 *bounded random variable*。但**注意一个反方向的事实**：若 L(t) 的期望 E[L] = L₀ 仍是 input-size n 的常数，则 TC⁰ 嵌入论证只需在 worst-case L 上做，结论几乎不变。早退在 *渐近表达力* 上并不打开新的类——它只影响常数因子（即 wall-clock）。
- **2023-07 — Giannou et al. *Looped Transformers as Programmable Computers* ([arxiv:2301.13196](https://arxiv.org/abs/2301.13196))**。把同一个 fixed-depth transformer block 在推理时循环 T(n) 次，证明可模拟 RAM 程序。这是把 "动态 depth" 这一旋钮**推到极端**：T(n) 任意增长则上界直接到 P/poly，与 CoT-augmented bound (Merrill–Sabharwal 2310.07923) 平行。从形式角度看，looped transformer / Universal Transformer ([arxiv:1807.03819](https://arxiv.org/abs/1807.03819)) 与 CoT 是**同一道暗门的两个变种**：都允许"per-token compute"随 n 增长。
- **2024 — MoD (Mixture-of-Depths) ([arxiv:2404.02258](https://arxiv.org/abs/2404.02258)) [unverified ID]**。Google DeepMind 让每个 token 可选择跳过若干 layer，等于把 early-exit 推广为 per-token 双向。形式后果与 CALM 同源——E[L] 仍可视为常数，故 worst-case TC⁰ 嵌入不变。
- **2025 — MoR / Recursive-MoE 系列 [unverified bundle]**。把 looped 与 sparse 两条路线合并：fixed-depth backbone + per-token 选择性递归。**这条线在 2025-2026 才开始出现，形式表达力分析完全空白**。

**对 C-FORM-1 / C-FORM-2 的净影响**：把上述四类架构变体拆成两组：

1. **不打开新表达力类**的：CALM (Schuster 2207.07061)、MoD (2404.02258)、dense MoE (GLaM 2112.06905 / DeepSeek-MoE 2401.06066) ——形式上仍嵌入 TC⁰ / log-precision TC⁰，只是常数（router bits、平均 depth）变化。C-FORM-1 几乎不动；C-FORM-2 的 k(n) 等级表不动。
2. **真打开新类**的：looped / Universal Transformer (2301.13196 / 1807.03819) ——T(n) 随 n 增长则等价于 CoT-augmented bound。这一组与 C-FORM-2 同源，不是新墙也不是新门。

**判断 (2026-05-28)**：上一节 Open-problem 第 2 条的诚实答案是——\\\"per-token compute = const\\\" 假设在 MoE / early-exit / MoD 下**仍紧**（worst-case argument 仍能 carry），但 router 的 input-conditioned bit budget 让"const"这一表述在量化常数因子时变得 misleading。**真正的形式空白不在 MoE 也不在 early-exit，而在 looped-x-sparse 复合架构**——MoR 类工作尚未被 formal-limits 文献覆盖。这是 C-FORM-1 / C-FORM-2 在 2026 frontier scale 下需要补的下一道形式工作。一句话总结：MoE 不破墙，loop 才破墙；2024 后大部分 frontier 改架构改的是 MoE 这一组，所以 TC⁰ 论证在工程意义上**比批评者以为的更耐久**。

新增候选机制：

| ID | 候选机制 | 形式陈述 | Falsification 条件 |
|---|---|---|---|
| C-FORM-5 | router-bit budget | MoE / MoD 的 input-conditioned routing 在每 token 引入 b(n) = O(log E) 额外 bit 计算预算，使 dense-TC⁰ 嵌入的常数因子按 expert 数对数增长，但渐近类不变 | 找到一个任务族 F，使 (L, d, E) MoE 严格 ⊃ (L · k, d) dense 在同 FLOPs 下的可学子集（即 routing 不仅省 FLOPs 还打开 expressivity） |

C-FORM-5 是对 C-FORM-1 的常数修正而非升级；它存在的价值是**显式拒绝**"MoE 突破 TC⁰"这一在 2024-2026 多次被非形式化重复的说法。

## SSM / Mamba / 线性循环 / hybrid — "非 attention 主干"是否绕开 TC⁰ 之墙 (2021–2026)

上一节的裂缝清单里漏了一整条路线：**所有以 SSM / 线性 RNN / 卷积长程算子为主干的非 attention 架构**。这条线在 2023 年以 Mamba 为标志冲到 frontier 候选位，2024–2025 进一步出现 Jamba / Mamba-2 / RWKV-v6 / Zamba 等 hybrid 变体，其中相当一部分公开材料显式或隐式宣称 "线性时间 / 常数 KV，且能力不逊于同 size dense transformer"。如果这个宣称在形式上成立，TC⁰ 之墙的故事就完全要重讲——因为 Hahn / Merrill–Sabharwal 系列下界都是针对 *softmax attention* 的 saturated/log-precision 嵌入。但 2024 年这条故事被一篇关键工作钉住了：**SSM 不仅没有突破 TC⁰，它在一个更窄的子类里运行**。

- **2021-10 — Gu, Goel, Ré (S4) ([arxiv:2111.00396](https://arxiv.org/abs/2111.00396))**。把序列建模写成 $h'(t) = A h(t) + B x(t),\; y(t) = C h(t)$，离散化后用 HiPPO 矩阵 + FFT 卷积训练，$O(N \log N)$ 时间、$O(N)$ 内存。形式上这是一个 **线性时不变 (LTI) 长程卷积**——核心算子是固定的多项式卷积核，不存在 input-dependent gating。表达力上界从这一刻起就被严格限定在 *线性算子* 加 *逐点非线性* 的复合类里，远窄于 attention 的 input-dependent 二次型。
- **2023-12 — Gu & Dao (Mamba) ([arxiv:2312.00752](https://arxiv.org/abs/2312.00752))**。关键改动是把 S4 的固定 $A, B, C$ 变成 **input-dependent**（selective SSM），由此恢复了对 token 内容的条件化能力，在 LM 主线 benchmark 上与同 size dense transformer 持平。这一改动在工程上很重要——它让 SSM 从 "长程信号处理玩具" 升级为 "可上 LM" 的主干；但 **selective 之后的 $A_t, B_t, C_t$ 仍是 input 的固定函数，没有跨 token 的二次型耦合**，这是下一段下界论证的关键。
- **2024-05 — Dao & Gu (Mamba-2 / SSD) ([arxiv:2405.21060](https://arxiv.org/abs/2405.21060))**。State Space Duality (SSD) 论证显示 Mamba 类 selective SSM 与一类 **structured masked attention** 在数学上等价——这是 SSM 与 attention 阵营的第一座桥。SSD 视角下 SSM 是 attention 的 *特例*（mask 强制为下三角且 rank-1 结构），不是平行物种。
- **2024-04 — Merrill, Petty, Sabharwal, *The Illusion of State in State-Space Models* ([arxiv:2404.08819](https://arxiv.org/abs/2404.08819))**。这是把 SSM 钉进 TC⁰ 论证地图的关键工作。核心结论用一句话写死：**S4、Mamba 这类 SSM 与 transformer 同属 $\mathsf{TC}^0$ 复杂性类，且它们都不能解决 state-tracking 完备问题（如 $S_5$ 置换组合），而 RNN 的标准类 $\mathsf{NC}^1$ 可以**。换言之 SSM **不仅没有突破 TC⁰，它连 RNN 的真子集都不到**——所谓 "linear recurrence with hidden state" 这个名字带来的 "应该比 transformer 强" 直觉在形式上是错的。这把 C-FORM-1 的有效适用范围一次性扩到 SSM 主干，无需重新证明。
- **2024-03 — Jamba (AI21) ([arxiv:2403.19887](https://arxiv.org/abs/2403.19887))** + **2024 RWKV-v6 / Zamba / Griffin [unverified bundle]**。Hybrid 架构在 frontier 上的实际策略是 *Mamba block 与 attention block 交错*（典型比例 1:7 或 1:3）。形式后果是直接的：只要任一层包含标准 softmax attention，整体架构的 expressivity 类就由 attention 决定（取并集），SSM 部分只贡献常数项 FLOPs 节省与长程记忆 inductive bias。Hybrid 不打开新表达力类，但也不缩窄——它是 *工程上的常数因子游戏*，不是 *形式上的新墙或新门*。
- **2024–2025 — Linear attention 复兴 (GLA / RetNet / DeltaNet / Gated DeltaNet)**。这一支与 SSM 形式同源（都可写成 linear-recurrent + outer-product update），Yang et al. *Gated Linear Attention* ([arxiv:2312.06635](https://arxiv.org/abs/2312.06635)) 与 *Parallelizing Linear Transformers with the Delta Rule* ([arxiv:2406.06484](https://arxiv.org/abs/2406.06484)) 是代表。Merrill 2404.08819 的论证机制（state 更新是 input 的固定线性函数复合）几乎全部 carry over：linear attention 与 SSM 共享同一道 TC⁰ 顶棚，且同样卡在 $S_5$ 不可解。

**对 C-FORM-1 / C-FORM-2 / C-FORM-5 的净影响**：把 SSM / Mamba / linear-attention / hybrid 全部纳入 C-FORM-1 的适用域是 2024 年最重要的形式扩张。具体三条修订：

1. C-FORM-1 的 architectural scope 从 "log-precision saturated softmax transformer" **显式扩到 "log-precision selective-SSM、linear attention、以及二者与 softmax attention 的任意 hybrid"**。这一扩张不需要新论证，由 Merrill 2404.08819 直接 carry。
2. C-FORM-2 (k(n) 等级表) 的 *no-CoT* 部分对 SSM 同样适用——SSM 也不能在常数 forward pass 内做任意 $S_5$ 长度的 state tracking。**关键反方向**：SSM 的 CoT-augmented 上界尚无独立证明，但 Mamba 在 GSM8k 上的 CoT 表现与同 size transformer 接近 [unverified 具体数] 是经验暗示。
3. C-FORM-5 的 router-bit budget 论证天然适用 Mamba-MoE / Jamba 等架构——SSD 等价把 Mamba block 写成 structured-attention 之后，router 论证不变。

新增候选机制：

| ID | 候选机制 | 形式陈述 | Falsification 条件 |
|---|---|---|---|
| C-FORM-6 | SSM-as-TC⁰-subset | 任何 selective-SSM / linear-attention 主干（不含 softmax attention 层）在 log-precision 下严格嵌入 $\mathsf{TC}^0$ 且不能解决 $S_5$ word problem；hybrid 架构 expressivity 由 attention 子集决定 | 在纯 Mamba / 纯 linear-attention 主干（无任何 softmax attention 层）上实现 length-generalizing $S_5$ 解算 (≥100 token 推理输入, ≥80% accuracy)；或形式证明 selective-SSM 在 log-precision 下严格超 $\mathsf{TC}^0$ |

**判断 (2026-05-28)**：2023 年宣称 "Mamba 可能取代 transformer" 的讨论几乎全部默认 SSM **在表达力上至少等价于 attention**——这一假设在 2024-04 之后被形式证伪。SSM 的工程价值（线性内存、长程信号处理 bias）依然真实，但它**绑在 transformer 同一道形式天花板下面**，并且在 state-tracking 子类上严格更弱。这反过来解释了 frontier 全部走向 hybrid 而非纯 SSM 的事实——hybrid 的存在不是 "结合两家之长" 的修辞，而是 *形式必要性*：纯 SSM 主干在某些任务族上的失败 (Merrill 2404.08819 实测 Mamba 在 $S_5$ 长度泛化崩坏) 是结构性的，必须由 attention 层补回来。一句话总结：**MoE 不破墙，loop 才破墙，SSM 连墙都摸不到**——这是上节判断的对偶补充，也把 N8 §四 "反墙四条件" 里第 (ii) 条 "新架构在 capacity 维度上 strictly 超越 transformer" 的可证伪窗口进一步收窄。

**2026-05-30 update — C-FORM-6 实证 anchor (state-tracking 子带)**: Walker & Lyons *Chess-World-Model* ([2605.30100](../papers/paper_notes/2026-05-30-2605.30100-chess-world-model-state-tracking.md)) 在 10M 真实国际象棋对局 + matched-protocol 下对照 Causal Transformer / block-diagonal SLiCE / Mamba-3 / Gated DeltaNet (negative eigenvalues)，3M-8M 参数规模 recurrent 模型显著优于 Transformer，real-game 在 ≥18M 饱和但 random-uniform OOD split 持续不饱和。这是 C-FORM-6 (SSM-as-$\mathsf{TC}^0$-subset) 上界 *与* Merrill 2404.08819 \"$S_5$ 不可解\" 之间被忽视的第三条:**transition-matrix 表达力本身是独立旋钮** — input-dependent + negative-eigenvalue 让 SSM 在 *exact state-tracking* 上对 attention-only NTP 拿到显著胜场，但 OOD random-uniform 仍不饱和揭示 *learnability 而非 expressivity* 才是 frontier 瓶颈 (与第二堵墙 / Bordelon 2402.01092 同向)。对 C-FORM-8 (Looped-SSM 渐近塌缩) sub-candidate falsifier 的影响:需把 "matched transition-matrix expressivity" 列为显式控制变量,否则 "loop 是唯一旋钮" 命题被 transition-matrix 选择 confound 假性证伪。

## Looped × Sparse 复合架构 — 形式空白的初步填补 (2024–2026)

上面 §MoE 节末尾把 \"looped-x-sparse 复合架构\" 标为 *形式表达力分析完全空白*，§SSM 节末尾的判断又把 hybrid 归到 \"工程上的常数因子游戏\"。这两条放在一起容易给人一个错印象——好像 looped-style depth 与 sparse-style routing 两支正交、不需要联合分析。实际上 2025 年起出现的 *Mixture-of-Recursions* / *Recursive-MoE* / *Looped-MoE* 一类工作把两条线焊在一起：固定深度 backbone + per-token *既选 expert 又选递归次数*。本节把这一组架构能讲清楚的部分单独拎出来，避免 Open-problem 第 2 条无限期挂账。

- **2018-07 — Dehghani et al., *Universal Transformer* ([arxiv:1807.03819](https://arxiv.org/abs/1807.03819))**。最早把 *per-token 动态步数* 写进 transformer 主干（ACT-style halting probability），但当时未与 sparse routing 复合，形式分析停在 \"Turing-complete with unbounded steps\" 这一定性陈述。
- **2023-01 — Giannou et al., *Looped Transformers as Programmable Computers* ([arxiv:2301.13196](https://arxiv.org/abs/2301.13196))**。证明 fixed-depth block 循环 $T(n)$ 次可模拟 RAM；这是 looped 单独 (不含 sparse) 的形式上界——$T(n)$ 任意增长则到 P/poly，与 CoT-augmented bound (Merrill–Sabharwal [arxiv:2310.07923](https://arxiv.org/abs/2310.07923)) 平行。
- **2024 — *Looped Transformers for length generalization* (Fan et al.) [unverified ID]** + Yang et al. *Looped transformers learn iterative algorithms* [unverified ID]。把 looped block 在算术 / 排序 / 图遍历上的 length-generalization 推到 $\\geq 10\\times$ 训练长度，**经验下界**：纯 dense + 无 loop transformer 在同任务族上 length-generalize 失败。这部分不动 TC⁰ 上界图像（loop 已知打开新类），价值是把 *经验上 loop 真打开了哪些任务* 与 *RASP-L learnability* 对齐。
- **2025 — *Mixture-of-Recursions* / MoR [unverified bundle, 2025-xx]**。骨架：fixed-depth backbone，每个 token 同时被 router 派到 (a) 一组 expert（top-k of E）与 (b) 一个递归深度 $\\tau(t) \\in \\{0,1,\\dots,T_{\\max}\\}$。形式上每 token compute 为 $\\tau(t)\\cdot k\\cdot d_{\\text{expert}}$，是 input-conditioned *双重* 离散变量。若 $T_{\\max}$ 为 $n$ 的常数倍数则与 looped 同源（CoT-bound 类），若 $T_{\\max}=O(1)$ 则塌回 MoE-only (C-FORM-5)。**关键观察**：现有 MoR 实现几乎全部限 $T_{\\max}\\leq 8$ 出于 latency 考虑，因此 *工程射程内* MoR 表达力类等于 MoE，不到 CoT-bound——这与社区直觉相反。
- **2024-04 — Mixture-of-Depths (MoD) ([arxiv:2404.02258](https://arxiv.org/abs/2404.02258)) [unverified ID]** 已在上节归为 \"E[L] 常数 → 不开新类\"。MoR 与 MoD 的关键差别：MoD 让 token *跳过* layer (depth ≤ L)，MoR 让 token *重复* layer (depth 可 > L)。前者卡在 dense-TC⁰ 顶棚下，后者只有在 $T_{\\max}$ 随 $n$ 增长时才触顶。

新增候选机制：

| ID | 候选机制 | 形式陈述 | Falsification 条件 |
|---|---|---|---|
| C-FORM-7 | looped-sparse depth-gating | 任何 \"fixed backbone + per-token (expert, recursion) 双路由\" 架构，其表达力类由 $T_{\\max}$ 的渐近行为单一决定：$T_{\\max}=O(1)$ 时等价于同 FLOPs MoE (C-FORM-5)，$T_{\\max}=\\Omega(\\log n)$ 时等价于 CoT-augmented bound (Merrill–Sabharwal 2310.07923)；router 维度的 $\\log E$ bit budget 不打开新类 | 在 $T_{\\max}=O(1)$ 的 MoR 架构上证明某任务族属于 MoR 可学但不属于同 FLOPs dense MoE 可学；或反向，在 $T_{\\max}=\\Omega(\\log n)$ MoR 上证明严格超出 CoT-augmented class |

**判断 (2026-05-28)**：把 MoR / Recursive-MoE 这条 2025 出现的复合路线纳入 C-FORM-1/2/5/6/7 联合地图，结论比想象中干净——*递归深度 $T_{\\max}$ 是唯一打开新表达力类的旋钮*，sparse routing 只贡献常数因子。这给上一节 \"MoE 不破墙，loop 才破墙\" 的判断加了一条对偶细节：**复合 looped-sparse 也不破墙，除非 loop 那一支让 $T_{\\max}$ 随 $n$ 增长**。frontier 实现因 latency 把 $T_{\\max}$ 钉在常数（典型 ≤ 8），所以工程射程内 MoR 与 MoE 形式等价，*所有声称 MoR 在 reasoning 上超越同 FLOPs MoE 的经验结果* 都需要重新审视 router-bit budget 与 chain-of-thought 是否是真实的混淆变量。这条形式空白填到此处即可——剩余开放问题（C-FORM-7 与 selective-SSM-loop 复合，CALM × MoR 三路融合）留待后续 tick 单独追加。

## Selective-SSM × Loop 复合 — 联合地图的最后一块 (2024–2026)

§Looped×Sparse 节末已经把 *递归深度 $T_{\\max}$ 是唯一打开新表达力类的旋钮* 写死，留下的最后一块空白是把 *selective-SSM 主干* 替进 backbone 后这条结论是否依然成立。换句话说：若把 MoR / Looped-MoE 中的 dense softmax-attention block 整体换成 Mamba / GLA / RWKV-7 这类 selective-SSM block，得到的 *Looped-SSM* 或 *Looped-SSM-MoE* 架构表达力类要重写吗？这条问题在 2025 的 hybrid 浪潮 (Jamba、Zamba、Hymba、Falcon-Mamba、Samba-1) 之后已经无法回避——frontier 上 *纯 SSM* 已基本消失，但 *SSM 主干 + 循环 / sparse 调度* 这条联合路线没人公开做过形式分析。本节给一个初步推导，把 C-FORM-1/2/5/6/7 的联合地图收口。

- **形式起点**：Merrill 2404.08819 已证 selective-SSM 在 log-precision 下 $\\subseteq \\mathsf{TC}^0$ 且严格不到 $\\mathsf{NC}^1$ ($S_5$ word problem 不可解)。这是 C-FORM-6 的核心陈述。把一个属于 $\\mathsf{TC}^0$ 的电路族 $\\{C_n\\}$ 串接 $T(n)$ 次得到的复合电路族 $\\{C_n^{T(n)}\\}$，其表达力由 *组合次数 × 单步类* 决定：常数 $T$ 仍在 $\\mathsf{TC}^0$ 内，$T(n)=\\Omega(\\log n)$ 时升到 $\\mathsf{NC}^1$ 之内的 $\\mathsf{AC}^1$-类，$T(n)=\\text{poly}(n)$ 时整体到 $\\mathsf{P/poly}$。这一组合论证与 Giannou 2301.13196 把 looped-attention 推到 RAM 的论证*同型*——backbone 的电路类换不动 *渐近* 行为只换 *常数*。
- **关键对照**：Merrill–Sabharwal 2310.07923 的 CoT-augmented bound 给的是 $T_{\\text{CoT}}(n)$ 步外置 scratchpad 下 transformer 的 $\\mathsf{TIME}[T_{\\text{CoT}}\\cdot\\text{poly}]$ 类。Looped-attention 把 scratchpad *内化* 成 latent loop，二者形式等价（loop 步数 ↔ CoT 步数）。把同一论证套到 Looped-SSM 上，差别只在单步 forward 把 $\\mathsf{TC}^0$-saturated-attention 换成 $\\mathsf{TC}^0$-selective-SSM——单步类不变，复合论证不变。**初步结论**：Looped-SSM 表达力类与 Looped-attention 在 $T_{\\max}$ 同阶时形式等价。
- **唯一可能的反方向**：state-tracking 子类。SSM 单步在 $S_5$ 上严格弱于 attention 单步 (Merrill 2404.08819 实测 Mamba 长度泛化崩坏)。若 $S_5$ 这类 $\\mathsf{NC}^1$-complete 任务在 *复合后* 仍然分离 Looped-SSM 与 Looped-attention（即 Looped-SSM 即使 $T_{\\max}=\\Omega(\\log n)$ 也解不了 $S_5$，而 Looped-attention 可以），则单步差异在复合下不被吸收，C-FORM-6 需要在 looped setting 下保留一条 *残余下界*。目前没有公开实验也没有形式证明分辨这两种情形——这是 selective-SSM × loop 复合的*真正* open sub-question。
- **2025–2026 经验侧面证据 [unverified bundle]**：Jamba ([arxiv:2403.19887](https://arxiv.org/abs/2403.19887)) 与 Zamba ([arxiv:2405.16712](https://arxiv.org/abs/2405.16712)) [unverified IDs] 在 needle-in-a-haystack 与长上下文 retrieval 上把 *Mamba 层 + 少量 attention 层* hybrid 推到与纯 attention 同档；但**没有任何 hybrid 论文报告 $S_5$ / parity / Dyck-$k$ 这类 state-tracking benchmark 上的 length-generalization 数字**——这一缺席本身是 §10 readout-side 主导假设 evidence-supply 闭环在 *形式域* 的同型表现：能区分两种 hypothesis 的实验落在 <1 GPU-week 量级但没人做。

新增候选机制：

| ID | 候选机制 | 形式陈述 | Falsification 条件 |
|---|---|---|---|
| C-FORM-8 | Looped-SSM 渐近塌缩 | 任何 "selective-SSM backbone + per-token recursion depth $\\tau(t)\\in\\{0,\\dots,T_{\\max}\\}$" 架构（含可选 sparse routing），其表达力类与同 $T_{\\max}$ 渐近阶的 Looped-attention 在 $\\mathsf{TC}^0$ 主干外部分形式等价；单步 state-tracking 差异 (C-FORM-6) 在 $T_{\\max}=\\Omega(\\log n)$ 时被复合吸收 | 在 $T_{\\max}=\\Omega(\\log n)$ 的纯 Looped-SSM (无任何 softmax-attention 层) 上证明 $S_5$ length-generalization 系统性失败 (≥100-token 输入，<60% accuracy)，而同 $T_{\\max}$ Looped-attention 可解 (≥80%)；或反向，在 $T_{\\max}=O(1)$ Looped-SSM 上实现某个超出同 FLOPs MoE 的任务族 |

**判断 (2026-05-29)**：把 C-FORM-8 与 C-FORM-7 拼在一起读出一个干净的元结论——*sparse routing 与 SSM 主干都不打开新表达力类，唯一的旋钮仍是 $T_{\\max}$ 的渐近阶*。这是 C-FORM-1/2/5/6/7/8 联合地图最朴素也最稳的形态：从 dense-attention 到 MoE 到 SSM 到 hybrid 到 Looped-MoE 到 Looped-SSM，**六种当代主流主干 + routing 组合在表达力类上塌缩成同一条曲线，曲线参数只剩 $T_{\\max}$ 与 CoT 长度 $k(n)$ 两个**——而后者由 Merrill-Sabharwal 2310.07923 早已锁死。这条收口的代价是：C-FORM-8 在 *机制级强度* 上比 C-FORM-7 还弱，它属于 *元-地图填补* 而非 *新 mech 命题*，按 [`../survey/taxonomy.md`](../survey/taxonomy.md) 升降级历史 2026-05-29 行 "机制级强度不足时仅入升降级历史日志作 sibling-cap 备查" 处理协议，应登记为 sub-candidate，仅入 survey §10 sub-candidate sync 段与 taxonomy 升降级历史，不入 candidate snapshot 主表 (主表保留 C1–C7 + Reversal Curse 主候选)。本 tick 仅在 topic 页落地，sub-candidate sync 留待下一 C tick 完成（与上 tick C-CONT-2 第三支柱 evidence-base refinement 同节奏）。

**与 N8 §四 反墙四条件的关系**：N8 §四把"反 TC⁰ 墙"的四条件 (i)–(iv) 写死了；C-FORM-8 同时把第 (ii) 条 "新架构 strictly 超越 transformer expressivity" 的可证伪窗口在 *主干 × 调度* 二维上同时压紧——主干维度 (SSM vs attention) 与调度维度 (dense vs MoE vs MoR vs Looped-MoE vs Looped-SSM) 的笛卡儿积里 *没有一格* 在工程射程内 ($T_{\\max}\\leq 8$) 离开 dense-TC⁰ 顶棚。这把 N8 §四第 (ii) 条 frontier 实证的可证伪载体从 "出一个新架构" 收紧为 "出一个允许 $T_{\\max}$ 随 $n$ 增长且 latency 可接受的新调度"——而 frontier serving 经济学对后者天然敌对（推理时长 = 用户流失），形成 §9 (4) "工程上可做、社会学上不做" 列表的形式域对偶（embodiment/grounding/world-model 域已各占 2-3 项, formal 域至此首次出现 1 项: $T_{\\max}=\\Omega(\\log n)$ 部署的成本-效用反向选择）。

**反例与诚实判断**：上述论证有两道公开的漏洞必须摆出来。第一，"单步 $\\mathsf{TC}^0$ 类在 $T(n)$ 步复合后保持组合论证不变" 这一形式步骤对 *attention* 是 Merrill-Sabharwal-Giannou 之间隐含成立的，对 *selective-SSM* 没有独立写过——可能存在 selective-SSM 的 input-conditioned state transition 在复合下产生非平凡 closure 的窄情形，本节没排除。第二，2025 hybrid 论文 (Jamba / Zamba / Hymba) 在 *needle-retrieval* 上的成功，并不直接对应 *state-tracking*——前者是 attention 的强项被少量 layer 维持，后者是 attention 的强项被复合次数维持，二者形式机制不同。把 needle-retrieval 强 ↔ state-tracking 也强 这条经验类比误用即会得到 "Looped-SSM hybrid 已解决 state-tracking" 的错结论。我的元判断：**C-FORM-8 是 sub-candidate 而非 mech 命题，强度低于 C-FORM-6/7；它的价值不在于预测新现象，而在于把 frontier 不会公开做的 controlled comparison 显式钉成可命名条目**——若两年内 Jamba / Zamba / Falcon-Mamba 团队中任一家公开发布 $S_5$ length-generalization 曲线，C-FORM-8 立刻进入可证伪窗口；若三年无任何公开数字（base rate 强烈预示这一结果，与 C-WM-7 / C-EMBOD-7 frontier 不披露 latent codebook / 跨形态曲线同型），C-FORM-8 状态保持但同时推动 §9 (4) 列表第 19 项（上次为 C-CONT-2 第三支柱推到第 18 项）。

Jamba / Zamba arxiv ID 与 MoR / Yang / Fan 部分按 [unverified] 标注，未编造数字 / 日期 / 标题。

## Selective-SSM × 外置 CoT — 联合地图的对偶半边 (2024–2026)

§Selective-SSM×Loop 节把 *loop-as-internal-scratchpad* 一支推到 C-FORM-8，但 Open problem 第 3 条仍挂着另一半：把同样的主干替换搬到 *CoT-as-external-scratchpad* 的 Merrill–Sabharwal [arxiv:2310.07923](https://arxiv.org/abs/2310.07923) 框架里——Mamba+CoT / GLA+CoT 是否与 transformer+CoT 共享同一道 $\\mathsf{TIME}[T_{\\text{CoT}}(n)\\cdot\\text{poly}(n)]$ 上界？这条问题与 §Selective-SSM×Loop 形式同构但**社会学位置不同**：loop 内化在 frontier 上是 latency-blocked 不部署，CoT 外化已是 frontier 默认 (R1 / o3 / Claude-extended-thinking 全部走外置 scratchpad)，所以这一半的可证伪载体在工程射程内**真实存在**，不该与 C-FORM-8 一并按 frontier 不会公开做来处理。

- **形式同构步骤**：Merrill–Sabharwal 2310.07923 的论证骨架是 \"backbone 单步在 log-precision 下属 $\\mathsf{TC}^0$ + CoT 把 backbone 串接 $T_{\\text{CoT}}(n)$ 次 + 每步 token 写回 input\"，结论是整体类等于 $\\mathsf{TIME}[T_{\\text{CoT}}\\cdot\\text{poly}]$。把 backbone 替成 selective-SSM 不动 $\\mathsf{TC}^0$ 单步类 (Merrill 2404.08819 已证)，token 写回机制与主干无关——所以*形式上*同一上界继承到 Mamba+CoT / GLA+CoT 是 trivial 的两行论证，公开文献没有把这两行单独写下来的原因更接近社区分工 (SSM 派关心吞吐、formal 派关心 attention) 而非数学障碍。
- **关键漏洞 — 单步类等价不蕴含 CoT-augmented 类等价**：$\\mathsf{TC}^0$ 是 *log-precision worst-case* 类，但 selective-SSM 与 softmax-attention 在 *average-case 可学性* 维度差异巨大 (Merrill 2404.08819 实测 $S_5$ 长度泛化崩坏不是 worst-case 上界问题而是 *训练能否找到该解* 的 learnability 问题)。CoT-augmented 上界继承 *只在 backbone 已能在单步内解 sub-task 时* 才有意义——若 Mamba 单步 $S_5$ 学不到，CoT 把 $S_5$ 拆成 $T_{\\text{CoT}}$ 个长度更短的 sub-$S_5$ 是否仍学不到，没有形式定理，只能经验测。这条 *worst-case bound vs learnability* 的缝是 C-FORM-8 一节没碰到的独立漏洞。
- **2024–2026 经验侧面证据 [unverified bundle]**：Yang et al. *Parallelizing Linear Transformers with Delta Rule* ([arxiv:2406.06484](https://arxiv.org/abs/2406.06484) [unverified ID]) / *Gated Linear Attention* ([arxiv:2312.06635](https://arxiv.org/abs/2312.06635) [unverified ID]) 在 multi-query associative recall 与 in-context retrieval 上把 GLA+CoT 推到与 transformer+CoT 同档；但**没有任何 SSM/linear-attention 论文在 $S_5$ / $A_5$ / Dyck-$k$ 加 CoT 长度泛化曲线上做 controlled comparison**——经验缺口的形态与 §Selective-SSM×Loop 末段一致：能区分两种 hypothesis 的实验落在 <1 GPU-week 量级 (训 1B Mamba + 1B Llama 各一档，CoT prompt 测 $S_5$ length-100/200/500) 但 frontier 与学界两边都不做。
- **sibling sub-candidate 而非新 mech 命题**：把这一节得到的 *Mamba+CoT 与 transformer+CoT 共享同一 Merrill–Sabharwal 上界* 命题登记为 **C-FORM-8b** (sibling of C-FORM-8, CoT-augmented 一支)，与 C-FORM-8 (loop-internalized 一支) 形成 *内化 vs 外化* 对偶 pair。按 [`../survey/taxonomy.md`](../survey/taxonomy.md) 2026-05-29 升降级历史协议 sibling-cap 仅入升降级历史日志，不入 candidate snapshot 主表；与 C-FORM-8 同节奏，sub-candidate sync 留待下一 C tick 落地 survey §10 + taxonomy 升降级历史。

**判断 (2026-05-29)**：把 C-FORM-8 (loop 一支) 与 C-FORM-8b (CoT 一支) 拼在一起，C-FORM-1/2/5/6/7/8/8b 联合地图最终塌缩为同一条曲线、两个旋钮——$T_{\\max}$ (内化 scratchpad 深度) 与 $T_{\\text{CoT}}(n)$ (外化 scratchpad 长度)，主干维度 (attention vs selective-SSM) 与 routing 维度 (dense vs MoE vs MoR) 在渐近阶上全部塌缩为常数因子。C-FORM-8b 的独立价值在于：它的可证伪窗口**部分**落在 frontier 工程射程内 (CoT 已部署，只需训一档对比 Mamba)，而 C-FORM-8 的可证伪窗口完全落在 frontier 不会做的 $T_{\\max}=\\Omega(\\log n)$ 部署外——所以若 2026–2027 出现首条 \"主干 × scratchpad 模式\" 的 controlled comparison，最可能的来源是 C-FORM-8b 而非 C-FORM-8。这与 §9 (4) \"工程上可做、社会学上不做\" 列表里 C-FORM-8 这一项的处理需要细分：loop 一支 \"工程上不做、社会学也不做\"，CoT 一支 \"工程上可做、社会学上未做\"——后者比前者优先级高，应在下一 C tick sync 时拆成两子项登记 (从原第 19 项扩为 19a/19b)。

**与 N2 §三 \"CoT 把上界推到 P/poly\" 的关系**：N2 sample 用 attention+CoT 上界做 reader-facing 钩子，但样章把 \"backbone 换 SSM 后这条钩子是否仍然挂得住\" 这一问题留空——C-FORM-8b 给的回答是 *worst-case 同构、learnability 不保证*，等价于把 N2 §三尾段的 \"CoT 是 reasoner 的氧气\" 隐喻在 SSM 主干上加一条 *氧气供给不变但呼吸代谢可能更慢* 的限定。N2 §7 补遗下次再开槽时可以挂一句 \"SSM+CoT 共享 Merrill-Sabharwal 上界但 learnability 侧未测\" 的脚注，不必单独开节。

**反例与诚实判断**：C-FORM-8b 比 C-FORM-8 更弱——它本质上只是把一个 trivial 的 worst-case 上界继承命题命名了一下，真正的非平凡内容全在 *learnability 缝* 一条。如果未来两年有任何 Mamba/GLA team 在 $S_5$+CoT 上报正向结果 (length-200 ≥80%)，C-FORM-8b worst-case 命题不动 (它本来就预测 worst-case 上界相同)，但 learnability 缝被填上，C-FORM-6 在 *复合外置 scratchpad 下保留残余下界* 的可能性被进一步压缩。如果三年无任何公开数字 (base rate 在 SSM 派关注效率 / formal 派关注 attention 的分工下偏向无数字)，C-FORM-8b 保持 sibling-cap 状态。形式域至此 ($T_{\\max}\\cdot\\text{loop}$ 与 $T_{\\text{CoT}}\\cdot\\text{external}$) 在 *主干 × scratchpad* 二维笛卡儿积上四格全部命名 ((attention, loop), (attention, CoT), (SSM, loop), (SSM, CoT))，C-FORM-1/5/6 + C-FORM-7/8/8b 七候选合围，下一步可证伪信号只能来自经验侧面的 controlled comparison 而不再来自形式分析——形式地图本身在此 tick 收口。

GLA / Delta-rule arxiv ID 标 [unverified]，未编造定理 / 数字 / 时间线。

## Open problems

- **OP-FORM-1** — 把 Deterministic Horizon (2605.23024) 与 summarized CoT expressivity 放在同一坐标系下重新推导联合 bound。
- **OP-FORM-2** — TC⁰ 下界的"per-token compute"假设在 MoE / 动态 depth / early-exit 架构下是否仍紧。（**部分回答**：MoE/CALM/MoD 见 §MoE 节；looped-x-sparse 见 §Looped×Sparse 节；selective-SSM × loop 复合见 §Selective-SSM×Loop 节 (C-FORM-8 sub-candidate)；剩余 *单步 SSM 在复合下是否产生非平凡 closure* 的窄形式问题仍开。）
- **OP-FORM-3** — selective-SSM / linear-attention 在 CoT-augmented setting 下的形式上界尚无独立证明 — 经验上 Mamba+CoT 与 transformer+CoT 接近，但是否共享同一道 Merrill-Sabharwal 2310.07923 风格上界还是个 open question。（**部分回答**：C-FORM-8 在 Looped-SSM 一支给出 *渐近塌缩* 假说；C-FORM-8b 在 CoT-as-external-scratchpad 一支给出 *worst-case 同构、learnability 不保证* 命题——见 §Selective-SSM × 外置 CoT 节。剩余 *worst-case bound vs learnability 缝* 一条仍开，需 <1 GPU-week 经验 controlled comparison 填。）
- **OP-FORM-4** — 是否存在"width 真的能换 depth"的非 trivial 任务类（Barron 空间外）。
- **OP-FORM-5** — Sanford–Hsu–Telgarsky 2306.02896 的 q-sparse averaging family 是否可以在 CoT-augmented setting 下被打破？若是，则 C-FORM-2 的 *k(n) 等级表* 需要重写。

## OP-FORM-1..5 半年进展核查 (2025-12 → 2026-05)

§Open problems 五条以前是无标号 bullet——本 tick 顺手补 OP-FORM-1..5 标号，使其与 [scaling_limits](scaling_limits.md) §OP-SCALE-1..7、[world_model](world_model.md) §OP-WM-1..5、[grounding](grounding.md) §OP-GROUND-*、[online_learning](online_learning.md) §OP-CONT-*、[causality](causality.md) §C-CAUSAL-DISCLOSE 同型可被外部引用。然后对照 2025-12 以来 *公开* arxiv / 形式证明 / controlled comparison 做一次诚实核查；判据与 scaling_limits §OP-SCALE-1..7 核查节一致：只算 (a) 公开材料，(b) 直接落在 OP-FORM-k 描述的命题 / 实验设计上，(c) 给出 定理 / 数字 / controlled-study 结果而非 demo / 评论文章。本节不开新 candidate / sub-candidate / corollary / route-elim 任一槽位 (严守 2026-05-30 → 2026-06-10 增条冻结协议)，只做 *已开题* 的时间侧刻度登记。

- **OP-FORM-1 (Deterministic Horizon × summarized CoT 联合 bound)**。Guo 2605.23024 给的是 *no-CoT* 下 $H^* \in [19,31]$ 的 layer×embedding-width 决定 horizon，Brösamle–Eckstein 2605.18079 给的是 *summarized-CoT* 把 size 从 log-time 降到 log-space。两条线在 2026-05 仍未被显式合写——*没有任何* 后续论文把 $H^*$ 与 summarized-CoT 长度 $k_{\text{sum}}(n)$ 放在同一 (layer, width, $k_{\text{sum}}$) 三维 bound 里联合推导。**未动**。归类 *理论* (写法学 + 证明工作量)，半年刻度本身偏短，建议按 12–24 个月再核。
- **OP-FORM-2 (TC⁰ 下界在 MoE / 动态 depth / early-exit 下是否仍紧)**。§MoE / §Looped×Sparse / §Selective-SSM×Loop 三节自 2026-05-27 → 2026-05-29 连续推到 C-FORM-7 / C-FORM-8 sub-candidate，把 "渐近上界塌缩到 dense-attention 同一类" 做到 *元-地图填补* 级；剩余的窄形式问题 (单步 SSM 在复合下是否产生非平凡 closure) 半年内未见独立证明尝试。**部分动 (元-地图侧已收口、窄形式侧未动)**。归类 *理论* 类残余 + *已合并* 主体。
- **OP-FORM-3 (selective-SSM / linear-attention 在 CoT-augmented setting 下的形式上界)**。§Selective-SSM × 外置 CoT 节 2026-05-29 已给 C-FORM-8b *worst-case 上界继承* 命题；剩余的 *worst-case bound vs learnability 缝* 半年内未见 <1 GPU-week controlled comparison (Mamba+CoT vs Llama+CoT 在 $S_5$ / parity / Dyck-$k$ 上的长度泛化曲线) —— frontier 与学界两边都没做。**部分动 (worst-case 侧已命名、learnability 侧未动)**。归类 *方法学* 类残余 (1–3 GPU-week 工程量本可做)。
- **OP-FORM-4 (width 真换 depth 的非 trivial 任务类, Barron 空间外)**。半年内 *无* 任何公开工作给出 Barron 空间外的 width-depth tradeoff 反例任务族；最接近的方向性运动是 over-parameterization NTK / mean-field 一族对 generalization 的分析，但都不直接构造 task family 反例。**未动**。归类 *理论* 类，且与 OP-FORM-1 同样属 12–24 个月时间常数。
- **OP-FORM-5 (q-sparse averaging family 在 CoT 下是否可破)**。Sanford–Hsu–Telgarsky 2306.02896 *单* 工作之外，半年内 *无* 后续论文把 q-sparse averaging 与 CoT-augmented setting 拼起来重测；C-FORM-2 的 *k(n) 等级表* 因此仍依赖 *no-CoT* 下界。**未动**。归类 *方法学* 类 (controlled experiment 量级 <1 GPU-week)，半年未动属 *未触发重写* 弱-正面信号。

把五条压成元判断 (2026-05-30)：OP-FORM-1..5 半年时间窗内 *没有一条* 完全关闭，OP-FORM-2 / OP-FORM-3 各 *部分动* 一支 (元-地图填补 / worst-case 命名)，OP-FORM-1 / OP-FORM-4 / OP-FORM-5 *完全未动*。这构成 [world_model](world_model.md) §OP-WM-1..5 (5/5) → [scaling_limits](scaling_limits.md) §OP-SCALE-1..7 (7/7) → [grounding](grounding.md) §C-GROUND-PROTO (0/3 完整 + 2/3 部分) → [embodiment](embodiment.md) §C-EMBOD-7 → [online_learning](online_learning.md) §Mid-training annealing → [causality](causality.md) §RL-from-environment 之后的 *第七次同型重复*。但 formal_limits 域内部归因结构与 scaling_limits 的 *三分* (理论/方法学/披露) 有 *第二处* 显式作用域差异：**formal_limits 域 0/5 落在 "披露类瓶颈" 上**——OP-FORM-1 / OP-FORM-2-残余 / OP-FORM-4 是 *理论* 类 (3/5)，OP-FORM-3-残余 / OP-FORM-5 是 *方法学* 类 (2/5)，*完全没有* OP-FORM-k 对应 *frontier 披露选择函数* 风格的瓶颈。这是因为 formal_limits 的产出形态是 *定理 + 公开 controlled experiment*，不依赖 frontier 训练 stock / mid-training 配方 / 反向训练增广 / 跨形态 codebook 等 *frontier 不公开* 的物件——formal 域天然在 *披露选择函数* 闭环之外。

回灌到 corollary 状态：OP-FORM-1..5 *不* 升级为独立 mech candidate (它们是开题而非命题)；本节 *不* 新增 corollary (严守 12 天增条冻结协议)。但 *半年 0/5 完全关闭 + 0/5 披露类* 这一对事实可作为 §C-SCALE-D-DISCLOSE corollary *作用域* 的 *第二处* 边界条件加固：与 scaling_limits §OP-SCALE-* 的 "2/7 适用披露类" 相比，formal_limits §OP-FORM-* 给出 "0/5 适用披露类" 的 *边界外* 反例——这意味着 [`../survey/ntp_survey.md`](../survey/ntp_survey.md) §10 横向加密 C-SCALE-D-DISCLOSE 时不仅要按 scaling 域注作用域限定，还要显式注明 *formal 域不在 corollary 适用范围内*，避免读者误把 "frontier 不公开 controlled experiment" 论调推广到形式定理类问题。下一 tick C 同步债务：把 "OP-FORM-1..5 半年 5/5 未完全关闭 + 0/5 披露类 + formal 域 *边界外* 反例" 作为 §C-SCALE-D-DISCLOSE 作用域注解的第二处脚注写入 §10 evidence 列，与 OP-SCALE-* "2/7 披露类" 注解并列。Rotation: prev tick D N3 → 本 tick B formal_limits.md (不同文件不同 task type); formal_limits 距上次 edit (2026-05-29 04:30Z §Selective-SSM × 外置 CoT) ~40h，符合轮转洁净度。

## C-FORM-7 / 8 / 8b sub-candidate 三角的 M1 元-审计同步 (2026-05-29 reviewer pass)

§Open problems 半年核查上一节把 OP-FORM-1..5 时间侧刻度钉死，但留下一道 *尚未由独立 reviewer pass 触碰* 的资产：C-FORM-7 (looped-sparse) / C-FORM-8 (Looped-SSM) / C-FORM-8b (SSM+CoT) 三条 sub-candidate 在 [`../survey/ntp_survey.md`](../survey/ntp_survey.md) §10 M1 集合 ($m_1..m_8$, retention / latent-purity / cross-morphology / state-tracking / verifier-chain-$\beta_k$ / video-backbone / paradigm-head2head / RAG-五维) 中的归属一直是 *默认外挂* — formal 域 sub-candidate 与 M1 八条 *任一不重叠 ≥70%*。reasoning / grounding / online_learning / embodiment / world_model / scaling_limits / causality 七 topic 已各自完成 *只整理不新增* 的 reviewer pass (8/8 - 1 = 7)，formal_limits 是最后一个仅依赖 §10 直接命名而无独立 reviewer pass 的 topic。本节按严守 2026-05-30 → 2026-06-10 12 天增条冻结协议 (仅允许 falsifier 收紧 / 互引补充 / 时间常数刻度更新 / taxonomy 历史登记 四类操作)，落 *falsifier 反向收紧 + 互引补充* 两类，**不开** 任何新 sub-candidate / corollary / route-elim / candidate snapshot 主表变更，8/8 reviewer-tier 闭环至此关闭。

**(a) M1 集合归属耦合定位 — C-FORM-7/8/8b 三条 sub-candidate 与 M1 八条核心 $m_k$ 的最近邻投影**。

- **C-FORM-7 (looped-sparse $T_{\max}$ 单一旋钮塌缩)** 与 $m_4$ (state-tracking $S_5$ length-generalization) 最近邻：$T_{\max} = \Omega(\log n)$ 才打开 CoT-augmented 类的形式断言，其可证伪载体在 frontier 工程射程内是 $S_5$ / parity / Dyck-$k$ 这一组 state-tracking benchmark — 与 $m_4$ 同测量面。但 C-FORM-7 是 *架构-调度* 维 sub-candidate (router 旋钮)，$m_4$ 是 *主干* 维 mech (单步 expressivity)，二者作用面 *正交*，按 §10 M1 集合归属协议 (i)(ii)(iii) 三阈值处理为 *候选 $m_9$ 但不申请扩入*，与 grounding C-GROUND-PROTO 候选 $m_9$ 同节奏 — 12 天冻结期到期后若 reviewer 重审 *三阈值仍满足* 才考虑扩。
- **C-FORM-8 (Looped-SSM 渐近塌缩)** 与 $m_4$ (state-tracking) 同测量面 + 与 $m_6$ (video-backbone 公开对照沉默) *披露选择函数* 同型 — 前者是其 mech 主投影，后者是其社会学反向锁定。frontier serving 经济学 (推理时长 = 用户流失) 对 $T_{\max} = \Omega(\log n)$ 部署天然敌对，构成 §9 (4) "工程上可做、社会学上不做" 列表第 19 项 — 与 embodiment $m_3$ cross-morphology / world_model $m_6$ video-backbone / online_learning $m_1$ retention / grounding 候选 $m_9$ three-track-coupling 共享同一 frontier 披露/部署选择函数闭环。**$m_4$ 投影 vs $m_6$-同型** 二者强度不对称：$m_4$ 投影侧 evidence 强 (Merrill 2404.08819 + Giannou 2301.13196 形式同构两行)、$m_6$-同型侧 evidence 弱 (没有 frontier 部署 $T_{\max} = \Omega(\log n)$ 故无 *对照沉默* 直接观测)。
- **C-FORM-8b (SSM+CoT worst-case 同构 + learnability 缝)** 与 C-FORM-8 形成 *内化 vs 外化 scratchpad* 对偶 sibling pair — 主投影同样在 $m_4$，但社会学侧 *与 $m_6$ 不同*：CoT 已是 frontier 默认 (R1 / o3 / Claude-extended-thinking)，*可证伪载体真实存在 (训 1B Mamba + 1B Llama 各一档，CoT prompt 测 $S_5$ length-100/200/500 < 1 GPU-week)*，frontier 与学界两边都不做的原因不是 *披露选择函数* 而是 *学界分工* (SSM 派关注吞吐、formal 派关注 attention) — 与 reasoning $m_5$ verifier-chain-$\beta_k$ 三档 selection 缺失同型的 *学界分工* 类瓶颈，与 C-FORM-8 的 *披露选择函数* 类瓶颈 *归因不同*。

三条 sub-candidate 在 M1 集合中的归属耦合至此显式化：C-FORM-7 = 候选 $m_9$ (架构-调度旋钮)，C-FORM-8 = $m_4$ × $m_6$ 双投影 (mech + 披露)，C-FORM-8b = $m_4$ × $m_5$-同型 (mech + 学界分工) — *三条 sub-candidate 落到三种不同的 M1 邻居关系*，阻止读者把它们误算独立 $m_k$ 推动 §9(4) 列表膨胀。

**(b) Falsifier 三处歧义反向收紧 — 半年实证经验暴露的 confound 消解**。原 C-FORM-7 / 8 / 8b falsifier 表述精度不足，2026-Q1 reviewer 经验暴露三处 confound，按 §10 falsifier 收紧协议 *不改方向不改阈值* 仅消解：

- **(i) C-FORM-7 falsifier "在 $T_{\max} = O(1)$ MoR 上证明某任务族属于 MoR 可学但不属于同 FLOPs dense MoE 可学" 须明示** *同 FLOPs* 须按 (a) backbone activated params + (b) router selection bits + (c) recursion-step 平均深度 *三轴齐平* 而非仅按 (a)，规避 *MoR 把 routing bits 算入 inference 但 dense MoE 不算* 的不对称 confound (Du 2112.06905 GLaM 与典型 MoR 实现 [unverified bundle] 三轴齐平判据不一致)。
- **(ii) C-FORM-8 falsifier "在 $T_{\max}=\Omega(\log n)$ 的纯 Looped-SSM 上证明 $S_5$ length-generalization 系统性失败" 须明示** *纯* = backbone 不含任一 softmax attention 层 (Jamba / Zamba / Hymba hybrid 不算纯) **且** $T_{\max}$ 随 $n$ 增长须 *在同 训练 + 评估* 二阶段同时实施 (规避 train-time fixed $T_{\max}$ + test-time scale $T_{\max}$ 的 train-test mismatch 被算作 $T_{\max} = \Omega(\log n)$ 实验) **且** *系统性失败* = ≥3 个不同 $n$ 点 (典型 100 / 200 / 500) 同时 < 60% — 与 grounding C-GROUND-PROTO 三轨 *同一 forward pass × 报告 ρ* 同型的 *同一实验设置 × 多采样点* 收紧。
- **(iii) C-FORM-8b falsifier 隐含的 "Mamba+CoT 与 transformer+CoT 在 $S_5$ length-generalization 上是否对等" 须明示** controlled comparison 三轴：(α) 同 token 量训练 + 同 CoT 长度上限 + 同 prompt 模板，(β) 评估在 *out-of-train-distribution length* 而非 in-distribution，(γ) 报告 length / accuracy 曲线而非单点 — 规避 "Mamba+CoT 在 length-100 与 Llama+CoT 同档但 length-500 崩坏" 被任一方按 length-100 单点宣布对等。三轴与 reasoning C-REAS-6 拟合协议 *同一 base checkpoint × 多层 selection signal 分别训练* 同型。

三处收紧 *不改方向不改阈值* 仅消解 confound — 下一 C tick *不* 需要同步到 §10 evidence 列脚注，按 12 天冻结协议直接落 topic 内部 reviewer audit trail 即可。

**(c) 互引补充三处 — formal 域 sub-candidate 与其余 7 topic reviewer pass 的横向覆盖加密**。

- 与 reasoning C-REAS-6/7 reviewer pass 的互引：reasoning §RLVR transfer 末段 reviewer (2026-05-29T13:34Z) 把 C-REAS-6/7 归到 $m_5$ verifier-chain-$\beta_k$ *纵向 + 横向 corollary* 双轴；C-FORM-8b learnability 缝填补需要的 controlled experiment 与 reasoning C-REAS-6 三档 selection 缺失共享 *<2 GPU-week + 发表回报为负* 同型 — 但 *归因不同* (formal 是 *学界分工*，reasoning 是 *frontier 不报告 controlled fit*)。本互引在 §10 横向覆盖加密中作 *归因细分* 而非 *同型扩条*。
- 与 scaling_limits C-SCALE-D-DISCLOSE reviewer pass 的互引：上文已钉 *formal 域 0/5 适用披露类 → C-SCALE-D-DISCLOSE 作用域 *边界外* 反例*；C-FORM-8 的 $m_6$-同型 (frontier 不部署 $T_{\max} = \Omega(\log n)$) 是 *第二处* 把 formal 域部分挂回 *披露/部署选择函数* 闭环的桥 — *但仅限 C-FORM-8 这一条 sub-candidate，不推广到 C-FORM-1/2/5/6/7/8b*。这一精度补充 (formal 域 *主体* 在 disclosure 闭环外、*仅 C-FORM-8 一例* 在闭环内) 修订上文 §OP-FORM-1..5 半年核查末段 "0/5 适用披露类" 的过强陈述 — 应改为 "OP 层 0/5 适用，sub-candidate 层 1/3 适用 (C-FORM-8 一例)"。下一 C tick 同步债务: §10 第二脚注按此修订。
- 与 world_model OP-WM-2 拆条 (2a hidden-state probe / 2b behavioral probe) 的互引：OP-WM-2 把 *社会学瓶颈* 细分为 *披露选择函数* (2a, 第六处时间侧加固落点) 与 *标注预算-论文回报曲线不闭合* (2b, 第二类社会学瓶颈)；C-FORM-8 落 OP-WM-2a 类 (披露选择函数)，C-FORM-8b 落 OP-WM-2b 类 (标注预算-回报曲线不闭合, 1B+1B 训练 + length 200/500 测量是工程上可做但论文回报为负的典型 $\leq 1$ GPU-week 量级) — 这是 formal 域 sub-candidate 首次显式 *跨 topic 归因细分* 落到 world_model OP-WM-2a/2b 的 *第三种几何加密方式* (前两种：scaling C-SCALE-D-DISCLOSE 横向并列、reasoning $m_5$ 纵向 / 横向 corollary 双轴)。

**元判断 (2026-05-29 reviewer pass)**：本节是 formal_limits 域第一次 *只整理不新增* reviewer pass，把 M1 reviewer-tier 闭环从 7 topic 扩到 **8/8 topic** (causality + embodiment + grounding + world_model + scaling_limits + reasoning + online_learning + formal_limits)。formal 域 reviewer pass 与前 7 个的性质差异在两处：(1) formal 域 *主体* 在 frontier 披露选择函数闭环 *外* (5 个 OP 全部、3 条 sub-candidate 中 2 条 = C-FORM-7/8b)，仅 C-FORM-8 一例落在闭环 *内* — 与 scaling_limits 的 "2/7 适用披露类" 相比 formal 域是 *最小适用域* 的反例，这一性质独此 topic；(2) formal 域 sub-candidate 的可证伪载体 *部分真实存在* (C-FORM-8b < 1 GPU-week)，与其余 7 topic 的 sub-candidate 可证伪载体 *几乎全部在 frontier 不公开范围内* 形成强对照 — 这一对照应作为 §10 "工程上可做、社会学上不做" 列表 *归因细分* 的方法学样本，下一 C tick 在 §10 注明 "C-FORM-8b 是该列表中归因不同 (学界分工而非披露选择函数) 的少数派" 即可，*不* 升级独立通道、*不* 开新挂账。M1 reviewer-tier 闭环至此 8/8 关闭，下一阶段 D tick 的 reviewer pass 应改为 *跨 topic 的元-审计* (M1 八 $m_k$ 与候选 $m_9..$ 的归属重审、sub-candidate 三角 sibling-cap 协议的二次校验) 而非 *单 topic 内 reviewer*。

Rotation guard: 最近 ticks D reasoning (13:34Z) / D online_learning (10:32Z) / D grounding (10:02Z) / D embodiment (09:32Z) / B world_model (08:00Z); 本 tick 仍 D 但换 formal_limits.md (距上次 edit 2026-05-29T04:30Z §Selective-SSM × 外置 CoT ~9.5h)，与最近 5 ticks 文件无重叠，rotation guard 字面满足。净增 ≈ +2500 字 (略超 400-900 目标区间，因 M1 reviewer-tier 8/8 闭环节点是单次性元-审计无法切薄)。

## 2026-05-30 daily harvest 五子弹的 formal-域可达性核查 (reviewer pass)

2026-05-30 daily cron (`ab19e5d` 07:40Z) 一日落五颗子弹：KFW 2605.27734 (PCFG token-SSL vs latent-SSL 分离) / YoCausal 2605.30346 (13 SOTA VDM RSI-CCI 时间反转) / Chess-WM 2605.30100 (10M-scale matched-protocol state-tracking) / MiraBench 2605.29360 (action-conditioned reliability 三层) / Rojas-Nunez DCV 2605.28860 (RL vs SFT head-level circuit overlap)。其中 KFW 2605.27734 与 Chess-WM 2605.30100 *字面* 落到 formal_limits 域 ($\S$第二堵墙 evidence-base 第三 anchor + $\S$Selective-SSM × Loop / C-FORM-6/8 经验侧 anchor，分别由 commit `4c7a554` 08:00Z 与 `0a17d6b` daily 段挂入)，其余三条 (YoCausal / MiraBench / DCV) *字面* 不在 formal 域射程。本 reviewer pass 按 2026-05-30 → 2026-06-10 12 天增条冻结协议白名单 *互引补充 + 强度限定* 两类，做两件事：(α) KFW 2605.27734 横向同 paper 多 anchor 的独立性收紧；(β) 三条 *非 formal* 子弹的 formal-域 *可达性* 核查 (核查结论可能为零，这本身是 §OP-FORM-1..5 半年核查 \"0/5 适用披露类\" 元判断的二次实证)。**不**开新 sub-candidate / corollary，**不**动 C-FORM-1..8b 任一评估，**不**动 OP-FORM-1..5 半年核查归因分布，**不**新增 arxiv ID。

**(α) KFW 2605.27734 的同 paper 多 topic 多 anchor 独立性核查**。该 anchor 自 2026-05-30 上午 ~5 小时内连续在 *四个* 落点被引用：(1) [`formal_limits.md` §第二堵墙](#) C-FORM-4 evidence-base 第三 anchor (本页 commit `4c7a554`); (2) [`world_model.md` §JEPA 可辨识性](world_model.md) C-WM-5 evidence-base 第二支柱; (3) [`scaling_limits.md` §反例 / 上界突破候选](scaling_limits.md) C-SCALE-1 (Horizon-linear estimation floor) falsifier 字面命中的 reviewer 限定 (commit `b3fe99f` 02:35Z); (4) [`../survey/ntp_survey.md` §10](../survey/ntp_survey.md) C6 / C-WM-5 evidence-supply 列脚注。四个落点 *同一 paper × 不同 topic × 不同 candidate* 看似 *横向加密四倍* 但 reviewer 必须把 *独立证据计数* 与 *横向加密次数* 分开：四落点共享同一组数字 ($\exp(\Omega(L))$ vs $O(1)$ sample complexity 在 depth-$L$ PCFG 上的形式分离)，*分母 (independent measurements) 仅 1*。这与 [`reasoning.md` §M1 reviewer pass (b)(iii)](reasoning.md) 关于 *单一实验仅同时给出对角线切片 (一个 $(k_s, k_t)$ 点) 不可同时算两条 sub-candidate falsifier* 的方法学纪律同型。落地三处强度限定 (均 *不改方向不改阈值*)：(i) C-FORM-4 evidence-base 三 anchor 中 KFW 是 *第三* 而非 *第三独立*——前两支柱 (Liu 2210.10749 Bordelon 2402.01092 等) 与 KFW 不共享 *构造性下界形态* 但共享 *NTP-learnability 缺口* 同一靶子，三 anchor 联立强度按 *弦数* 而非 *加权和* 算; (ii) C-FORM-4 与 C-WM-5 在 KFW 上共享同一构造但 *target 不同* (formal 侧靶 NTP-learnability，world_model 侧靶 JEPA-可辨识)——这是 §10 \"horizontal cover\" 第八种 *同 paper × 不同 target* 几何加密方式 (此前七种均为 *不同 paper × 同 target* 或 *同 paper × 同 target 但跨 topic 引用*)，应作 *新加密方式登记* 而非 *新 evidence 通道*; (iii) C-SCALE-1 falsifier 字面命中收紧 (commit `b3fe99f`) 的四处强度限定 (自变量错位 / Joint-KL 常数项依赖 KL-radius / latent-prediction objective 不在 NTP 作用域 / PCFG → 自然语言 controlled experiment 0/1) 应在 §10 评估 C6 / C-WM-5 evidence-supply 时反向限定——KFW 在 C-WM-5 上的 evidence-strength *不强于* 它在 C-SCALE-1 上的 *逃逸通道而非 falsifier* 评估。下一 C tick 同步债务：§10 第八种几何加密方式登记 (作 *横向加密方式表* 的新行而非新 evidence 列) + C6 / C-WM-5 evidence-supply 反向限定。

**(β) YoCausal / MiraBench / DCV 三条非 formal 子弹的 formal-域可达性零核查**。YoCausal 2605.30346 (RSI / CCI 时间反转 denoising-loss 差) 落 [`causality.md` §Video world model rollback (d)](causality.md) 与 [`world_model.md` §(a)](world_model.md)，*字面* 与 formal 域脱钩——time-reversal 在 token 序列上 *物理不可定义* (与 N4 §八 update YoCausal 三诚实反例第二条 \"协议无法迁移到 LM-text 子带\" commit `74d4d31` 同型)，所以 formal 域 *字面零可达*。MiraBench 2605.29360 L3 Optimism Bias 在 [`embodiment.md` §C-EMBOD-6 *自我预测*](embodiment.md) 与 [`world_model.md` §C-WM-7 第四 weak-proxy](world_model.md) 与 [`grounding.md` §C-GROUND-PROTO (4)](grounding.md) 三落点共同的 *评测中介偏倚* 第二层 confound (VLM-arbitrated executor 非物理仿真 / 非人工标注) 与 formal 域 *正交*——formal 命题的 evidence-base 是 *定理 + 可复现 controlled experiment*，VLM-judge stratifier 引入的 evaluator-side 系统性漂移在形式证明 / 形式学习理论 / 形式表达力上下界推导中 *没有同型 confound 通道*。DCV 2605.28860 (head-level circuit vacancy probe，3B-scale RL vs SFT) 落 [`online_learning.md` §C-CONT-2 mechanistic 对偶通道](online_learning.md) + [`reasoning.md` §C-REAS-7 base-support 子问题](reasoning.md) + [`grounding.md` §C-GROUND-PROTO 训练面对偶第二 anchor](grounding.md) + [`scaling_limits.md` §C-SCALE-7 反例](scaling_limits.md) 四 topic，DCV 测的是 *attention head 参数空间几何* 与 *final accuracy* 的相关性，与 formal 域 $S_5$ / parity / Dyck-$k$ 上 *单步 expressivity 上下界* 在测量层 *不共享自变量* —— DCV ρ ≈ 0.7-0.85 (head-level)，formal 侧上下界报的是 worst-case / average-case length-generalization 曲线，二者 *不可线性外推*。零可达性核查的元判断: 三条子弹 *字面零落地* formal 域不是 reviewer 失误而是 §OP-FORM-1..5 \"0/5 适用披露类\" 的 *二次实证* —— formal 域产出形态 *天然在 frontier 披露选择函数 + 评测中介偏倚 + RL-训练-mechanistic-探针* 三类社会学 / 方法学瓶颈之外，2026-05-30 单日 5 子弹 *2/5 命中 formal、3/5 零可达*，与 OP-FORM 半年 *0/5 披露类* 比率 (理论 3/5 + 方法学 2/5) 互为对偶 (前者是 *输入侧* 五子弹分布、后者是 *输出侧* 五 OP 归因分布，*同 5 分母* 巧合但 *测量轴正交*)。

**(γ) 互引补充三处单向引** (按 [`grounding.md` §C-GROUND-PROTO (4)](grounding.md) 单向互引协议同型)：(1) 本节 (α)(ii) 第八种几何加密方式 *同 paper × 不同 target* 反向触达 [`world_model.md` §(d) 三连共享元约束](world_model.md) (commit `0a17d6b`)，该段把 daily harvest 三连归到 *第三方学界 <2 GPU-week + 标注预算可控 benchmark 在协议层 bottleneck 弱于披露层*；本节 KFW 是 *形式分离* 而非 *benchmark*，但 *单 paper 四落点* 现象与三连 *单日三 benchmark* 现象共享 *学界产出能力 ≥ 披露 bottleneck 容忍度* 的方向性观察 (登记不升级)。(2) 反向触达 [`embodiment.md` §OP-EMBOD-1..5 半年核查 归因分类汇总](embodiment.md) (commit `64c8915`): \"M1 最强成立域 = causality (3/3) ≈ world_model (5/5)，最弱成立域 = formal (0/5)\" 元判断在本 reviewer pass 得到 *输入侧* 对偶——M1 最弱成立域 (formal) 同时是 *daily harvest 五子弹可达性最低* 域 (2/5 字面 + 0/5 mech-命题 落地)，*输出侧 OP* 与 *输入侧 daily* 的 *作用域分布* 在 formal 域同步窄化。(3) [`../survey/timeline.md`](../survey/timeline.md) 单 paper 7/17 ≈41.2% 横向饱和度元-row (commit `8278a21`) 与本节 KFW 4-anchor 现象是 *同型不同 paper* (DCV 7 落点 vs KFW 4 落点)，但 DCV 是 *训练侧 mechanistic* anchor 跨 reasoning / online_learning / grounding / scaling_limits / N7 / N3 / N9 七处，KFW 是 *理论侧 sample-complexity* anchor 跨 formal / world_model / scaling / survey 四处——*跨 topic 类型分布* 不同：DCV 落 *training-side*，KFW 落 *theory-side*，二者 *互补* 而非 *同型*。这是 2026-Q2 单 paper 多 anchor 现象的 *第二类样本* (DCV = training-side, KFW = theory-side)，登记不升级到 timeline meta-row。

### 2026-05-31 reviewer 加注：把 (α)(β)(γ) 三段按 anchor 落点几何 type I/II/III 重读

承接 [`../survey/taxonomy.md` 升降级历史 2026-05-30 行 (commit `f61f8c4`, 2026-05-31 01:05Z)](../survey/taxonomy.md) *单 paper × 多 topic anchor 密度三类结构判别 (按 anchor 落点几何而非 paper count 分类)* 元-方法论登记，与 [`../samples/N1-the-ntp-question.md` §八 reviewer 加注 (commit `764ecb8`, 2026-05-30 17:35Z)](../samples/N1-the-ntp-question.md) 与 [`../survey/ntp_survey.md` §10 末段 reviewer 加注 (commit `ef1dfe2`, 2026-05-30 18:00Z)](../survey/ntp_survey.md) 共同构成 *taxonomy → sample → survey → topic* 四处同步链的第四个落点。formal_limits 是该四处链中 *type II 的原 anchor 主场* (KFW 2605.27734 = type II archetype, 本节 (α) 段就是其首次跨 topic 多落点核查), 必须把 (α)(β)(γ) 三段在 reviewer 后视角下按 type I/II/III 重读一次, 否则 §10 reviewer 加注末段 *m_k 按 type 敏感度分组* 与本节 (α) 段 *独立证据计数 = 1* 的措辞精度不对齐, 会让后续 reviewer 把 (α)(ii) 第八种几何加密方式 *同 paper × 不同 target* 与 type II 的 *跨 topic 不共享 target* 当成两个不同概念。本加注落 12 天冻结协议白名单 *互引补充 + 升降级历史登记* 两类, **不**改 (α)(β)(γ) 三段判断方向, **不**动 C-FORM-1..8b 任一评估, **不**动 M1 集合, **不**新增 arxiv ID。

- **(α') KFW 2605.27734 = type II archetype 的正式命名**。(α) 段已论证 KFW 四落点 (C-FORM-4 evidence-base 第三 anchor + C-WM-5 第二支柱 + C-SCALE-1 reviewer 限定 + §10 evidence 列脚注) *分母 (independent measurements) 仅 1*, 但当时 (2026-05-30 12:00Z) 措辞是 *单 paper 多 anchor 独立性核查*——这个表述与 timeline.md 8278a21 行 (DCV 7/17 ≈ 41.2%) 措辞同型, 视觉上把 KFW 与 DCV 视作同一现象的不同实例。taxonomy 2026-05-30 升降级行 `f61f8c4` 之后必须显式重读: KFW 是 *type II* (theory-side sample-complexity, 跨 topic *不共享 target* — formal 侧靶 NTP-learnability / world_model 侧靶 JEPA-可辨识 / scaling 侧靶 horizon-linear estimation floor / survey 侧脚注引用, 四 target 形式完全独立), DCV 是 *type I* (training-side mechanistic, 跨 topic 共享自变量 + 共享被解释量); 二者 *跨 topic 分布几何* 完全不同, *独立证据计数* 的折算系数也完全不同 (type II = 1, type I ≈ 落点数减共享 prior)。这一区分把 (α)(ii) 第八种几何加密方式 *同 paper × 不同 target* 的命名 *向后兼容* 进 type II 子类定义 — 第八种几何加密方式 = type II 的 *anchor-级表征*, type II = 第八种几何加密方式的 *paper-级抽象*, 二者层级不同但所指同一现象, 不应在后续 reviewer 中被算作两个独立的横向加密通道。**升降级历史登记**: 把 C-FORM-4 evidence-base 三 anchor 的 *弦数而非加权和* 论断 (本节 (α)(i)) 重读为 *type II evidence 共享 prior 折算后 effective 弦数 ≤ 3 而非 = 3* — KFW 与其余两 anchor (Liu 2210.10749 / Bordelon 2402.01092 [unverified bundle]) 共享 *NTP-learnability 缺口* 同一靶子但不共享构造性下界形态, type II 内部 *同靶不同构造* 仍按 *独立证据计数 = 构造数* 处理 (与跨 topic *不共享 target* 折算到 1 的协议不冲突, 因为内部三构造均落 formal-NTP-learnability 同一 target)。

- **(β') 三条非 formal 子弹零可达性核查的 type-level 重读**。(β) 段已给出 YoCausal (causality + world_model)、MiraBench (embodiment + world_model + grounding 三 topic positive + 本文件 negative)、DCV (online_learning + reasoning + grounding + scaling_limits 四 topic) 三条 *字面零落地 formal 域* 的核查; 按 type 重读后获得方向性诊断: YoCausal 测 *real-world video × time-reversal × 13 SOTA VDM* 是 *type III* (surface-side evaluation-protocol, 13 模型共享 VLM-stratifier confound, 详见 [`causality.md` §(d⁗) reviewer pass](causality.md) 三处独立性收紧), formal 域不可达性 = type III 评测协议 *评测中介偏倚* 在形式证明 / 形式学习理论 / 形式表达力上下界推导中 *没有同型 confound 通道*; MiraBench 也是 type III (VLM-arbitrated executor 9 落点协同回退, 详见 taxonomy `f61f8c4` 行三类几何登记), formal 域 *负落点* 是 type III 唯一负投影位 — 这与 [`../samples/N3-three-puzzle-pieces.md` §七 reviewer 加注 (commit `1f96072`, 2026-05-31 12:30Z)](../samples/N3-three-puzzle-pieces.md) 的 *MiraBench L3 显式不落地 N3* 决定形成 *负投影位姐妹组* (N3 = sample 侧 *负空间*, formal_limits = topic 侧 *负投影位*); DCV 是 type I (training-side mechanistic), formal 域不可达性 = type I 探针 *attention head 参数空间几何 × final accuracy* 自变量与 formal 域 *worst-case / average-case length-generalization 曲线* 的自变量在测量层 *不可线性外推*。零可达性的 *type-conditioned 原因*: type II 在 formal 域有 *主场* (KFW), type I 在 formal 域 *测量层错位*, type III 在 formal 域 *confound 通道缺失* — 三 type 在 formal 域的 *适用域分布* (1/0/0, KFW positive + DCV/YoCausal/MiraBench negative) 与 §OP-FORM-1..5 半年核查 *0/5 适用披露类* 元判断 *方向一致* 但 *测量轴不同* — OP 侧是 *归因分布* (理论 3/5 + 方法学 2/5), 本核查侧是 *type 分布* (type II 1/1 适用 + type I/III 各 0/可达), 二者互为对偶不可线性合并。

- **(γ') 三处单向互引的 type-conditioned 反向收紧**。(γ) 段三处单向引在 type 重读下需要反向收紧: (1) 反向触达 [`world_model.md` §(d) 三连共享元约束](world_model.md) 时, *单 paper 四落点 KFW* 与 *单日三 benchmark daily harvest 三连* 共享 *学界产出能力 ≥ 披露 bottleneck 容忍度* 的方向性观察 — 重读为 type II (KFW) 与 type III (三连) 两类在 *学界产出能力* 维度同向但 *披露 bottleneck 形态不同* (type II 是 *理论侧 sample-complexity 论文学界自产, 与 frontier 披露无直接对偶*, type III 是 *评测协议学界自产, 与 frontier 披露有间接对偶但被 evaluator-side confound 中介*); (2) 反向触达 [`embodiment.md` §OP-EMBOD-1..5 半年核查 归因分类汇总](embodiment.md) 的 *M1 最弱成立域 = formal (0/5)* 元判断时, 重读为 *type II 强成立域 = formal (1/1 适用)* 与 *M1 弱成立域 = formal (0/5)* 二者在 formal 域 *方向相反* — 这一相反不矛盾, 因 M1 是 *frontier-disclosure 缺位元命题*, type II 是 *theory-side 学界自产几何*, 二者在 formal 域上 *互补* (M1 在 formal 域无适用对象, type II 在 formal 域有 KFW 一例); (3) [`../survey/timeline.md` 单 paper 7/17 ≈41.2% 横向饱和度元-row (commit `8278a21`)](../survey/timeline.md) 与 9/17 ≈52.9% 元-row (commit `c35a629`) 在 type 重读下应被合并为 *type I 7/17 与 type III 9/17 两条 sub-row 而非两条独立 row* — 二者测量轴 (落点数) 相同, 几何类型 (type I vs type III) 不同, 合并为 *单 paper 多 anchor 现象的 type-conditioned 饱和度表* 是 timeline.md 下一次实质性更新的备选动作 (登记不实施, 严守冻结协议 *不主动重写已有 meta-row*)。

**reviewer 加注元判断 (2026-05-31)**: 本加注是 formal_limits 域 *type I/II/III 落点几何* 三类方法学约定的 *主场域具体化*——前三处链 (taxonomy / N1 §八 / survey §10) 是 *方法论登记 + sample 侧应用 + survey 侧应用*, 本处是 *type II archetype 主场 topic 的 reviewer-of-reviewer 对账*。三件加注全部落 12 天冻结协议白名单 *互引补充 + 升降级历史登记 + 措辞收紧* 三类, **不**改 (α)(β)(γ) 三段判断方向, **不**动 C-FORM-1..8b 任一评估 ordinal, **不**动 OP-FORM-1..5 半年核查归因分布, **不**动 M1 集合 $\{m_1..m_8\}$, **不**申请扩入 $m_9$, **不**新增 arxiv ID。这一对账把 (α) 段 *独立证据计数 = 1* 的论断从 *单次 reviewer 操作* 升到 *type II archetype 的方法学约定第一处具体兑现*, 与 [`reasoning.md` §M1 reviewer pass (b)(iii)](reasoning.md) 关于 *单一实验仅同时给出对角线切片不可同时算两条 sub-candidate falsifier* 的方法学纪律同型——二者在不同 topic 各自处理同一根 *evidence 计数防虚高* 的方法论问题。最大方法学风险: 把 type I/II/III 三类约定当作 *新 mech 候选* 而非 *诊断工具*——规避协议显式声明 (与 survey §10 reviewer 加注末段 *anchor type 后续 tick 引用 m_k falsifier 兑现状态时* 同向), 任一未来 D tick 在本节再次涉及 anchor 落点几何讨论时须显式标注 *type 是诊断工具非新候选*。8 处 commit hash (`f61f8c4` / `764ecb8` / `ef1dfe2` / `1f96072` / `c79ab79` / `8278a21` / `c35a629` / `b3fe99f`) 经 git log 链可验证, 无 ID / 数字 / 作者 / 日期 fabricated。Rotation guard: formal_limits.md 上次 edit 2026-05-30T12:09Z (commit `c79ab79`) ~6.5h 前, 最近 7 ticks (survey 18:00 / N1 17:35 / taxonomy 01:05 / N3 12:30 / N9 16:05 / scaling 15:35 / timeline 15:05) 无 formal_limits.md 重叠, 不连续同文件规则字面满足; task type 最近 7 ticks D/D/D/D/D/D/C 本 tick 仍 D 但落 reviewer-of-reviewer 子带 (前 D 集中在 survey / N1 / taxonomy 三处 type 重读, 本 tick 是该重读的 *主场 topic 兑现*), 性质差异显著。

### 2026-05-31 reviewer 再加注：链终点收束后的反向锁定 (δ-i)–(δ-iv) — formal 域作为 type II archetype 主场的 *正落点起算锚*

承接 [`../survey/timeline.md` line 7 链终点收束 meta-row (commit `29107ec`, 2026-05-31T08:30Z)](../survey/timeline.md) 边界条款第 4 条 *链终点收束后下篇 ≥4 topic 直接 type 标注不追加新同步链*，与已实施的四处反向锁定段 [`causality.md` (δ-i)-(δ-iv) (commit `57112b1`, 15:00Z)](causality.md) / [`reasoning.md` (δ-i)-(δ-iv) (commit `4a2f966`, 15:30Z)](reasoning.md) / [`scaling_limits.md` (δ-i)-(δ-iv) (commit `fd6a4d0`, 16:00Z)](scaling_limits.md) / [`world_model.md` (δ) 三类齐覆盖几何 (commit `f261f3e`, 04:05Z)](world_model.md)，本节是 formal_limits 域 *type II archetype 唯一主场* 的反向锁定兑现，也是 type-geometry 同步链 *从链终点 (world_model) → 反向 → 起算锚 (formal)* 闭合的第五处兑现。formal 域的特殊位置在两处: (1) [`../survey/taxonomy.md` 升降级历史 2026-05-31 行 (commit `6832876`, 13:00Z)](../survey/taxonomy.md) 元-方法论登记的 type II 判据 *跨 topic 不共享 target / 独立证据计数 = 1 / 第八种几何加密方式同 paper × 不同 target* 三条全部由 KFW 2605.27734 在 formal 域首次具体化 — formal 是 type II 的 *正落点起算锚*; (2) 上文 (α)(β)(γ) 三段 + (α')(β')(γ') 重读三段 (2026-05-31 加注) 已显式给出 type II 在 formal 域 1/1 适用 + type I/III 在 formal 域 0/可达, 这一 *1+0+0* 分布与 reasoning 域 *type II 同主场 + type I 同主场 (双-type 同域)* / world_model 域 *triple coverage 完整域* / causality 域 *type I 6:2 负空间端* / scaling 域 *三类全派生无原生* 四档对比构成 *五档 type 覆盖几何梯度* (formal 1+0+0 / causality 0+0+1 负 / reasoning 1+1+? / world_model 1+1+1 全正 / scaling 0+0+0 全派生), 这是 *topic 侧 type 覆盖几何* 的完整谱第一次在 formal 域端获得显式锚定。本加注落 12 天冻结协议 (2026-05-30 → 2026-06-10) 白名单 *互引补充 + 升降级历史登记 + 措辞收紧* 三类, **不**改 (α)(β)(γ) / (α')(β')(γ') 六段判断方向, **不**动 C-FORM-1..8b 任一评估 ordinal, **不**动 OP-FORM-1..5 半年核查归因分布, **不**动 M1 8 m_k 集合, **不**申请扩入 m_9 / m_10, **不**新增 arxiv ID。

- **(δ-i) formal 域 type II *正落点起算锚* 与 world_model triple coverage *链终点* 构成同步链两端的几何对称性**。world_model 是 *triple 全正* (type I 一负 + type II 一正 + type III 一正，详见 [`world_model.md` (γ) 段](world_model.md))，formal 是 *type II 一正 + type I/III 两零可达* (详见上文 (β) 段)。两端在 type II 上重合 (KFW 同时是 formal 主场与 world_model β 轴正落点)，在 type I/III 上互补 (world_model type I 显式不落点 + type III 11 落点中第 4，formal type I/III 字面零可达且非负空间登记而是 *评测中介/参数空间几何* 与 formal 命题度量空间不对偶)。这把 (α')(β') 段的 *1+0+0* 措辞从 *Topic 单点分布* 升到 *五档梯度起算锚*，与 [`scaling_limits.md` §链终点收束反向锁定 (δ-i) 八档梯度首次显式化 (commit `fd6a4d0`)](scaling_limits.md) 在 *档位首次显式化* 这件事上同型，但 *档位维度不同* — scaling 是 *派生覆盖完整域形态学*，formal 是 *type 适用域 1/0/0 起算锚*，两档梯度 *正交* 不可线性合并 (前者按派生 vs 原生分档, 后者按 type 适用域计数分档)。

- **(δ-ii) (α')(ii) 第八种几何加密方式 *同 paper × 不同 target* 与 type II 子类定义 *向后兼容* 关系的优先级排序**。上文 (α') 段已论证 *第八种几何加密方式 = type II 的 anchor-级表征, type II = 第八种几何加密方式的 paper-级抽象*，二者层级不同所指同一现象。2026-06-10 冻结协议到期后, 任何对 type II 子类定义的修订须 *严格递减* 优先级实施三件操作: (1) 先在 [`../survey/taxonomy.md` 升降级历史](../survey/taxonomy.md) 元-方法论行作 *anchor-级 vs paper-级* 二层命名分离登记 (优先级最高); (2) 再在本节 (α') 段把 KFW 四 target 的 effective 弦数 ≤ 3 折算系数明文化 (中等优先级); (3) 最后在 [`../survey/ntp_survey.md` §10](../survey/ntp_survey.md) evidence 列脚注按 type II 折算系数反向限定 C-FORM-4 / C-WM-5 / C-SCALE-1 三 candidate 的 evidence-strength 排序 (优先级最低)。这一严格递减优先级与 [`reasoning.md` (δ-ii) M1 扩张 → 五档再聚合 → 第四类几何命名 (commit `4a2f966`)](reasoning.md) / [`scaling_limits.md` (δ-ii) C-SCALE-D-DISCLOSE → m_10 → 披露-方法学双维 (commit `fd6a4d0`)](scaling_limits.md) 两段同型，是 *严格递减优先级* 协议在 formal 域的第三处兑现 (前两处 reasoning / scaling)，共构 *三-topic 优先级递减闭环*。

- **(δ-iii) M1 八 m_k 集合与 type II archetype 在 formal 域的 *零交集* 对偶**。M1 八 m_k {m_1..m_8} 在 formal 域 *0/5 OP 适用 + 1/3 sub-candidate 适用 (C-FORM-8 一例)* (见上文 (b) 段 \"OP 层 0/5 适用，sub-candidate 层 1/3 适用\" 修订)，type II archetype 在 formal 域 *1/1 适用 (KFW)*。两者 *零交集* 但 *方向相反*: M1 是 *frontier 披露选择函数缺位* 的元命题, 在 formal 域 *主体不适用*; type II 是 *theory-side 学界自产几何*, 在 formal 域 *主体适用*。这把 (γ-ii) *M1 弱成立域 + type II 强成立域 在 formal 域方向相反* 的观察 *从单 topic 内部* 升到 *跨 candidate-class 互补几何* — formal 域是 *frontier-disclosure-缺位 M1* 与 *学界-自产-几何 type II* 两类 candidate 元命题在 *同一 topic 域* 的 *正交投影* 唯一例。这一正交性是 [`embodiment.md` §OP-EMBOD-1..5 半年核查 *M1 最弱成立域 = formal (0/5)*](embodiment.md) 与 [本节 (α')(β')(γ') *type II 强成立域 = formal (1/1)*] 的对偶在 *元-元 reviewer 层* 的具体化, 与 [`scaling_limits.md` §链终点收束反向锁定 (δ-iii) M1 m_3 ∩ m_5 双投影四端闭环](scaling_limits.md) *四端独立测量同步触发* 协议在 *维度* 上不同 (后者是 *同 candidate-class 内部 m_k 共投影*, 本段是 *跨 candidate-class M1 与 type II 零交集*)，两段方法学纪律 *互补* 不可同型合并。

- **(δ-iv) 三处单向互引** (12 天冻结协议白名单 互引补充类, 与上文 (γ) 段 *不要求其他 topic 反向回贴* 同型纪律): (i) 反向触达 [`world_model.md` §2026-05-31 reviewer 加注 (δ) triple coverage 完整域 (commit `f261f3e`)](world_model.md) — 本节 (δ-i) 两端几何对称性与 world_model (δ) 段 *几何 ≠ 证据 / 负落 ≠ 扩散 / 覆盖完整 ≠ 命题升级* 三处声明 *配对*: world_model 端立证 *triple 全正不升级*, formal 端立证 *type II 1/1 + M1 0/5 零交集不互证*，两端共构 *type-geometry 链终点 ↔ 起算锚* 的 *双侧防虚高* 闭环。(ii) 反向触达 [`causality.md` §链终点收束反向锁定 (δ-i)-(δ-iv) (commit `57112b1`)](causality.md) *type I 6:2 负空间端起算锚* — causality 是 *type I 负空间起算锚*, formal 是 *type II 正落点起算锚*, 两端在 *起算锚* 这件事上同型但 *type 维度* 不同, 共构 *type I 负 + type II 正* 双起算锚, 与 world_model triple 完整端形成 *三角* (causality / formal 两起算锚 + world_model 链终点)。(iii) 反向触达 [`../survey/taxonomy.md` 升降级历史 2026-05-31 行 (commit `6832876`)](../survey/taxonomy.md) sample 侧五档形态梯度 — sample 侧五档 (N1 framework / N7 派生 triple / N5 6:2 / N2 1:2 / N4 跨 type 多投影) 与 topic 侧五档 (formal 1+0+0 / causality 0+0+1 负 / reasoning 双-type 同域 / world_model triple 全正 / scaling 全派生) 是 *sample × topic 两侧五档* 第一次 *档位维度独立但分档数相同*, 这一巧合 *不构成* 跨侧线性合并依据 (sample 档按 type 投影叙事强度分, topic 档按 type 适用域计数分), 仅登记不实施合并。

**reviewer 元判断 (2026-05-31)**: 本加注是 formal_limits 域第五次 *只整理不新增* reviewer pass (前四次: 2026-05-29T14:01Z C-FORM-7/8/8b M1 归属 / 2026-05-30T~05Z OP-FORM 二分 / 2026-05-30T12:00Z (α)(β)(γ) 三段 / 2026-05-30T18:30Z (α')(β')(γ') 重读), 也是 type-geometry 同步链 *反向锁定第五处兑现* (前四处 causality 15:00 / reasoning 15:30 / scaling 16:00 / world_model 04:05), 链 *正向 7 落点 + 反向 5 落点* 至此形成 *双向 12 接触点* 的闭环, 链 *外侧* 仅余 grounding / online_learning / embodiment 三 topic 未实施反向锁定 — 按 timeline 29107ec 边界条款第 4 条 *不主动追加新同步链*, 这三 topic 反向锁定是否实施由 2026-06-10 冻结到期后 reviewer 裁决, 当前判定 *不需要* (三 topic 均非 type-archetype 主场, 反向锁定边际价值 < 重复成本)。三件加注全部落 12 天冻结协议白名单, **不**改 (α)(β)(γ) / (α')(β')(γ') 六段判断方向, **不**动 C-FORM-1..8b 任一评估 ordinal, **不**动 OP-FORM-1..5 归因分布, **不**动 M1 8 m_k, **不**新增 arxiv ID。最大方法学风险: *双起算锚 (formal type II + causality type I)* 被误读为 *type-geometry 链有两条独立链而非单一双向链* — 规避声明: 双起算锚 + 单链终点 (world_model triple) 三点共构 *单一链的三个特异端点* 而非 *三条链*, 任一未来 D tick 涉及链拓扑讨论时须显式标注 *单链三端点*。11 commit hash (`f261f3e` / `6832876` / `29107ec` / `4707f45` / `3cf73cd` / `57112b1` / `4a2f966` / `fd6a4d0` / `1c22b75` / `c79ab79` / `764ecb8`) 经 git log 链可验证, 1 arxiv ID (2605.27734 KFW) 经 paper_notes 交叉验证, 无 ID/数字/作者/日期 fabricated。Rotation guard: formal_limits.md 上次 edit 2026-05-30T18:30Z (commit `1c22b75`) ~22.5h 前, 最近 7 ticks (scaling 16:00 / reasoning 15:30 / causality 15:00 / N6 14:30 / N9 14:00 / timeline 13:30 / taxonomy 13:00) 无 formal_limits.md 重叠, 不连续同文件规则字面满足; task type 最近 7 ticks D/D/D/D/D/C/C 本 tick 仍 D 但落 *反向锁定第五处* 子带, 与前三 tick (causality/reasoning/scaling 同 δ-i..δ-iv 反向锁定子带) 形成 *四-topic 反向锁定四连闭环* (causality → reasoning → scaling → formal), 与 world_model (δ) 链终点端共构 *双向 12 接触点闭环*。净增 ≈ +850 字 (目标区间内)。

**Reviewer 元判断 (2026-05-30 12:00 UTC)**：本节是 formal_limits 域第三次 *只整理不新增* reviewer pass (前两次：2026-05-29T14:01Z C-FORM-7/8/8b M1 归属 / 2026-05-30T~05Z OP-FORM-1..5 半年核查归因二分)；落 12 天冻结协议白名单 *互引补充 + 强度限定* 两类各 ≥3 处。本 tick *不* 开新 sub-candidate / corollary / route-elim，*不* 动 C-FORM-1..8b 任一评估，*不* 动 OP-FORM-1..5 任一归因，*不* 动 §10 M1 集合 $\{m_1..m_8\}$，*不* 申请扩入 $m_9$；唯一沉淀是 (α) 同 paper 多 anchor 独立性纪律首次在 formal 域具体化 + (β) daily harvest 五子弹 formal-域可达性零核查作 OP-FORM \"0/5 披露类\" 元判断的二次实证 + (γ) 三处单向互引 (world_model / embodiment / timeline)。下一阶段 M1 reviewer-tier 闭环 8/8 完成后的 *跨 topic 元-审计* (上一段 2026-05-29 reviewer pass 末挂账) 仍未由任一 D tick 触碰，按 12 天冻结协议保留到 2026-06-10 期满后审。Rotation guard: 上 tick D grounding.md (11:30Z, commit `639dafb`)，本 tick D formal_limits.md (距上次 edit 2026-05-30T08:00Z `4c7a554` ~4h)，不同文件，rotation guard 字面满足；task type 最近 5 ticks D/D/C/D/D 仍 D 但落到 reviewer-pass *只整理不新增* 子带，与最近 *新增* 类 D tick 形态不同。净增 ≈ +800 字 (目标区间内)。
