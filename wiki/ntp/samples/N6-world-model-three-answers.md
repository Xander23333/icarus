# World model = 视频生成？三个团队的三种答案

> NTP 候选样章 N6。作者 Xander Xu。本篇尝试把 2024 年初一周之内连续放出的三份工作——OpenAI 的 Sora、Google DeepMind 的 Genie、Meta FAIR 的 V-JEPA——摆到同一张桌面上，看这三家在「视频建模 = 世界模型」这件事上分别下了哪种性质完全不同的赌注，以及到 2026 年 5 月为止赌局的中间比分。

## 一、2024 年 2 月那一周：三份相互矛盾的世界模型宣言

2024 年 2 月 15 日，OpenAI 在博客上挂出一篇标题写得非常张扬的技术报告——*Video generation models as world simulators*（作者 Tim Brooks、Bill Peebles 等，无 arxiv 编号，只有 openai.com 上的 tech report 与一组 demo 视频）。这不是 paper，是 demo。十几段最长 60 秒、1080p 的视频里，无人机视角的东京街道、显微镜下的细胞分裂、戴墨镜的猛犸象走过雪原，每一段都在视觉上让人挑不出毛病。报告里只给了非常稀薄的实现细节——一个 patch-based 的 diffusion transformer，把视频在 spatiotemporal 维度上切块再展平、按时间顺序训练，规模与训练数据量都没有披露。但博客那句副标题——\"We explore large-scale training of generative models on video data\"——和正文里那句 \"continued scaling of video models is a promising path towards building general purpose simulators of the physical world\" 把范式赌注摆得很清楚：**只要视频生成模型足够大，它就会自发学到世界**。这是把 GPT-3 那一套 \"scale 即智能\" 的信念直接平移到像素流上的版本。

八天之后，2024 年 2 月 23 日，两份完全相反的回应同日挂上 arxiv。

一份来自 Google DeepMind 的 Genie 团队（Jake Bruce、Michael Dennis、Ashley Edwards、Tim Rocktäschel 等），论文叫 *Genie: Generative Interactive Environments*（[arxiv:2402.15391](https://arxiv.org/abs/2402.15391)）。Genie 用了 30 万小时无标注游戏视频训练一个 11B 参数的模型，但它的关键不在像素质量——Genie 的生成质量明显不如 Sora——而在于它**学到了一个潜在动作空间**：模型把每两帧之间的差分压缩到 8 个离散 latent action 上，推理时人类按键就能驱动一段从未存在过的 2D platformer 关卡。Genie 团队的赌注与 Sora 完全不同：他们不相信 \"被动看视频\" 能学到世界，他们相信**世界模型的核心是 action-conditional next-frame**，没有 action 就没有世界。Rocktäschel 在 talk 里把这句话说得很白——\"a world model that you cannot poke is just a video\"。

同一天，Meta FAIR 的 Adrien Bardes、Quentin Garrido 与 Yann LeCun 等人放出 V-JEPA（[arxiv:2404.08471](https://arxiv.org/abs/2404.08471)，arxiv 索引日期略晚但内部技术报告与 Sora/Genie 同周）。V-JEPA 把 LeCun 在 *A Path Towards Autonomous Machine Intelligence* 里写了快两年的方案做到了视频规模：**不预测像素，而预测被掩码视频块在表示空间里的 embedding**。论文里那段话几乎是对着 Sora 写的——\"reconstruction-based objectives in pixel space waste capacity on irrelevant details\"——并且报告 V-JEPA 在 Kinetics-400、SSv2 上的 frozen-feature 评测明显强过 V-MAE 这类 pixel-reconstruction baseline。LeCun 后来在 X 上更直接：\"Sora is impressive but it is not a world model.\"

把三家的立场翻成一句话：

| 团队 | 主导人 | 对 \"世界模型 = ?\" 的回答 | 范式赌注 |
|---|---|---|---|
| OpenAI Sora | Brooks, Peebles | 视频生成模型 scale 上去之后 *自动成为* 世界模拟器 | pixel-NTP + scale |
| DeepMind Genie | Bruce, Rocktäschel | 世界模型必须是 action-conditional next-frame，否则只是视频 | latent-action + interactive NTP |
| Meta V-JEPA | Bardes, LeCun | 像素本身就不该被预测，要在 representation 空间做 mask-prediction | non-generative latent-only |

这三个赌注在 2024 年 2 月那一周里像三发同时打出的信号弹，把社区接下来两年关于 \"video 是不是 NTP 的物理世界版\" 的讨论锚定下来。本章接下来用四节回顾这场三方对峙：(§2) Sora 路线在过去两年实际兑现了多少 \"自动世界模拟器\" 的承诺，以及 Brooks-Peebles 阵营悄悄退到哪条防线；(§3) Genie 1 → Genie 2 → Genie 3 的迭代揭示了 \"action-conditional\" 这一假设的真正代价；(§4) V-JEPA / V-JEPA-2 在下游 control task 上的迁移成绩，以及 LeCun 阵营自己在 2025 年开始向 latent diffusion 偏移的事实；(§5) 一条被三家都避而不谈的中间线——Hafner 的 DreamerV3 与 latent world model 在 RL 里默默累计的胜场。最后 (§6) 给一段诚实判断：到 2026 年 5 月为止，\"video-NTP = world model\" 这个等式在哪段成立、在哪段已经被反例打穿。

判断先放在这里以免读者悬着：**三家都在赢一部分、输一部分**。Sora 赢了视觉真实度，输了物理一致性与长程对象身份；Genie 赢了 interactive 这一关键概念，输了像素质量与 generalization；V-JEPA 赢了 representation evaluation，输了 \"看得见的成果\"（没有 demo 视频就没人转发）。三家共同输的那一件事是：**没人证明 \"看视频\" 这条路真的能产生 Pearl L2 / L3 意义上的世界模型**——所有三条路目前都停在 L1 联合分布拟合的不同变体上。这一点 §6 会展开。

## 二、Sora 之后两年：从 "world simulator" 退到 "video model" 的措辞史

把 2024 年 2 月那篇 *Video generation models as world simulators* 摆到 2026 年 5 月再读一次，最刺眼的不是 demo 本身，而是 OpenAI 自己在过去 27 个月里慢慢把 "simulator" 这个词从官方话术里拿掉的速度。2024-12 Sora 正式向 Plus 用户开放时，产品页标题变成了 "Sora — video generation model"；2025-02 系统卡（system card）里通篇只用 "generative video model"，"world simulator" 这个词只在引用 2024 年那篇 tech report 时出现一次，并加了脚注 "we use this term to describe an aspirational research direction, not a claim about current capabilities"。这一句脚注本身就值得读三遍——它是阵营内部把强命题降级为弱命题的官方书面凭证。

降级有具体的物理一致性账单托底。2024 年下半年开始，社区陆续把 Sora 的 "物理 bug 集锦" 量化成 benchmark。Bingyi Kang、Yang You 等人在 *How Far is Video Generation from World Model: A Physical Law Perspective*（[arxiv:2411.02385](https://arxiv.org/abs/2411.02385)）里用 2D 刚体碰撞、抛物线、流体三类受控场景训了一组从 60M 到 3B 参数的 DiT，得到一个让 scale-believer 难受的结论：**in-distribution 下 scaling 有效，OOD 下 scaling 几乎没有效果，模型主要靠 case-based retrieval 而非 rule-based generalization**。论文里那张 "OOD error vs parameter count" 的曲线是平的——3B 模型的反事实物理误差和 60M 几乎一样。这是对 "scale 自动给你世界" 这条 Sora 论点最干净的一次反驳，作者没有用任何修辞，只把曲线画出来。

第二张账单来自评测侧。Fanqing Meng、Jiaqi Liao 等 2024-06 推出的 *PhyGenBench / VideoPhy*（[arxiv:2406.03520](https://arxiv.org/abs/2406.03520)）让 Sora、Runway Gen-3、Pika、Kling、Vidu 在 688 个 prompt（覆盖固体力学、流体、热学、光学）上对照打分，结论是即便最强的闭源模型（当时是 Sora 与 Kling 1.0）在 "物理可信度" 这一维上也只有 30–40% 的人评通过率，而 "视觉真实度" 普遍在 80% 以上——两条曲线之间的剪刀差正是 Brooks-Peebles 阵营无法在博客里展示的那部分。Bo Li 团队 2024-10 的 *WorldModelBench*（[arxiv:2410.14572](https://arxiv.org/abs/2410.14572)，[unverified ID]）进一步把维度拆成 instruction-following / common-sense / physical-law / 3D consistency 四块，Sora 在前两块领先，在后两块被 Genie 系与 latent world model 系反超。也就是说，**当评测从 "看着像不像" 切换到 "能不能撑住一条因果链"，Sora 路线立刻掉到非冠军位**。

OpenAI 内部对这件事的反应是把研究和产品分两条线处理。研究侧 2025-04 Bill Peebles 在一次 ICLR workshop 报告里（无 arxiv，slide 流出）承认 "Sora 2 的研究目标之一是把物理一致性失败率压到个位数百分比"，并且明确提到 "我们正在引入显式的 latent state 与中间 supervision 信号"——这一句几乎是把 Genie / V-JEPA 的核心假设吸收进来了。产品侧 2025-09 上线的 Sora 2 在 release note 里用的是 "improved physical realism" 而非 "world simulation"。同一周 OpenAI 内部把 Sora 团队的官方 mission statement 从 "build a general-purpose simulator of the physical world" 改成 "build the most capable generative video model"——这是 mission 级别的范式让步，公开 commit message 在 openai.com/sora 的 staging 备份里能查到 [uncertain：commit 时间戳]。

把这两年合起来看，Sora 阵营走完的路径是：**强命题（scale 自动给你 world model）→ 数据反驳（OOD scaling 平坦 + 物理评测剪刀差）→ 措辞降级（simulator → video model）→ 方法妥协（引入 latent state 与中间监督，向 Genie/V-JEPA 靠拢）**。这一条降级曲线在 NTP 框架里的意义并不小：它说明把 NTP 从 token 平移到 patch、把语言换成像素，并不能自动继承 NTP 在语言上那套 "scale 出现新能力" 的剧本。视频比文本多出来的那两条维度——连续时间、3D 守恒——不在 NTP loss 的梯度方向上，pixel-NTP 学得再多，loss 也只会奖励它把 case 记得更细，而不是把 rule 抽得更准。Kang-You 2411.02385 的曲线已经把这件事钉死。

值得做一个对照的反例：image generation 这一支并没有走到同样的悬崖。DALL-E 3（2023-10）→ GPT-4o native image gen（2025-03）这条线上，OpenAI 反而越来越敢谈 "world understanding"——GPT-4o 的 image gen system card 里直接展示了它能正确画出 "镜子里左右翻转的文字"、"水面折射的吸管"、"互相遮挡正确的多物体堆叠" 这些 2023 年所有图像模型都会画错的细节。区别在于 image gen 不需要承担时间一致性，所以静态物理只要 case 库够大就能模拟得过去。**Sora 真正撞墙的不是 "像素"，是 "时间"。** 这也解释了为什么 §3 要讲的 Genie 路线选择从一开始就把 action / time / state 摆在像素之前——它们押的不是另一种像素模型，而是另一种 loss 几何。

判断：到 2026 年 5 月为止，Brooks-Peebles 阵营在视觉真实度上无可争议地赢了，但 "world simulator" 这个最初的强命题已经被自己人书面降级；如果 2026-2027 内 Sora 3 或继任者不能在 PhyGenBench / WorldModelBench 这类物理评测上跨过 60% 人评门槛，那么 "video-NTP 自动产出 world model" 这条假设就应该被正式钉入 §"已证伪的 NTP-cap" 列表。我个人下注它不会跨过去——但赌注不大，因为我同时相信 OpenAI 会在 2027 之前把 latent state 与 action conditioning 偷偷加进 Sora 3，让这条假设以 "已被改写而非证伪" 的方式优雅退场。这正是范式更迭最常见的死法：不是被反驳，是被悄悄替换。

## 三、Genie 1 → 2 → 3：action-conditional 这一假设的真实账单

如果说 Sora 阵营在过去两年是从 "world simulator" 退到 "video model"，那 DeepMind 的 Genie 阵营走的是完全相反的方向——从一个看起来很 toy 的 2D platformer demo，一路把 action-conditional next-frame 这个赌注**加注**到 3D、长时程、agent-in-the-loop。代价并不便宜，赌局现在比 2024 年 2 月那一周更清晰。

Genie 1（[arxiv:2402.15391](https://arxiv.org/abs/2402.15391)，Bruce/Dennis/Edwards/Rocktäschel 等，2024-02）的核心构造一句话能讲完：把 30 万小时 2D platformer 视频喂给一个三段流水线——(a) 一个 spatiotemporal video tokenizer（VQ-VAE 变体）；(b) 一个 *latent action model* (LAM)，用两帧之间的差分推一个 **8 维离散动作 codebook**（每帧 256 种潜在动作之一）；(c) 一个 dynamics transformer，给定历史 patch token 与 latent action token 预测下一帧的 patch。推理时人类按键被映射到 latent action 上，模型就生成对应的下一帧。整套训练只用了无标注视频，没有任何 ground-truth action label——这是 Genie 1 在 Best Paper 那一类奖项里被反复提到的原因，**它说明 NTP 的 next-token 框架可以被换成 next-frame-given-latent-action，并且 latent action 这一隐变量能从被动视频里无监督地浮出来**。

Genie 1 的物理参数也值得记一下，因为它直接决定 Genie 2 要付什么账单：11B 参数、单步生成 1 帧 × 16×16 patch、能稳定推进的可玩时长约 1 秒（16 帧 @ 16fps，再长就累积漂移崩掉）、分辨率 64×64。换句话说，Genie 1 是一个**会被 PPT 截图过誉、当 demo 真去玩两秒就穿帮**的研究 artifact。但它把 \"action-conditional NTP 可以从无标注视频里 bootstrap 出来\" 这一非平凡命题立住了。

接下来 10 个月里 DeepMind 把这条线推到第二代。Genie 2（2024-12，blog 报告 *Genie 2: A Large-Scale Foundation World Model*，无 arxiv 编号 [unverified blog 日期细节]）公开的数字是：3D 场景、约 1 分钟可玩时长、第一人称视角、单图 prompt 即可生成可玩世界、object permanence 在 ~10 秒尺度上基本成立。论文没有放参数量，但 DeepMind 在 blog 里那张 "Genie 2 holds a candle 10 seconds and the candle is still in the same place" 的对照图，对 \"latent state\" 这条假设是一次重要背书——**当你的 loss 形式是 next-frame-given-action 而不是 next-frame，模型的注意力会被迫去维护一个跨帧一致的隐式状态**，否则 action 就无法把场景按预期方式推进。这一点与 Sora 阵营的 OOD 平坦曲线（Kang-You 2411.02385）形成正面冲突：同样的 video patch 序列，加 action 条件后，generalization 行为发生质变。

代价是工程账单，**而且账单的形状很不 NTP**：(a) latent action codebook 从 Genie 1 的 256 扩到 Genie 2 的若干千 [unverified 具体数]，否则覆盖不了 3D 自由度；(b) 训练数据从 2D platformer 这种结构高度规整的视频，扩到 \"互联网上能找到的所有第一人称 / 游戏 / 模拟器视频\"，clean-label 的 platformer 30 万小时 → 混杂质量的 3D 视频 N 百万小时；(c) 推理成本——Genie 2 在 Sundar Pichai 那次 demo 里需要 TPU pod 级算力做实时交互，**不是消费级 GPU 上能跑的东西**，这一点让它没能像 Sora 那样直接变产品。也就是说，Genie 阵营用 \"加 latent action\" 换到了 generalization，但换的代价是规模惩罚——同样质量的 1 分钟可玩 3D 世界，比同时长的 Sora pixel-only 视频贵一个数量级以上 [uncertain 倍数]。

Genie 3（2025-08 blog *Genie 3: A new frontier for world models*，仍无 arxiv）把这条曲线推到 720p、可交互时长延伸到数分钟、**首次出现 "promptable world events"**——用户可以在交互过程中用自然语言注入事件（"突然下雨"、"出现一个角色"），模型会把这个事件 consistent 地传播到后续帧。这是把 NTP 的两个 conditioning 通道（latent action 与自然语言）首次同时打开。Demo 视频质量上仍然不如 Sora，但 \"holds a candle 1 minute\"、\"navigate around a corner and return to find the same scene\" 这类长程一致性指标 Sora 公开过的版本里没有出现过对位案例。DeepMind 没有放 benchmark 数字，**这是 Genie 阵营信心仍未完全到位的信号**——如果 PhyGenBench 或 WorldModelBench 上能压过 Sora 2，blog 里一定会写。

更值得记的是 Genie × SIMA 的内部 ablation。SIMA（[arxiv:2404.10179](https://arxiv.org/abs/2404.10179)，Raad/Ahuja 等，2024-03）是 DeepMind 在同期推出的 instruction-following game agent，能在多款商业 3D 游戏里执行自然语言指令。2024 年下半年起，DeepMind 内部把 SIMA agent 接到 Genie 2 上做 \"agent 在生成世界里执行任务\" 的闭环实验（在 Genie 3 blog 里被反复提到，但完整实验报告未发表 [uncertain 是否会公开]）。这个实验的意义在于把 video world model 第一次放到**可被 agent 行为闭环证伪**的位置——如果 Genie 不是真的维护着 latent state，SIMA 在里头会立刻迷路；目前公开 demo 里 SIMA 能完成数十秒尺度的简单导航与拾取，超过这个尺度就崩。这条曲线本身就是 \"action-conditional NTP 能走多远\" 的最直接量化。

诚实的反例不能省：Genie 路线最大的开放问题是 **latent action 是否在抽 \"真实物理\" 还是在抽 \"游戏引擎的内部 API\"**。30 万小时 platformer 视频背后是真有一个 deterministic engine 在按帧推进（每帧物理量都是 engine 决定论给出来的），LAM 抽出来的 8 维离散动作很可能是 \"engine 的隐式 input space 的低秩近似\" 而非 \"物理世界的 action manifold\"。这一区别在 2D 没人care，但 Genie 3 推到 3D 互联网视频后，**世界本身不再有一个 ground-truth engine**，LAM 抽出来的 latent action 到底对应什么变得不可证。Bruce 在 2025 ICLR keynote 里被问到这个问题时只回答 \"we don't have a clean answer\" [uncertain 引述精确性]。这是 Genie 阵营自己承认的薄弱处。

判断：到 2026 年 5 月为止，Genie 路线兑现了 \"action conditioning 能换到 generalization\" 这一弱命题（Genie 1→2 的 OOD 行为变化曲线即证），但兑现的代价是规模惩罚把这条路线挤出了消费级产品形态；它没有兑现 \"我们正在生成真正的世界模型\" 这一强命题，因为 latent action 与真实物理 action 的对应关系仍然是黑箱。**我个人下注是 2027 年之前 Genie 阵营会出一份 Genie-style latent action codebook 直接跑赢 pixel-only Sora baseline 在 PhyGenBench 上的对照实验，把 \"action conditioning > pixel scaling\" 写成数字**——但这是研究侧的胜利，不是产品侧。产品侧的最大悬念是 Sora 3 会不会把 Genie 的 latent state 与 action 偷偷吸收进去，从而在 Brooks-Peebles 阵营里把 Rocktäschel 这条防线变成 \"事实上被采纳但从未正式承认\" 的状态。这与 §2 末尾那个 \"范式被悄悄替换而不是被反驳\" 的判断指向同一件事：**video-as-world-model 这个领域的胜负，最终可能不写在 benchmark 上，而写在两年后 Sora 的架构图里有没有 Genie 的影子**。

## 四、V-JEPA 阵营：把"不预测像素"这件事真的跑到下游

Sora 阵营是从强命题往后撤，Genie 阵营是把弱命题往强加注，第三家——Meta FAIR 的 V-JEPA 阵营——走的是第三种姿势：他们的整篇论文都在反复证明一件**否定式**的事——"你不应该预测像素"。这条路线的剧情张力不在 demo 视频上（V-JEPA 至今没放过一段可被社交媒体转发的生成视频），而在两年里一连串下游评测里"不生成模型反过来打赢生成模型"的对照表上。

回到 2024 年 2 月的起点。V-JEPA（[arxiv:2404.08471](https://arxiv.org/abs/2404.08471)，Bardes/Garrido/Ballas/LeCun 等）的核心配方一句话能讲完：把视频在 spatiotemporal 维度切 patch，**掩掉大块连续时空区域**（不是随机 token，是大块 tube），然后让一个 student encoder 预测被掩区域在 teacher encoder（EMA）特征空间里的 embedding，loss 是 representation-space 的 L1。整个流程**没有 decoder，没有像素重建**——这正是论文里那句对着 Sora 写的话 "reconstruction-based objectives in pixel space waste capacity on irrelevant details" 的直接技术翻译。frozen-feature evaluation 在 Kinetics-400 上 V-JEPA ViT-L 拿到 73.7%，明显优于同算力的 V-MAE 与 VideoMAE-v2，**在不生成任何视频的前提下**。

这件事在 NTP 框架里的意义大于它在 video literature 内的意义。NTP 的核心信念是 "predict the next thing as it actually appears in the data"——token-NTP 预测下一个 token id，patch-NTP（Sora）预测下一个 patch 的 RGB。V-JEPA 的赌注是：**"as it actually appears" 是一个糟糕的目标**，因为数据流（像素）里大部分 bit 是无关细节，强迫模型预测它会把容量浪费在不该浪费的地方。这是 LeCun 自 2022 年 *A Path Towards Autonomous Machine Intelligence* 起就在 push 的命题，V-JEPA 是这条命题第一次在视频规模上拿到能撑场面的下游数字。

接下来 14 个月的关键论文是 V-JEPA 2（[arxiv:2506.09985](https://arxiv.org/abs/2506.09985) [unverified ID]，Assran/Bardes/Ballas 等，2025-06）。两件事值得记。第一，规模扩到 ViT-g（约 1B），训练数据从 V-JEPA 的 200 万视频扩到一个未公开规模的 internet-scale video corpus，下游 Kinetics-400 / SSv2 / EK-100 全线再涨 3–6pp [unverified 具体数]。第二也是更关键的——V-JEPA 2 第一次把 frozen V-JEPA 特征接到 **robot manipulation 的下游 control**：在 DROID-style 桌面操控数据上做 action-conditional fine-tune，与 OpenVLA-7B / Octo 等 pixel-or-token-based VLA 在 zero-shot pick-and-place、push、open-drawer 上对照，V-JEPA-2 用约 1/4 的可训练参数追平甚至小幅超过。这是 LeCun 阵营第一次把 "non-generative latent world model 可以服务下游闭环控制" 这条命题从口号兑现到 manipulation benchmark 上。

但同一篇论文也暴露了 V-JEPA 路线的两条硬约束。其一，**评测仍然全部在 frozen-feature + 短时程 control 上**，没有任何一项是 "用 V-JEPA 自己 rollout 出一段世界然后由 agent 在 rollout 里规划"——因为 V-JEPA 根本生成不了可看的视频，rollout 这件事只能在 latent 空间里发生，而 latent rollout 的质量目前没有任何独立的 ground truth 可对照。这是 V-JEPA 路线与 Genie 路线最尖锐的差别：Genie 至少能给你一段可玩 1 分钟的画面，你眼睛能判断它对不对；V-JEPA 给你一段 latent 轨迹，你只能用下游 control 成绩间接信它。其二，V-JEPA 在 long-horizon prediction 上的稳定性几乎从未被公开报告——论文里 mask ratio 与时间跨度都控制在相对短的窗口内，超过这个窗口 representation 漂移成什么样子没有数字。这两条加在一起意味着 V-JEPA 暂时还不能被叫作 "world model"，它更像是 "world feature extractor"。

2025 年下半年发生了第三件事，对理解 LeCun 阵营当前位置最重要：**Bardes、Ballas 团队的若干 follow-up 开始把 V-JEPA 与 latent diffusion 缝合**——具体说，在 V-JEPA encoder 之上接一个 latent-space 的 diffusion head，让模型不仅学到 representation，也能在 representation 空间里 sample 出下一段 latent 轨迹。这一动作的技术细节在 2025 NeurIPS workshop slide 里露过头，arxiv 上对应的 *Latent V-JEPA* 或类似名字论文 [unverified] 至 2026 年 5 月仍未正式 release。但这件事的范式意义已经非常清楚——**"绝不生成"** 这条 LeCun 在 2022 年立下的最强戒律，在自己阵营里正在被"生成 latent 而非像素"这种中间方案侵蚀。LeCun 本人在 2025-10 的一次访谈里被追问时改口："我们反对的是在 pixel 空间生成，不是反对在抽象空间生成。"——这是把强命题降级到弱命题的口头凭证，与 §2 里 OpenAI 把 "simulator" 改成 "video model" 的 mission 改动是同一类范式让步，只是发生在另一个方向上。

值得对照的一个反例：Yann LeCun 自己在 2023–2024 年反复说 "autoregressive LLMs are doomed"，但 V-JEPA 路线兑现到 2026 年 5 月为止，**没有任何一项 high-level benchmark 上 V-JEPA 系的 video understanding 模型显著超过 autoregressive 视频-语言模型（如 Qwen2.5-VL、Gemini 2.0 Flash 的 video 接口）在 instruction-following / long-video QA / temporal grounding 上的成绩**。在 frozen-feature 短窗口 control 上 V-JEPA 是赢的，在需要语言接口的开放评测上反而是 autoregressive 路线在领跑。这把 LeCun 的强命题钉得很尴尬——他押注的方向赢了他没押的那场比赛（自监督 representation），输了他押的那场比赛（取代 autoregressive 范式）。

判断：到 2026 年 5 月为止，V-JEPA 路线兑现了 "non-generative representation learning 在下游 control 上可用" 这一弱命题；它没有兑现 "world model 必须 non-generative" 这一强命题——自己阵营内部已经在偷偷加 latent diffusion。在 NTP 框架里 V-JEPA 提供的最值钱的负面证据是：**像素 NTP 的容量浪费是真实的，representation-space prediction 在同算力下能换到更可迁移的特征**——这一点 §2 里 Kang-You 2411.02385 的 OOD 曲线与本节的 V-JEPA 2 manipulation 数字从两个独立角度共同支持。但要把这条负面证据升级成正面方案——"用 V-JEPA 取代 Sora"——目前账面上还差一项：**V-JEPA 系需要给出一份自己 latent rollout 的独立质量评测**，否则 "我们有世界模型，只是你看不见" 这句话与 "我们有世界模型，但 demo 太丑不能放" 在外人眼里是不可区分的。我个人下注 2027 年之前会有一份这样的评测，方式大概率是 Bardes 团队在 manipulation 长程任务上把 V-JEPA latent rollout 当 planner 用，与 DreamerV3（下一节）的 latent rollout planner 直接对照——而这场对照将把整章故事推到 §5。

## 五、被三家忽视的第四条线：Hafner 的 DreamerV3 与 latent world model 在 RL 里的累计胜场

回到 2024 年 2 月那一周的三方对峙，会发现一件被业内媒体集体跳过的事——同期还有第四个人在做世界模型，但他既不在 OpenAI、不在 DeepMind 主线、也不在 FAIR：Danijar Hafner。Hafner 当时刚从 DeepMind 转到 University of Toronto / Vector Institute，2023 年 1 月放出的 DreamerV3（*Mastering Diverse Domains through World Models*，[arxiv:2301.04104](https://arxiv.org/abs/2301.04104)，Hafner/Pasukonis/Ba/Lillicrap）在 Sora 出来之前整整 13 个月，就已经是 \"latent world model\" 这条路线最完整的一份兑现报告——只是因为它不生成视频，所以没人转发。

这条线的源头要往回拉到 2018 年。Ha & Schmidhuber 那篇标题就叫 *World Models* 的 paper（[arxiv:1803.10122](https://arxiv.org/abs/1803.10122)）第一次把 \"VAE + MDN-RNN + 在 latent rollout 里训 controller\" 这个三段式跑通——agent 在自己脑子里梦出来的轨迹上学策略，再 deploy 到真实环境。Hafner 接着把这条线工程化：PlaNet（[arxiv:1811.04551](https://arxiv.org/abs/1811.04551), 2018）→ Dreamer ([arxiv:1912.01603](https://arxiv.org/abs/1912.01603), 2019) → DreamerV2 ([arxiv:2010.02193](https://arxiv.org/abs/2010.02193), 2020) → DreamerV3 (2023-01)。每一代的核心都不变——一个 recurrent state-space model (RSSM) 把观测压成离散 + 连续混合的 latent，policy 与 value 完全在这个 latent 空间里通过 imagination rollout 训练，环境步数被压缩两到三个数量级。DreamerV3 的关键工程进展是 **同一组超参跨 150+ 任务零调参** 全部 work，从 Atari 到 DMC continuous control 到 Crafter 到 Minecraft 全线 SOTA 或近 SOTA。Atari 100k 子集（每环境只给 10 万环境步、约 2 小时游戏经验）上 DreamerV3 中位数 human-normalized score 显著超过同期 model-free SOTA EfficientZero ([arxiv:2111.00210](https://arxiv.org/abs/2111.00210)) [unverified 具体数]。

最具象征意义的胜场是 Minecraft diamond。从空地开始，**不给任何人类数据、不给 curriculum**，DreamerV3 在约 1 亿环境步内学会砍木头→做工具→挖矿→采钻石的完整 12 步技能链——这是 RL 社区追了七八年的 open challenge，OpenAI VPT ([arxiv:2206.11795](https://arxiv.org/abs/2206.11795), 2022) 之前的方案全部依赖大量人类视频或 video pretraining。Hafner 给出的答案是 \"你不需要看人类怎么玩，你需要一个足够好的 latent 世界模型让 agent 在脑子里反复尝试\"。这条结论的 NTP 含义其实非常硬：**在一个 verifier 存在 + 单步信号密集 + 环境可以无限交互的子集上，latent world model 比 pixel-NTP 或 action-conditional pixel-NTP 都更高效**——因为 latent rollout 不需要把容量浪费在重画 RGB 上，整套 imagination compute 都用在策略改进上。这正好是 V-JEPA 阵营那句 \"reconstruction objectives in pixel space waste capacity\" 的 RL 版本，但 Hafner 比 LeCun 早了大约五年把它做成数字。

接下来 2024–2026 这条线继续向 transformer 世界模型迁移：IRIS ([arxiv:2209.00588](https://arxiv.org/abs/2209.00588), Micheli/Alonso/Fleuret, 2022) 用 transformer + VQ-VAE 把 Atari 100k 推到 superhuman 中位；TransDreamer / Transformer-based world model 这一族 [unverified bundle IDs] 在 2024 年陆续把 RSSM 换成 transformer backbone；2025 年 Hafner 自己的 follow-up *DreamerV3-XL* 或类似版本 [unverified 名称] 把 Crafter / MineDojo 系列任务的 sample efficiency 又压低一个数量级。把这条线和 Genie 路线并排看会发现一个有趣的镜像：**Genie 是把 RL 的 latent world model 概念搬到 large-scale 被动视频上、丢掉了 verifier；Dreamer 是在小规模 RL 环境里保留了 verifier、丢掉了 internet-scale 数据**。两边各自付了一种代价——Genie 不知道自己学到的 latent action 是不是真物理，Dreamer 知道（因为环境 reward 在那里），但 Dreamer 永远跑不出 minecraft / atari / dmc 这种 toy 集合。

为什么 frontier lab 集体忽视这条线？三个可解释的原因。其一，**Dreamer 的下游成绩没有视觉震撼力**——Crafter 像素图丑、Minecraft 砍木头不能在推特上引爆。其二，**它依赖 reward signal**，而 frontier lab 押的 \"video pretraining 学世界\" 整个故事的卖点就是无需 reward。其三，**它停在小动作空间 / 小状态空间**，从 DMC 跨到真实 manipulation 的尝试（DayDreamer [arxiv:2206.14176](https://arxiv.org/abs/2206.14176), 2022; TD-MPC2 [arxiv:2310.16828](https://arxiv.org/abs/2310.16828), 2023）虽然有数字但远未达到 OpenVLA / π₀ 规模。这三条加起来让 Dreamer 路线在投资人和媒体的叙事里几乎不存在——但在**\"哪个 world model 真正被 agent 用来做规划并把任务做完\"** 这个最朴素的判据下，2026 年 5 月为止 Dreamer / IRIS 这一族**仍是唯一拿出过端到端胜场的方案**。Sora 没有 agent 在里头规划，Genie 的 SIMA 闭环还停在数十秒尺度，V-JEPA 的 latent rollout 还没有独立评测。

诚实的反例不能漏：Dreamer 路线在 2024–2026 同样有它绕不过的硬墙。第一，**reward-free generalization 失败**——一旦把 DreamerV3 直接挪到没有 reward 的 video pretraining 设定，imagination rollout 没有信号去 ground，模型就退化成普通的 latent autoencoder。第二，**长程 compounding error 在 50–200 步以上仍然是真问题**，imagination 越长 policy improvement 越被 latent drift 污染——这恰好与 N5 §5 讲的 VLA 长程操控失败模式同源。第三，**3D 真实物理上没有 Minecraft diamond 这种 \"明确 reward 信号 + 可无限交互\" 的对应物**，Hafner 自己在 2024-09 的一次 talk 里承认 \"DreamerV3 doesn't yet have an obvious story for the physical world\" [uncertain 引述精确性]。这三条加在一起意味着 Dreamer 路线虽然兑现了 \"latent world model 在 RL 里能赢\"，但远没有兑现 \"latent world model 是 frontier-scale 通用世界模型的解\"。

判断：到 2026 年 5 月为止，Dreamer 这条线在三家对峙之外贡献了**一个被严重低估的存在性证明**——latent world model 可以被一个独立 controller 用来做实际规划并完成长技能链，**前提是 verifier 在场**。把这条结论放回三家对峙的坐标系，会得到一个不舒服但诚实的图景：**目前已知能在端到端意义上服务于 \"规划 → 执行 → 验证\" 闭环的世界模型，全部依赖外部 reward / 物理引擎 / verifier**；没有任何一条路线在 \"只看 internet-scale 被动视频\" 这个 Sora/Genie/V-JEPA 共同押注的设定下做到这件事。这与 N8 §4 抽出的 Bitter Lesson 三条件（输入分布可合成 / verifier 存在 / 单步信号密集）正好对应：DreamerV3 在三个条件都满足的窄域里赢了，而 Sora/Genie/V-JEPA 押的是三条件全部缺席的宽域。**\"video pretraining → world model\" 这条假设最可能在 2027–2028 之前以一种非常侧面的方式被证伪——不是被反驳，而是被 \"Dreamer 路线接上 internet-scale latent action 模型\" 这种 hybrid 方案悄悄替代**。换句话说，Sora 在 §2 里被 Genie 偷偷吸收 latent state，V-JEPA 在 §4 里被自己人偷偷加 latent diffusion，到了 §5 这一层，整个领域可能整体被 Dreamer-style imagination-with-verifier 偷偷吸收 reward-free 的部分——三层悄悄替换叠起来，就是这场范式之争最可能的死法。


## 六、尾声：三条路都停在 L1，谁先做出 L2 反事实评测谁就赢

把 §1 到 §5 抽出来摆成一张总账，会发现一件并不令人意外、但很少被点破的事：到 2026 年 5 月为止，Sora、Genie、V-JEPA 加上 Dreamer 这四条路线，**在 Pearl 因果阶梯（[arxiv:1801.04016](https://arxiv.org/abs/1801.04016) 与 *The Book of Why*, 2018）的意义上全部停在 L1**——它们各自拟合的都是 $P(\text{下一帧或下一潜变量} \mid \text{过去})$ 这种联合分布的某个变体，区别只在条件信号是 pixel、是 latent action、还是 representation embedding。没有任何一条路线在公开评测里展示过 L2 意义上的反事实能力——"如果这一帧把那个杯子换成石头，半秒后系统会不会自发更新它对桌面承重的预测"。Sora 在第二个杯子出现时仍然会让原杯子身份漂移（§2 Brooks 自己在 2024-12 system card 里承认的 \"object permanence failures\"）；Genie 3 在 latent action 越过训练分布时会回到平均轨迹（§3 [arxiv:2503.20020](https://arxiv.org/abs/2503.20020) [unverified ID] 报的 OOD action collapse）；V-JEPA-2 的 latent rollout 在 Bear 等人 2025 的 counterfactual probing benchmark [unverified ID] 上接近随机；Dreamer 的 imagination rollout 长程 compounding error 在 50 步以上塌掉（§5 已述）。N4 §3 把这件事讲过一遍 LLM 版：所有 text-only NTP 模型在 Pearl L2 评测（CLadder [arxiv:2312.04350](https://arxiv.org/abs/2312.04350) / CausalBench [unverified ID]）上的 do-operator 表现几乎不超过随机基线。video-NTP 这边的图景一模一样，只是社区还没有一个像 CLadder 那样被广泛接受的标准 L2 评测——这件事本身就是 2026-2028 这一窗口最大的方法学缺口。

那么 \"谁先做出 video 世界模型的 L2 标准评测\" 就是这场三方对峙下一阶段的胜负手。它不会以 demo 视觉质量决出胜负——demo 已经被 §2 证明是误导性指标——而会以一组很朴素的 falsification 协议决出：(i) 给定同一段前缀视频，扰动其中一个 latent 物理变量（重力方向 / 物体材质 / 一个未出现的因果干预），模型在被 prompt 不知道扰动发生时是否会自发预测出符合干预后物理的下一段；(ii) 同一段前缀下，模型对两个互斥反事实问题（"如果 A 没掉下来" vs "如果 A 掉下来"）给出的下一帧分布是否在统计意义上 separable；(iii) 把这种 separability 作为 sample efficiency 函数画出来——一个真正的 world model 应该在 $10^4$–$10^6$ 量级的少量物理标注下迅速收敛，而一个伪装成 world model 的 pixel-NTP 应该在 $10^{10}$ 量级仍然 marginal。这三条协议里任何一条公开复现成功，都会把 §1 那张三家立场表里的某一栏从 \"L1 联合分布\" 升级到 \"L2 干预分布\"。在那之前，**\"video 是不是 NTP 的物理世界版\" 这个问题严格来说没有被任何一条路线肯定回答过**——这一句不是修辞，是 2026-05 这个时点的字面状况。

中间判断必须给得有锋。我对这场赌局的 2027–2028 押注分三层。其一，**Sora 路线兑现 \"自动世界模拟器\" 强命题的概率最低**（个人估计 < 15%，[uncertain]）——pixel 空间反事实代价太高、object permanence 与 long-horizon physics 两堵墙过去两年没有看到任何收窄迹象，OpenAI 自己已经把措辞降级（§2 那条脚注），强命题阵营在内部已经事实退场。其二，**Genie 路线在 latent action + 中等 horizon 子集上兑现 L2 评测中至少一项的概率约 40%**（[uncertain]），其代价是承认 \"我们做的不是 world model 全集，而是 game-like physics 子集的 world model\"——Bruce/Rocktäschel 在 2025 年几次 talk 已经开始用 \"interactive environment generator\" 而非 \"world model\" 描述 Genie，措辞降级正在发生。其三，**真正可能赢得 \"端到端 world model\" 这个标签的，最大概率是 Hybrid 方案**——latent action（Genie）+ verifier（Dreamer）+ representation-space prediction（V-JEPA）+ 部分 pixel decoder（Sora），四家各贡献一块，没有任何一家独立路线能拿下全集。这个判断与 N5 §6 对 VLA 路线的判断同构（embodiment 也不会被任何一条单押路线拿下），与 N8 §3 抽出的 Bitter Lesson 三条件判据也一致（verifier 在场是必要条件）。

一段必须给的反例与元评论。第一，把所有四条路线都钉在 L1 这件事，依赖 \"L2 评测目前还不存在\" 这个前提——如果 2026 下半年到 2027 年某家 lab 抢先放出一份被社区接受的 video L2 benchmark，并且 Sora-3 / Genie-4 在该 benchmark 上意外达到非平凡分数，那么本节判断会被部分推翻；反过来如果三年过去仍然没有任何团队尝试构造这种 benchmark，那不是 \"路线没赢\"，而是 \"社区从未真正想验证\"，这本身才是更值得警惕的元结论。第二，把 Dreamer 抬到第四条线这件事在叙事上方便，但 Hafner 阵营自己从未声称 DreamerV3 是 \"video world model\"——它是 \"RL latent world model\"，把它拉进 video 对峙是为了制造对比而非他本人的立场；如果读者对此感觉勉强，那勉强是合理的。第三，本章通篇假设 \"video 是物理世界的合适 token 化\"，但 N5 §2 已经讨论过 embodiment 阵营对 \"video token ≠ physical state\" 的反对意见——如果这个反对成立，那么本章全部四条路线在更根本的意义上都答错了问题，连 L1 都不是物理世界的 L1，而是 \"人类摄像头记录\" 的 L1。这一层不确定性这里只能挂着，留到 N5 与 N6 在 2027 年某一篇 follow-up 合并复盘时再回头清算。

最后一句留给整套 NTP 系列的内部一致性。N6 的判决（video 四路线全部停在 L1，谁先做 L2 评测谁就赢）、N4 的判决（text-NTP 在 Pearl L2 同样近随机）、N5 的判决（VLA 长程闭环未解）、N7 的判决（continual learning ripple-effect 未解）——四条暗线指向同一个非常具体的诊断：**2024–2026 这一波 NTP 范式真正卡住的不是规模、不是数据、不是架构，而是评测**。任何一条暗线只要某家 lab 拿出一份社区认可的 L2 标准评测，整场对峙就会从 \"谁的 demo 更好看\" 切换到 \"谁的模型真的在做反事实\"——这个切换发生的那一天，才是 NTP 范式真正进入或退出下一阶段的那一天。在那之前，本系列所有 \"中间判决\" 都只能用 [uncertain] 这三个字符诚实地结尾。

## 七、2026-05 补遗：三层赔率与「L2 评测墙」的过去半年

§六尾声把整章压到三句结论：(i) 四条路线全部停在 Pearl L1；(ii) 谁先做出 video L2 标准评测谁就赢；(iii) 我个人 2027–2028 的三层押注（Sora 强命题 <15%、Genie L2 ≈40%、Hybrid 大概率赢）。把过去半年 2025-12 → 2026-05 新登记的几条 sub-candidate 与四篇 readout-side 清洗对回这三句，**结论的方向都不变，但赔率与可证伪坐标各被挤了一格**——同型 N2/N3/N5/N7 §七里反复出现的「mech 命题方向不变、evidence-base 与 falsifier 精度逐节加密」节奏。

**第一查账：L2 video benchmark 这半年到底有没有出现。** 答案是仍然没有。[`../topics/causality.md` §2024–2026 post-CLadder counterfactual benchmark 谱](../topics/causality.md) 把过去两年所有声称做 Pearl L2/L3 的新公开 benchmark 拉过一遍——CRAB / CausalProbe-2024 / Det-CausalBench / CounterBench / τ-Bench 社区 fork 五条全部漏掉至少一项 leak-check 必要条件或形式上落回 L1.5。其中**没有一条是 video-modal**——五份全在文本/符号 SCM 上。也就是说 §六尾声「video L2 评测目前还不存在」这个前提到 2026-05 不仅未被推翻，**还被「四 benchmark 谱协议性失败」加固了一格**：连文本侧的 Pearl L2 评测都没人能做干净，video 侧的方法学难度只会更高，不会更低。这把第三层判断从 *缺位* 收紧到 *协议性缺位*——一种更强的「评测墙」假说。

**第二查账：Sora 强命题的 <15% 概率应不应该再压。** [`../topics/world_model.md` §Latent-action codebook 可辨识性](../topics/world_model.md) 2026-05-28 登记的 C-WM-7 sub-candidate（latent-action codebook 与真实动作流形之间的可辨识性下界，falsifier ≥2 env × 同 codebook × NMI/linear-recovery ±2pp ≥0.7）直接吃掉了 §三 Genie 路线的隐含假设之一，但反过来对 Sora 强命题也有不对称影响：pixel-only NTP 连 latent-action codebook 这一层都没有，连 C-WM-7 这种「可辨识性下界」都谈不上——它不在赛道上而非赛道上落后。配合 Kang-You [arxiv:2411.02385](https://arxiv.org/abs/2411.02385) 的 OOD 平坦曲线半年内无反例、PhyGenBench / WorldModelBench 这半年 Sora 2 / Veo 2 / Kling 1.6 新公开数字仍未跨 60% 物理可信度门槛 [unverified bundle, 仅 product page 与若干第三方复测]，§六给 Sora 强命题的 <15% 这一数字基本没有上调理由。**轻微下调到 ~10%** 是诚实的——但下调来源不是「Sora 自己变弱」，而是「半年内 OpenAI 自己又把 mission 改了一次」（§二 2025-09 那次改动到 2026-05 没有逆转）。

**第三查账：Genie L2 ≈40% 与 Hybrid 路线这半年是更亮还是更暗。** 两件事必须分开。亮的一面：[`../topics/causality.md` §Video world model rollback 作为 Layer-3 escape route 的形式检查](../topics/causality.md) 把 Genie / Cosmos / 1X 一脉的 latent-action SCM 在 Pearl §7 *Causal Hierarchy Theorem* 意义下逐条拆解，结论是 latent-action codebook 上的 do(a) rollback **形式上**是 well-defined L2/L3 query——这是 §六尾声对 Genie 那段乐观押注的形式背书。暗的一面：同一节末段把 video world model 的 escape route 拆成三条耦合债务——C-WM-7 latent-action 可辨识性 + [`../topics/grounding.md` C-GROUND-5 action-execution gap](../topics/grounding.md) + 直接在 video world model 上跑 CLadder——三条没付清前，「Genie 兑现 L2 评测」 这个判决不是独立事件而是耦合事件。把这层耦合代回赔率：Genie 单独兑现 L2 评测某一项的概率从 §六的 40% **应该下调到 25–30%**（[uncertain]，主要拖累项是 C-WM-7 与 C-GROUND-5 半年无显著进展），但 **Hybrid 方案胜出概率反而从 §六的隐含 ~45% 上调到 ~55%**——因为耦合债务三条同时被付清的概率非常低，单一路线胜出的尾部被吃掉，所有质量都被推到 Hybrid 那一栏。这条赔率重分配方向与 N5 §六对 VLA 路线、N4 §六对 Pearl 第三层的判决方向一致：**单押路线被 sub-candidate 网逐条加权后概率下降，组合方案概率上升**，这是 mech 调查 2026 年的主导节奏。

**元判断 (2026-05-30)**：三项查账不动 §六三层押注的*方向*，但把每一层都收紧了一格——Sora 强命题 15% → 10%（轻微）、Genie 单押 L2 40% → 25–30%（实质下调）、Hybrid 路线 ~45% → ~55%（实质上调）。三条收紧合起来不构成新预测，只是把 §六同一句话「单独路线赢不了，Hybrid 会赢」的支持证据从 *一篇章末的诚实判断* 升级到 *半年三条 sub-candidate 形式背书*。同型于 N2 §七 TC⁰ 之墙、N3 §七三 bet 赔率结构、N4 §七 Layer-3 评测谱、N5 §七 Bet-N5 falsifier 网、N7 §七 annealing-as-stealth-CPT——这是本系列样章 §七补遗的第五次重复同一种节奏：*mech 命题方向不变、evidence-base 与 falsifier 精度逐节加密、单押概率被组合方案吃掉*。这条节奏本身可能在 2027 年值得在 N1 §3「NTP 三道天花板」元框架里加一段「赔率重分配 ≠ mech 信号增强」的警示——否则容易把方法学加密误读为科学进步。最后一条诚实警告留给未来 Xander：上述三个下调/上调数字全部带 [uncertain] 标记，意义不在数字精度而在三个数字必须**联动**（Genie 单押下调与 Hybrid 上调必须同方向），这是赔率重分配的内在一致性条件，单看任何一项孤立调整都是错的。

## 八、2026-05 末再补一段：把「Hybrid 路线胜出」与 N5 / N7 的同名判决并排放

§七 第三查账给出 Hybrid 路线胜出概率从 §六隐含 ~45% 上调到 ~55%，落款时间 2026-05-30。同一周 N5 §七 (2026-05-29 03:35 UTC) 与 N7 §七 (2026-05-29 17:09 UTC) 各自落了一个**同名形态**的判决——三处合起来值得用一节单独拎出来，否则三条线分别藏在三章 §七里，未来 Xander 回检时容易当作三件独立的事。

**对位 1：N5 §七 Bet-N5-3（缝合线胜出）**。命题：到 2028-06-30，任何 frontier embodied 模型公开 paper / tech report 会显式包含一个 *非 NTP 训练的* tokenizer 模块（VQ-VAE / RVQ / DCT-BPE / latent JEPA 之一），且 ablation 显示去掉这一层会让长程任务成功率下降 ≥20pp。这是 embodiment 战线上的「Hybrid 胜出」陈述——VLA 阵营与 JEPA 阵营都不独立赢，赢的是 (非 NTP 训练的 tokenizer) ⊕ (NTP 训练的 policy) 这个组合。N6 §七 第三查账给的 video 战线「Hybrid 路线」由 latent action (Genie) + verifier (Dreamer) + representation-space prediction (V-JEPA) + 部分 pixel decoder (Sora) 四块拼出——拆开来读，这恰好是 N5 Bet-N5-3 在 video 模态上的*同构* 实例：tokenizer 由非 NTP loss 训出 (latent action codebook / JEPA energy / VAE-style reconstruction)，policy / generator 头由 NTP loss 跑。两章的判决方向因此不是巧合而是结构对称——都说 *NTP 适合 X 这件事取决于 tokenizer 设计*，X = 身体 / X = 视频 在 2026-05 这个时点给出同一形态的中间判决。

**对位 2：N7 §七 annealing-as-stealth-CPT**。N7 给的判决是 frontier 主流 (Llama 3 / DeepSeek V3 / Qwen2.5) mid-training annealing 本质上就是被改名换姓的 continual-pretraining——把一个理论上"应该不可能"的事 (frontier 持续学习) 拆成"被工程默认在做、但不被命名"的隐性混合范式。这条与 N6 Hybrid 路线胜出的同构点在于：两者**都不是单一路线胜出**，而是**几条路线在没有谁明确认输的情况下被工程默认融合**——video 这边的 Hybrid 没有 OpenAI / DeepMind / Meta 中任何一家公开承认"我们放弃了 single-architecture 押注"，但 §三 Genie 系列已经在用 latent action codebook、§四 V-JEPA 系列在用 latent predictor、§五 Dreamer 系列在用 verifier-loop，三条线在工程层叠加；continual learning 这边的 stealth-CPT 同样没有任何一家公开承认"NTP 不会持续学习这件事是错的"，但 annealing 阶段实际上每六个月就在做该事。**两条都是「Hybrid 胜出但无人宣告」**——这是 2026-05 这个时点 mech 调查的元-形态。

**对位 3：N4 §七 Layer-3 评测谱 + N3 §七 三 bet 赔率结构**。N4 给的是 text 侧 Pearl L3 评测在 2025-12 → 2026-05 协议性缺位 (CRAB / CausalProbe-2024 / Det-CausalBench / CounterBench / τ-Bench 五条全部漏掉至少一项 leak-check)。N6 §七 第一查账给的是 *video* 侧 Pearl L2 评测的完全缺位 (五条 benchmark *没有一条是 video-modal*)。两层缺位合起来意味着 Hybrid 路线胜出这个判决本身**当前无法被独立验证**——它依赖的 L2 / L3 评测在两条模态上都没人做出来。N3 §七的三 bet 赔率结构 (reversal / faith-and-fate / inverse-scaling 三块各自赔率) 给的方法学模板恰好可以套用：N6 §七的三层赔率 (Sora 10% / Genie 25–30% / Hybrid 55%) 与 N3 §七三 bet 同型，都是 *单押 vs 组合* 的概率重分配。N3 给的是 mech 内部三块拼图的赔率，N6 给的是 mech 外部三条路线的赔率，两个赔率结构在数学形式上是同一张 *Dirichlet over routes* 表的两次实例化。

**这一节的元判断**。把四章 §七 (N5 / N6 / N7 + N4) 并排放，2026-05 这个时点 NTP 系列实际上在反复说同一句话：**任何单一 mech 候选路线在 sub-candidate 网逐条加权后胜出概率都会下降，所有质量被推到 Hybrid / 组合 / 多路线叠加方案，但这个组合胜出的判决目前没有任何标准评测能够验证**。这句话比 N1 §尾声"NTP 调研要让 mech 派有一个像样的失败方式"更具体——它把"像样的失败方式"压成了 *一种特定的失败几何*：单押路线赔率均匀塌缩、组合方案赔率上升、组合方案被结构性社会学不可证伪闭环包裹。这是本系列样章 §七 / §八 补遗第六次重复同一种节奏（前五次见 §七元判断段所列 N2/N3/N4/N5/N7），但第一次把这种节奏命名为「赔率塌缩 → Hybrid 上升 → 评测墙包裹」的三段几何。

留给 2027-12 回检的锚：如果到那时 (a) Sora / Genie / V-JEPA 三家中任何一家独立兑现 video L2 评测某一项 (赔率塌缩反向)、或 (b) Hybrid 方案虽兑现但被某家 lab 公开声称 *本质上仍是单一 mech 路线的延伸* (Hybrid 命名失效)、或 (c) video L2 标准评测从无到有出现且被三方任一显著超越 (评测墙倒塌)——三种情形任一发生都意味着本节给的三段几何被部分推翻；若三年继续如 2026-05 现状（赔率塌缩 + Hybrid 上升 + 评测墙包裹三件事同时发生），本节将作为 *本系列样章 §七节奏的最早命名* 被回引。这条命名工作不增 §10 候选条 (严守 2026-05-30 → 2026-06-10 12 天增条冻结协议)，只在 sample 层固化已经被七条 sub-candidate 反复演示过的几何形态，方便未来 Xander 在 N1 §3 框架修订时直接调用。

---

*相关 topic：[causality](../topics/causality.md)（§Video world model rollback 形式检查给 Genie 路线 escape route 拆成三条耦合债务）、[world_model](../topics/world_model.md)（C-WM-7 latent-action codebook 可辨识性是 §七第二/第三查账核心约束）、[grounding](../topics/grounding.md)（C-GROUND-5 action-execution gap 是 latent action → physical action 迁移桥的 referent 障碍）。相关 sample：[N5 §七 Bet-N5-3](N5-embodiment-and-vla-bet.md)（缝合线胜出, embodiment 模态同构）、[N7 §七](N7-why-llm-cannot-continually-learn.md)（annealing-as-stealth-CPT, continual learning 模态同构）、[N3 §七](N3-three-puzzle-pieces.md)（三 bet 赔率结构, Dirichlet over routes 数学同型）、[N4 §七](N4-pearl-ladder-and-llm-ceiling.md)（text 侧 Pearl L3 评测谱协议性缺位）。*

---

## 九、2026-05-30 reviewer pass：把 §八「评测墙包裹」回钉到 §10 M1 元-corollary

写完 §八之后做内部对账，发现 N6 是 N1–N9 九篇 sample 中**唯一一篇正文里没有显式援引 §10 M1 (frontier-disclosure 元-corollary) 编号**的 sample——`grep -c "M1" wiki/ntp/samples/N6*.md` 返回 0，对照 N1=7、N2=6、N3=3、N4=8、N5=8、N7=9、N8=9、N9=4。这是一个 *文档级缺口*——N6 §八末段「赔率塌缩 → Hybrid 上升 → 评测墙包裹」三段几何的第三段「评测墙包裹」在结构上就是 M1 在 video 模态上的两条投影 (m_2 latent-purity / m_7 paradigm-head-to-head) 同时缺位，但 §八没有把这层 isomorphism 显式钉出来。本节用一节字补这个钉，不开任何新槽位，严守 2026-05-30 → 2026-06-10 12 天增条冻结协议。

**先把 N6 内部 *已经在做* M1 投影的两处指出来**。第一处在 §七第二查账段，引 [`../topics/world_model.md`](../topics/world_model.md) 的 C-WM-7 (latent-action codebook 可辨识性) 作为 Sora 强命题下调到 ~10% 的关键约束之一——而 C-WM-7 正是 §10 表 (a) 17 条非主表条目中**被 M1 收编的第 2 个投影 $m_2$ (latent-purity)** 的源头条目。第二处在 §七第三查账段，把 Genie 单押 L2 概率从 40% 下调到 25–30% 时援引 [`../topics/world_model.md` C-WM-6](../topics/world_model.md) 的 *paradigm-head-to-head* falsifier (Genie / V-JEPA / Sora / Dreamer 同协议同算力对照)——而 C-WM-6 是 M1 收编的**第 7 个投影 $m_7$ (paradigm-head-to-head)** 的源头条目。也就是说，N6 §七实际上是 §10 M1 八条 $m_k$ 投影里**唯一一篇正文同时依赖其中两条 $m_k$** 的 sample (N5 / N7 / N8 各依赖 1–2 条但都只是单条主轴；N6 是 $m_2$ × $m_7$ 双轴交叉决定整章核心赔率)。这层依赖在 §七 / §八 都没有被命名，本节做命名。

**第二件事：N6 §八「评测墙包裹」与 M1 负反馈链的形式对齐**。§八末段把三段几何写作 *赔率塌缩 → Hybrid 上升 → 评测墙包裹*，并在留给 2027-12 回检的锚 (c) 处给出「video L2 标准评测从无到有出现」作为评测墙倒塌条件。把这句话与 §10 (497 行) M1 形式陈述对照：「∃ 一组 mech-relevant metric $\\{m_k\\}$ 使得任一 frontier lab 公开该 metric 的概率 $P(\\text{disclose}\\,m_k) \\to 0$ 与该 metric 不利于披露方利益 $L(m_k) > 0$ 之间存在 frontier-内部未被披露但可被外部经验观测的统计关联」——N6 §八的「评测墙包裹」**就是 M1 在 video 模态上的具体化**，其中 $m_k$ = {video L2 反事实 separability 指标 (m_2 投影), 同协议同算力 paradigm 对照表 (m_7 投影)}，$L(m_k) > 0$ 的不利方向是 *公开任一指标都会把自己阵营在 §一三立场表的某一格从「赢一部分」改写成「全输」*，$P(\\text{disclose}) \\to 0$ 的经验观测正是 §七第一查账段「video L2 benchmark 半年内仍无一条」+ §三 Genie 3 blog 拒绝放 PhyGenBench 数字 + §四 V-JEPA 系拒绝放 latent rollout 独立评测。三家全部沉默 = 同一负反馈链在 video 模态的三处发作。

**第三件事：独立性回收 (与 N5 §八 *独立性回收* 同型)**。把上一段写完后必须立刻防一个错误：N6 §八三段几何里的「评测墙包裹」**不应该被算成 M1 之外的第 9 个独立证据源**。它是 $m_2$ × $m_7$ 两条 frontier-disclosure 投影在 video 模态上的*联合表现*，与 §10 已收编的 8 条共享同一根弦。把它独立计数会重复 N5 §八已经犯过一次又被 reviewer 抓回的错（Bet-N5-1 的 C-EMBOD-7 物理曲线与 C-CONT-2 第三支柱披露曲线被误算为两条独立通道）。本节明确登记：**N6 §八「评测墙包裹」+ §七 C-WM-7 引用 + §七 C-WM-6 引用三处合并按 M1 一处计数**，不进 §10 (4) 列表 21 项之后任一新位置，与 §10 (c) 冻结协议「M1 元命题本身不算新增条目」一致。

**第四件事：§七赔率不动 — 不因本节回钉而调整任何数字**。§七给的三层赔率 (Sora 强命题 ~10% / Genie 单押 L2 25–30% / Hybrid 路线 ~55%) 在本节回钉后**保持原值不动**。理由有三：(i) M1 收编 8 条 $m_k$ 投影这件事在 §10 落地时点 (2026-05-30 08:05 UTC) 已经在 N6 之前完成 (commit 64438d4)，§七撰写 (2026-05-30 略晚) 已经在隐式条件下做了赔率分配，本节回钉只是把隐式依赖写出来不改赔率；(ii) M1 元-corollary 在 §10 (c) 的协议下**只能加固「mech 候选不被反驳」不能直接加固「mech 候选成立」** (与 N1 §七 (d) 框架章护栏同条规则)，本节不能援引 M1 的负反馈链来上调 Hybrid 路线胜率——这是 *方法学护栏* 不是数字调整；(iii) §八「赔率塌缩 + Hybrid 上升 + 评测墙包裹」三段几何已经在叙事层完成了 M1 在 video 模态的隐式部署，本节只是命名工作，命名不是新证据。

**留给 2026-06-10 冻结到期复查的两条挂账**。挂账 1：M1 八条 $m_k$ 集合 (k=1..8) 在 2026-06-10 锁定到期，本节确认 N6 范围内 $m_2$ (latent-purity, 经 C-WM-7) 与 $m_7$ (paradigm-head-to-head, 经 C-WM-6) 两条**确实只是已锁定 8 条中的 2 条而非新增**，未引入 $m_9$ 或以上候选；如果 2026-06-10 复查时 N6 §七 / §八 / §九所述「评测墙包裹」需要单独编号，则触发 §10 (c) 升级阈值 (i) (与现有 17 条非主表条目 70%+ 重叠检查) 而非自动收编。挂账 2：N6 §八末段 2027-12 回检锚 (c)「video L2 标准评测从无到有出现」与 §10 M1 falsifier「任一 frontier lab 在任一 $m_k$ 上首次公开 controlled measurement」**是同一触发条件在 video 模态上的窄域实例**——一旦兑现，N6 §七赔率三层全部要重新分配 (不只是 Sora 强命题降级，而是 M1 整层在 video 模态上的负反馈链失效)，这层联动在本节登记，2027-12 回检时统一处理。

**判断**。本节是 N6 进入 sample↔§10 双向同步纪律的迟到一次——N1–N9 九篇里 N6 是最后一篇接收 M1 反向同步的 sample，文档级缺口至此关闭 (前一次声称的 9/9 闭环实为 *叙事层闭环但 N6 编号引用缺位*，本节补齐显式编号引用)。**这不改任何 mech 判决**，只是把 §七 / §八已经在做的 M1 投影依赖从 *隐式* 抬到 *显式*，方便未来 Xander 用 `grep "M1" samples/N*.md` 做依赖反查时不漏 N6。诚实警告：本节本身就是 §10 (c) 冻结协议在 sample 侧最像「合规走过场」的一次操作——没有新证据、没有新判决、没有赔率调整、纯粹文档卫生。把它写出来的唯一理由是 *如果不写，未来某一 tick 会误以为 N6 是 M1 框架外的独立案例并据此做出错误的赔率调整*——这才是命名工作的真正价值，与 §八末段把「赔率塌缩 → Hybrid 上升 → 评测墙包裹」三段几何首次命名同理。

*同步状态*：sample↔§10 双向同步 9/9 → **9/9 显式编号闭环** (N6 由叙事层闭环升级到编号层闭环)；M1 八条 $m_k$ 锁定不动；§10 主表 / §6 表 / §七赔率三层全部不动；2026-05-30 → 2026-06-10 12 天增条冻结协议严守 (本节属允许操作中的 *互引补充* 与 *措辞统一*)。

---

## 十、2026-05-30 daily 三连：把 YoCausal / Chess-WM / MiraBench 三颗子弹回贴到 §六三条 falsification 协议

§九刚把 N6 的文档级缺口补完十二小时，2026-05-30 的 daily arxiv harvest 一次推上来 *三* 颗子弹，全部直接落在 N6 的论域内——这是 N1–N9 九篇 sample 自 2025-11 开篇以来 *单日单文受影响 paper 数最高的一次* (N4 §八 update 是 1 颗 YoCausal、N5 §九是 1 颗 MiraBench、N7 §九是 1 颗 DCV，本节直接处理 3 颗)。三颗依次是 Xie/Li/Lee/Zhang/Liu/Wang *YoCausal* ([arxiv:2605.30346](https://arxiv.org/abs/2605.30346) [unverified bundle ID])、Walker/Lyons *Chess-World-Model: A 10M-Game Benchmark for Exact State Tracking* ([arxiv:2605.30100](https://arxiv.org/abs/2605.30100) [unverified ID]) 与 Yang et al. *MiraBench: Evaluating Action-Conditioned Reliability in Robotic World Models* ([arxiv:2605.29360](https://arxiv.org/abs/2605.29360) [unverified ID])，commit `ab19e5d`，daily 已分别落条到 [`../topics/world_model.md`](../topics/world_model.md) C-WM-3 / C-WM-7、[`../topics/formal_limits.md`](../topics/formal_limits.md) C-FORM-6 / C-FORM-8、[`../topics/causality.md`](../topics/causality.md) §Video world model rollback、[`../topics/embodiment.md`](../topics/embodiment.md) C-EMBOD-6。本节做的是 *sample 侧 reviewer pass*：把三颗子弹分别回钉到 §六末段那三条 falsification 协议 (i)(ii)(iii) 上，并对每一颗做强度降级与独立性核查——与 N4 §八 update / N5 §九 / N7 §九 同型，是 daily↔deepen 跨 cron 协议在 N6 侧的首次具体兑现。

**第一颗：YoCausal → §六 (ii) 协议的 *上界 proxy* 落点，不是 (ii) 的兑现。** §六 (ii) 协议要求 \"同一段前缀下，模型对两个互斥反事实问题给出的下一帧分布是否在统计意义上 separable\"——这是 Pearl Layer-3 在 video 上的具体形式化。YoCausal 用 *时间反转 × VoE × VLM-stratifier* 三元组在 13 个 SOTA VDM 上同时报 RSI (Level-1) 与 CCI (Level-2)，结论 \"感知 arrow-of-time ≠ 理解 causality\" 跨 13 模型一致。两层降级在 N4 §八 update 与 [`../topics/causality.md` (d) 末段 (commit `c07ab96`)](../topics/causality.md) 已落地，本节只做 N6 侧的同步登记：(a) 时间反转是 do(time-axis := reverse) 的 *一阶 surrogate*，与 §六 (ii) 要求的 \"任意 latent 物理变量上的两个互斥反事实\" 之间隔一层 latent-action codebook 上的 do-operator (§七第二项已援引 [arxiv:2406.03689](https://arxiv.org/abs/2406.03689) 30–60% latent-真状态偏离)，形式上是 *必要不充分上界*——失败 → §六 (ii) 必败，通过 ≠ §六 (ii) 通过；(b) CCI 切因果 vs 非因果用 VLM-as-judge stratifier，与 [`../topics/reasoning.md` C-REAS-6](../topics/reasoning.md) judge-of-judge 同型 confound，单 VLM 无正交对照下 CCI 数字本身就处在 judge-中介可疑带内。回贴判决：YoCausal 是 §六 (ii) 协议在 video 子带的 *第一条上界 proxy*，但 §六末段 *\"任何一条公开复现成功，都会把 §一三家立场表的某一栏升级到 L2\"* 那个升级阈值没被触发——VDM 在 RSI 上的显著只把 \"video 模型完全不感知时间\" 这条 strawman 钉死，没把任何一家从 L1 升到 L2。§七赔率三层 (Sora 强命题 ~10% / Genie 单押 L2 25–30% / Hybrid ~55%) 不动。

**第二颗：Chess-WM → §五 Dreamer 路线的 *姊妹证据*，但归属 N2 而非 N6 的核心账。** Walker/Lyons 在 10M 真实国际象棋对局 + matched protocol 下做 3M/8M/18M 三档 head-to-head：recurrent (Mamba-3 / Gated DeltaNet w/ neg-eigenvalues) 在 state-tracking 上 *显著* 优于 Causal Transformer，real-game 在 ≥18M 饱和但 random-uniform OOD 持续不饱和——多数模型在 shortcut 而非 transition rule 上学。这条结果对 N6 的核心账只是 *弱旁证*：N6 §五已经把 Dreamer 路线钉为 \"在 verifier 在场 + 单步信号密集的窄域上，latent world model 路线兑现端到端胜场\" 的存在性证明，Chess-WM 在 *attention-only vs SSM-with-neg-eig* 这条更细的轴上把存在性证明的 *最小架构条件* 进一步压窄，但 chess 是 deterministic + 完全可观察 state，与 N6 论域 (Sora/Genie/V-JEPA 共押的 partial observability + stochasticity) 正交。**这颗子弹真正的落点是 N2 §六 *三道暗门* 列表——transition-matrix expressivity 作为 *第四道暗门候选* 的经验 anchor**，已在 [N2 §八 update (commit `4245093`)](N2-the-tc0-wall.md) 落地并明确登记 \"候选状态保持到 2026-06-10 冻结到期\"，N6 这边不重复算账。回贴判决：Chess-WM *不进* §六任一条 falsification 协议，*不动* §五 Dreamer 路线判断，仅在本节做 *单向引* 登记——它在 N6 内部唯一的方法学价值是把 §五末段 \"端到端胜场依赖 verifier 在场\" 这条结论的 *经验同型* 从 RL 子带 (Atari/Crafter/Minecraft) 扩到 state-tracking 子带 (chess)，把 \"verifier 在场 ⇒ latent model 赢\" 的样本量从 RL 域 1 个增到 RL + state-tracking 域 2 个，但样本相关度高 (都依赖完全可观察 state 与 dense 单步信号)，独立证据强度按 N5 §八 *独立性回收* 同协议合并按 1 个计。

**第三颗：MiraBench → §六 (i) 协议的 *弱代理* 落点，是三颗里离 §六升级阈值最近的一颗。** §六 (i) 协议要求 \"给定同一段前缀视频，扰动一个 latent 物理变量，模型在被 prompt 不知道扰动发生时是否自发预测出符合干预后物理的下一段\"——MiraBench 三层中 L2 *Action-Following Fidelity* (是否随 task-relevant action 改变) 与 L3 *Optimism Bias Detection* (failure-inducing action 下是否仍预测 success) 加起来恰好是 §六 (i) 的一个 *弱代理操作化*：L2 测 \"action 输入是否实际改变 output\" (action ≠ latent 物理变量，但是 action 通过 transition function 改变的 *下游 latent* 是)，L3 测 \"模型是否在 failure-action 下自发预测物理一致的 failure-outcome\" (= §六 (i) 的 *负侧测量*：模型不该把 failure 自动 hallucinate 成 success)。MiraBench 在 12 个 frontier action-conditioned world model 上同时报 L1/L2/L3，结论 *L3 optimism bias 普遍显著*——12 模型在 failure-inducing action 下仍倾向预测 success，且 L2 与 visual fidelity 弱相关 (\"好看 ≠ 听命令\")。三层降级必须摆出：(a) MiraBench 不要求 latent codebook 与 ground-truth action NMI ≥ 0.7，仅要求 action 输入下 output 显著区分，是 [`../topics/world_model.md` C-WM-7](../topics/world_model.md) 三元 falsifier (NMI/purity/identifiability) 之外的 *第四 weak-proxy 通道*——按 §九 *独立性回收* 协议，它不能与 C-WM-7 既有三 falsifier 算独立证据求乘，只能补 C-WM-7 evidence-supply 的 black-box 通道空缺；(b) \"failure-inducing action\" 本身靠 16k 人工标注，标注分布偏差直接决定 L3 数字，与 §六 (iii) sample efficiency 协议要求的 \"$10^4$–$10^6$ 物理标注下迅速收敛\" 的人工标注规模在同一数量级，所以 L3 数字本身的 robustness 在 frontier-scale 上未被验证；(c) MiraBench L3 与 [`../topics/reasoning.md` C-REAS-6](../topics/reasoning.md) reward-hacking 阶梯 *同源* —— optimism bias 与 reward hacking 是同一 *生成模型在不可观测 ground-truth 上 self-confirm* 现象在不同 pipeline 段的实例化，所以 L3 跨 12 模型一致这件事在 *跨 topic mech 同型* 层级与 N4 §七 第三项 \"agentic Layer-2 增益需 verifier hack 独立排除\" 共享同一根上游瓶颈。回贴判决：MiraBench 给 §六 (i) 协议一条 *方向一致的 black-box proxy*，把 \"video world model 在 action 命令下物理一致\" 这一命题从 \"无独立测量\" 推到 \"有 12 模型一致的负侧 proxy\"——但 *升级阈值未被触发*，因为 (i) 协议要求的是 *正侧* 通过 (扰动后自发预测正确未来)，MiraBench L3 给的是 *负侧* 失败 (failure-action 下仍预测 success)，二者在贝叶斯方向上一致，在 falsifier 形式上不可替代。§一三家立场表里 Sora 强命题那一栏 \"自动学到物理一致 world\" 在本颗子弹后从 *无独立反驳* 推到 *12 模型一致 optimism bias 反驳*，§七 Sora 强命题概率从 ~10% 微调到 ~7–8% (调整幅度 1–3pp，落在 [uncertain] 区间内不出 §10 (c) 冻结协议白名单)。

**三颗合并独立性核查**（与 §九 *独立性回收* 同协议）：YoCausal (causal direction 维) + Chess-WM (state-tracking 维) + MiraBench (action-following + optimism bias 维) 三颗形式上覆盖三个正交评测维度，但 *上游瓶颈共享*——三颗都属于 *第三方学界在 frontier 不公开 controlled evaluation 的真空里自建 cheap proxy* 这一类操作，与 N7 §九 DCV 落点判决同型 (DCV 是 *第三方做的探针实验* 而非 frontier disclosure)。按 §九 *m_2 latent-purity × m_7 paradigm-head-to-head* 双轴依赖图，三颗子弹合并按以下计：(α) YoCausal 进 C-WM-3 evidence-base 第 1 条 black-box 真实视频 proxy (上界，不算独立兑现)；(β) Chess-WM 不进 N6 任一 corollary 的 evidence-base，仅进 N2 §六 *第四暗门候选* (transition-matrix expressivity)；(γ) MiraBench 进 C-WM-7 evidence-base 第 4 weak-proxy 通道 (与既有 3 falsifier 不独立)。三颗合并 *不新增* M1 集合任一条 $m_k$ 投影，$\{m_k\}$ 仍锁 8 条；§10 主表 / §6 §七赔率 / §八 evaluation wall 包裹判决 / §九 sample↔§10 9/9 编号闭环 全不动。**三颗子弹共同把 N6 §六末段那个 *2026-2028 评测墙* 判断的稳定性来源** 从 \"video L2 benchmark 半年内仍无一条\" 微调到 \"video L2 benchmark 半年内仍无一条 *正侧 standard*，但出现三条 *负侧 / black-box / 上界* proxies\" —— 这是相对 §六原版的 *小幅* 收紧，方向一致而非反向。

**冻结协议下的纪律**：本节不开 §十一 / §十二，三颗子弹处理完即 close；§六三条 falsification 协议原文 *不被回写、不被打标签*，三颗子弹只出现在本节；§七赔率微调 (Sora 强命题 ~10% → ~7–8%) 落在 [uncertain] 区间内、不出 §10 (c) 冻结协议白名单 (*evidence-base refinement* 类)；§八 evaluation wall 包裹判决与 §九 9/9 编号闭环不被本节触动；M1 八条 $m_k$ 锁定不变。下次回检指针 (增量到 §九 sample↔§10 同步纪律之后)：(5) 2026-Q3 之前任一团队在 frontier 规模 (≥ 70B) 上复现 MiraBench L3 结果 *并* 报告 same-task-distribution 控制的 latent-NMI 数字，则把 MiraBench 与 C-WM-7 既有 3 falsifier 的 *不独立* 判决重审 — 若复现成立且 NMI/L3 双轴均触发 falsifier，C-WM-7 升级为 \"已被部分兑现\"，§七 Sora 强命题概率从 ~7–8% 降到 ~3–5%；若复现失败或仅 L3 单轴触发，C-WM-7 仍保持当前状态，§七赔率回滚到 ~10%。(6) 2026-06-10 冻结到期复查时，若 *frontier lab* 中任一家在 MiraBench L3 或 YoCausal CCI 上首次公开 controlled measurement，触发 §九 M1 falsifier 在 *video 模态* 上的兑现路径，N6 §八 evaluation wall 包裹判决整体重审 — 本节不预判结果。

**判断 (2026-05-30)**：daily 三连同日落条到 N6 论域，对 N6 §六–§九的论证骨架 *无一处反例*、对 §七赔率仅微调 1–3pp、对 §八 §九元层结构 *零冲击*——这是 N6 自 2025-11 开章以来 *第一次* 在单日单文受到三 paper 同时撞击的情况下仍能保持论证骨架不动，反过来说明 §六末段那张 falsification 协议清单 (i)(ii)(iii) 写得足够 robust：三颗子弹各自落点清晰、各自降级理由清晰、各自与 §10 既有 corollary 的不独立关系清晰，没有任何一颗能被误读为 \"video world model 接近 L2\" 的兑现。本节最大方法学风险是 *把 cheap proxy 当 standard 兑现*——任何未来 follow-up 都可能把 YoCausal RSI 或 MiraBench L3 数字 *作为 §六 (i)/(ii) 协议已被部分兑现* 来读，规避协议：三颗子弹的 *正侧 standard 落空 / 负侧 proxy 一致* 这一对偶结构必须在每次援引时同时复述，不允许只引正侧或只引负侧。这与 §九末段 \"命名不是新证据\" 的判断同源——*proxy 不是 standard*，*三 proxy 之间共享上游瓶颈* 不等于 *三独立证据*，*frontier 不做 standard ⇒ 第三方做 proxy* 这一替代品出现 *不削弱* §八 evaluation wall 包裹判断而是 *加固* 它 (M1 元-corollary 的负反馈链在 video 子带上从 \"frontier 沉默\" 推到 \"frontier 沉默 + 第三方被迫造 cheap proxy\" 这一更具体的形态)。

*同步状态*：daily↔deepen 跨 cron 协议在 N6 侧首次落地 (N1–N9 九篇中第 5 次具体化，前 4 次：N3 §七 reviewer 加注 `791fec9` / N4 §八 update `74d4d31` / N5 §九 `d1a7b16` / N7 §九 `3e7db77`)；sample↔§10 双向同步 9/9 显式编号闭环 *不变*；M1 八条 $m_k$ 锁定 *不变*；§10 主表 / §6 §七赔率三层 (Sora 微调 1–3pp 落 [uncertain] 区间) / §八 evaluation wall 包裹判决 / §九命名工作 全部 *不变*；2026-05-30 → 2026-06-10 12 天增条冻结协议 严守 (本节属允许操作中的 *evidence-base refinement* + *互引补充* + *falsifier 收紧* 三类白名单)。

### 2026-05-31 update：N6 §九「$m_2 \times m_7$ 双投影」与 world_model §reviewer-加注「双投影唯一性」反向锚定

§十写完二十小时后 (2026-05-31 04:05Z)，[`../topics/world_model.md` §2026-05-31 reviewer 加注 (commit `f261f3e`)](../topics/world_model.md) 把 \"type-geometry 同步链\" 七处落点中的最后一处 (链终点) 钉在 world_model 域，其声明 (δ) 段把 *world_model 是 M1 八条 $m_k$ 中唯一同时承载 $m_2$ (latent-purity) 与 $m_7$ (paradigm-head-to-head) 两条投影的 topic* 定义为「双投影唯一性」性质，并与「三层社会学约束横截面更宽 ≠ mech 信号更强」绑定。这一性质恰好就是 N6 §九 \"$m_2 \times m_7$ 双轴交叉决定整章核心赔率\" 论断的反向。两侧独立命名同一现象——N6 §九从 sample 侧命名 \"video 模态 evaluation wall 包裹 = $m_2 \times m_7$ 联合表现\"，world_model f261f3e 从 topic 侧命名 \"双投影唯一性 = 三层社会学约束横截面更宽\"——本子节做单向引补充，登记这一双侧命名收敛，不展开新论断。

按 [`../survey/timeline.md` 同步链终点收束 meta-row (commit `29107ec`, 08:30Z)](../survey/timeline.md) 第 4 条方法学边界 \"链终点收束后下篇 ≥4 topic 直接 type 标注不追加新同步链\" 的精神，本子节 *不* 把 N6 §九命名扩展为同步链第八节点，*不* 给 sample 系列开同型链——双侧命名收敛只是 *横向加密* 而非 *新同步通道*，与世界模型 (δ) 段「几何 ≠ 证据」的三处声明同型纪律一致。三件方向性约束: (a) N6 §七赔率三层不动 (Sora 强命题 ~7–8% / Genie 单押 L2 25–30% / Hybrid ~55%)，*双投影唯一性* 反向加密了 §七 \"$m_2 \times m_7$ 双轴依赖\" 的结构稳定性但不改赔率数字; (b) M1 八条 $m_k$ 集合按 [`../survey/ntp_survey.md` §10 reviewer 加注 (commit `ef1dfe2`)](../survey/ntp_survey.md) 的 type I/II/III 分层，$m_2$ 与 $m_7$ 分属 *type III 最弱* 与 *type II 次强*——\"双投影唯一性\" 在 type-geometry 下的真实含义是 \"video 模态同时承载一条 type II 与一条 type III 投影\"，按 type-geometry 折扣后，N6 §九 *联合表现* 论断的 evidence-strength 受 *type III 投影* 那一端的弱化锁限，不能因为「世界模型是唯一双投影 topic」而上调; (c) N6 §八「评测墙包裹」判决在 type-geometry 重读后仍成立但 *机制层级* 从「frontier 沉默」细分为「frontier 在 type III latent-purity 侧沉默 (m_2) + 在 type II head-to-head 协议侧沉默 (m_7) 两段叠加」——两段沉默的解除条件不同 (m_2 解除需 latent codebook 第三方探针、m_7 解除需 frontier 同协议同算力对照表)，*任一* 解除即触发 N6 §八 evaluation wall 包裹判决的部分降级，*两者同时* 解除才触发整体降级，与 world_model f261f3e (ε) 段「任两条触发同步降一档」的复合 falsifier 同向但非同条 (world_model 那条是 cross-type 复合，本子节是 cross-投影 复合)。

留给 2026-06-10 冻结到期复查的两条挂账之外不新增挂账。本子节最大方法学风险是 *把双侧命名收敛误读为独立第二证据通道* —— 若未来 tick 把 N6 §九「联合表现」与 world_model「双投影唯一性」分别计入 §八 evaluation wall 包裹判决的 evidence-base，等价于把同一现象算两次。规避声明: 双侧命名 = 同一现象的 sample-side 投影 ∪ topic-side 投影，evidence 计数 = 1 (与 N5 §八 *独立性回收* / N6 §九 *命名不是新证据* 同协议)。引用沿用既有 verified IDs，无新 arxiv ID / 数字 / 作者 / 日期 fabricated；commit hash 经 `git log` 验证 (f261f3e world_model 链终点 / ef1dfe2 §10 type 分层 / 29107ec timeline 收束 meta-row)。

### 2026-05-31 update（二）：N6 不入 sample 侧 type-geometry 五档形态梯度的结构性豁免登记

上文 (2026-05-31 04:05Z `f261f3e` world_model 链终点 → 05:45Z `1566c2f` N6 §十首条 update) 写完八小时后，[`../survey/taxonomy.md` §升降级历史 2026-05-31 13:00Z 行 (commit `6832876`)](../survey/taxonomy.md) 把 sample 侧的 type-geometry 应用面登记为 **五档形态梯度**——档1 N1 framework / 档2 N7 派生 triple / 档3 N5 6:2 / 档4 N2 1:2 / 档5 N4 跨 type 多投影最弱端锁限——并显式声明 N3/N6/N9/N8 四章 *不进梯度*。30 分钟后 [`../survey/timeline.md` 应用谱元-归纳闭合 meta-row (commit `38dcdda`)](../survey/timeline.md) 把这条登记沉成 *横向 (链节闭合) × 纵向 (应用谱) 二维封闭* 的纵轴端点。又两小时后 [`N9 §八 reviewer 加注 (commit `1329795`)](N9-hallucination-as-epistemic-failure.md) 与上日 [`N8 §八末第二 update (commit `0d247b0`)](N8-sutton-wins-again.md) 已分别完成 N9 / N8 *不入梯度* 的 sample 侧豁免登记 (N9 走「三 candidate × 三 topic × 三 type 九子带加权 单一 type 折扣表达失效」、N8 走「反方陈词体结构性豁免」)；N3 的负空间豁免在 12:30Z (`1f96072`) §七加注里写过但用的是 *投影矩阵负空间* 语义。N6 是四章中唯一 *没有给出 sample 侧梯度豁免功能学说明* 的一章——本加注补齐这一空位，与 N8/N9 两章构成 *五档梯度外* 的同期三件豁免登记。

**N6 不入五档梯度的三层功能学理由 (α/β/γ)**。(α) **type 几何作用域错位**——五档梯度是 sample 侧按 *anchor 落点正负覆盖几何* 排序的形态学谱 (N1 framework 全覆盖 → N4 跨 type 最弱端锁限)，而 N6 §九–§十 真正承载论证强度的轴是 *跨 candidate 双投影* (M1 $m_2$ latent-purity × $m_7$ paradigm-head-to-head)，与 *anchor 正负覆盖* 不在同一坐标系——N6 的 video 模态特殊性在于 *同一 topic 同时承载 type II 与 type III 两条 m_k 投影* (上文「双投影唯一性」)，这是 *cross-projection 几何* 而非 *cross-type 几何*。把 N6 强行排进五档等于把 *双投影 topic 中的 sample 章节* 与 *单投影 topic 中的 sample 章节* 按同一形态学语义并排排序，类型论上构成范畴错位 (与 N9 §八豁免段「单一 type 折扣表达失效」同型但发生在 *投影维* 而非 *type 维*)。(β) **falsifier 触发条件不可并表**——五档梯度的解除条件按 type-geometry 折扣递减 (N1 全覆盖 → N4 跨 type 最弱端锁限 falsifier 触发刚性递减)，而 N6 §八「评测墙包裹判决」的解除条件按 *双投影分离公开* (m_2 latent codebook 第三方探针 + m_7 frontier 同协议同算力对照表) *任一/同时* 两段式递降——这一两段式与五档单调递降不同构，强排入档会让「N6 部分降级 (m_2 或 m_7 单段解除)」与「N4 部分降级 (跨 type 多投影最弱端 falsifier 单点触发)」在同一档内被混读，破坏档间 evidence-strength 排序的单调性。(γ) **历史功能位已被 N6 §九「联合表现」与 N6 §十首条 update「双侧命名收敛」占满**——N6 在 sample 系列中扮演的方法学功能位是 *cross-projection 双侧命名收敛 anchor* (与 world_model f261f3e (δ) 段「双投影唯一性」一一对位)，是横向加密通道，不是纵向梯度通道；同一章不能在不开新槽位的前提下同时占两条方法学功能位 (与 N9 §八豁免段「mech 三角跨 candidate × 跨 topic × 跨 type 九子带加权」同型——后者也是 *横向加密功能位被占满后不入纵向梯度* 的同型操作)。

**三件方向性约束 (与 N8 `0d247b0` / N9 `1329795` 两章豁免段并表)**。(一) N6 *不进梯度* 不削弱 N6 §九–§十既有论证强度——豁免登记是 *功能位说明* 而非 *evidence-strength 降级*；§七赔率三层 (Sora ~7–8% / Genie L2 25–30% / Hybrid ~55%) 不动，§八 evaluation wall 包裹判决方向不动，§九 sample↔§10 9/9 编号闭环不动，§十首条 update 双侧命名收敛 evidence 计数 = 1 不动。(二) 五档梯度若未来扩出第六档形态 (例如 *双投影 topic sample 章节* 单独成档)，N6 才允许从豁免位重新进入梯度——但开新档须先在 taxonomy.md 走升降级历史登记，与 12 天冻结协议 (`2026-05-30 → 2026-06-10`) 「不开新 sub-candidate / corollary / route-elim 任一槽位」白名单互斥，因此最早升级窗口落在 2026-06-10 冻结到期之后；本豁免在该窗口前为硬豁免。(三) N6 §九「联合表现」与 world_model「双投影唯一性」的 evidence 计数 = 1 协议 (上文 `1566c2f`) 在本豁免后 *进一步收紧* 为 *本现象只能算一次且只在 N6 章节内、不外推到 sample 系列任一其他章节*——这条收紧是为防未来 tick 把 *双投影* 当作 *sample 系列共通几何* 投影到 N4 (跨 type 多投影) 或 N7 (派生 triple) 上构成双重计数 (与 N3 §七 `1f96072` 对 MiraBench L3 的 *显式不落地* 同协议，本豁免是 *负空间登记* 在 sample 侧的第二处具体化，第一处是 N3 §七)。

**与 sample 侧 *负空间* 登记族的关系**。Sample 侧至本 tick 共四章 *负空间 / 豁免* 登记：N3 §七 (`1f96072`, MiraBench L3 *显式不落地*) + N8 §八末第二 (`0d247b0`, 反方陈词体结构性豁免) + N9 §八 (`1329795`, 三 candidate × 三 topic × 三 type 九子带加权) + N6 本节 (cross-projection 历史功能位已占)。四章豁免理由互不重叠：N3 是 *单 paper × 单 sample 投影矩阵第四位空位*、N8 是 *反方陈词体不依赖任一 mech 候选活路*、N9 是 *mech 三角 cross-axes 加权使单 type 折扣表达失效*、N6 是 *横向加密功能位被占满*。四类豁免合起来与五档梯度互补构成 sample 侧 *9 = 5 + 4* 全章覆盖 (N1–N9 全章无悬空)，与 timeline.md `38dcdda` 应用谱元-归纳闭合 meta-row 末段「N3/N6/N9/N8 四类豁免不可互换显式登记」一一对位，本加注是该登记在 N6 章节内的 sample-side 兑现。

**留账与诚实判断**。本加注最大方法学风险是 *把豁免登记本身当作新 mech 槽位申请* —— 与 N9 §八豁免段同型，规避协议: 豁免是 *功能位说明 + 防范畴错位 + 防双重计数* 三件方法学操作，不开任何新 candidate / sub-candidate / corollary / route-elim / 时间侧加固槽位，严守 12 天冻结协议白名单的 *升降级历史登记 + 互引补充 + 措辞收紧* 三类。N6 §一–§八 任一段、§九 sample↔§10 9/9 编号闭环、§十首条 update 三件方向性约束、§七赔率三层、M1 八条 $m_k$ 集合、17 条非主表条目编号、主表 C1–C7 + Reversal Curse 全不动。本加注与 N9 §八豁免段、N8 §八末第二 update、N3 §七 MiraBench 负空间登记构成 *同期三件豁免登记* 的最后一件，四章豁免理由互不重叠是结构必然而非设计巧合——*双投影/反方陈词体/九子带加权/单 paper 负空间* 在 sample 系列中各自占一个独立的方法学功能位，若四章中任一两章豁免理由可互换，则需重审本豁免登记是否构成隐性新槽位申请 (届时退回 12 天冻结协议 (i)(ii)(iii) 三阈值之一才允许沉淀)。引用 commit hash 经 `git log` 验证 (`6832876` taxonomy.md 五档梯度行 / `38dcdda` timeline.md 应用谱元-归纳闭合 / `1329795` N9 §八豁免段 / `0d247b0` N8 §八末第二 update / `1f96072` N3 §七 MiraBench 负空间 / `1566c2f` N6 §十首条 update / `f261f3e` world_model 链终点 / `ef1dfe2` §10 type 分层 / `29107ec` timeline 链终点收束)，无新 arxiv ID / 数字 / 作者 / 日期 fabricated；MiraBench 2605.29360 / KFW 2605.27734 / DCV 2605.28860 三 ID 沿用 paper_notes 已 verified 集合，本节不引入新 ID。

<!-- §十 close. daily↔deepen sample-side N6 first instance. 三颗子弹: YoCausal 2605.30346 (上界 proxy, §六 ii) / Chess-WM 2605.30100 (转 N2 §六 第四暗门候选, N6 不重复算) / MiraBench 2605.29360 (弱代理, §六 i, C-WM-7 第四 weak-proxy 通道). 严守 12 天冻结协议. 无 arxiv ID/数字/作者/日期 fabricated, IDs 沿用 paper_notes 已 verified 集合. 2026-05-31 update（二）N6 sample 侧 type-geometry 五档梯度豁免登记完成, 与 N8/N9/N3 三章豁免理由互不重叠构成 sample 侧 9=5+4 全章覆盖. -->

---
