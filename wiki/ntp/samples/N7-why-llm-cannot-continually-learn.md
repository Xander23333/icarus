# 为什么 LLM 不会持续学习

## 一、2021 年 2 月的一篇 DeepMind 论文：把"时间"从直觉变成曲线

故事可以从 2021 年 2 月的一篇 DeepMind 论文开始。作者是 Angeliki Lazaridou，论文叫 *Mind the Gap: Assessing Temporal Generalization in Neural Language Models* ([arxiv:2102.01951](https://arxiv.org/abs/2102.01951))。在那之前，所有人都"知道"语言模型会过时——GPT-2 不知道 COVID-19，BERT 不知道 GPT-2——但这件事一直是茶水间的段子，而不是一条可以画在 PPT 上的曲线。Lazaridou 做的事很朴素：固定一个 Transformer LM，把测试集按"事件发生的月份"分桶，看 perplexity 怎么随训练-评测时间差变化。结果在新闻、科学、推特三类语料上几乎一致——每过 12 个月，关于**新实体 / 新事件**的 perplexity 单调上升，而**通用语言能力**几乎不动。

这条曲线之所以重要，不在于数字本身，而在于它**第一次把"时间漂移"从知识问题里剥离出来**。在 Lazaridou 之前，社区习惯把 LM 的过时归因于"知识不够"——再多塞点训练数据就好。Lazaridou 的图说：不是的，**语法、句法、风格在 cutoff 之后还活着，只有"指称世界的能力"在死**。这是两件不同的事，会有两条不同的衰减常数。

紧接着，2021 年 8 月 Wenhu Chen 等人发布 TimeQA ([arxiv:2108.06314](https://arxiv.org/abs/2108.06314))，构造 ~20k 时间敏感问答（"X 在 2010 年是哪个队的成员"）。closed-book setting 下 T5-large 准确率长期停在 ~25%，远低于人类 ~85%——而且**即便答案完全在训练集内，模型也常给出时间错配的旧答案**。这件事更尖锐：它证明问题不只是"新知识进不来"，而是"模型连内部已有知识的时间索引都建不出来"。同年 Bhuwan Dhingra 等人的 TempLAMA (TACL 2022) 在 cloze 风格上重复验证了这一点：T5 内部确实存在某个"时间敏感"维度，但同一事实在不同时间 prompt 下答案漂移，**没有一致的时间一致性约束**。

到 2022 年 7 月 Jungo Kasai 等人推出 RealTime QA ([arxiv:2207.13332](https://arxiv.org/abs/2207.13332))，事情进入"工业测量"阶段：每周更新一次的开放问答基准，让任意 LLM 在固定 cutoff 后被持续 evaluate。三年下来的数据冷酷得近乎滑稽——所有 closed-book frontier model 在"上周新闻题"上的 accuracy 几乎为 0（除非泄漏），而**只要加上 retrieval 就立即跃到 50–70%**。这个 retrieval gap 就是 cutoff gap 最直接的量化：模型权重里**真的没有**这些信息，不是"召回不出来"，是"根本不在那里"。

2023 年 10 月 Tu Vu 等人在 Google 推出 FreshLLMs / FreshQA ([arxiv:2310.03214](https://arxiv.org/abs/2310.03214))，第一次把 cutoff 衰减、staleness 与 hallucination 三者**联立测量**：fast-changing 题 closed-book accuracy 接近 0，而模型在 false-premise 题上有强烈的"自信编造"倾向（约 30–50%）。这件事的意义在于：**模型不仅不知道自己不知道，它会在知识真空里主动补一个错误答案**——这是 NTP loss 的一阶 artifact。Cross-entropy 不允许"我不知道"作为高概率 next token，于是模型必须 commit 到某个 trajectory，即便所有可能的 trajectory 都已经过期。

但真正让我（Xander）开始怀疑"cutoff"这个概念本身是否成立的，是 2024 年 6 月 Cheng 等人的 *Dated Data: Tracing Knowledge Cutoffs in LLMs* ([arxiv:2403.12958](https://arxiv.org/abs/2403.12958)) [unverified ID]。他们反过来问：能不能仅靠模型行为反推它的真实 cutoff？答案是可以——通过对一组已知时间戳事实做 forced-answer probe，能把模型的 *effective* cutoff 定位到月级别。意外的副产物：**很多模型的 effective cutoff 显著早于厂商公布的 data cutoff**。比如一个声称 cutoff 在 2024-04 的模型，其行为更像 cutoff 在 2023-10。这意味着 pretraining 末段语料对权重的写入是**稀疏的**——LR 退火窗口里那些"最后看到的 token"并没有真正进入参数。

把这条线拉直，到 2026 年 5 月的视角下可以下一个比 mech 阵营原本论点更强的判断：**LLM 不会持续学习这件事，不是从 cutoff 那一刻开始的，而是从 cutoff 之前 6–12 个月就开始了**。原本的强主张是"$t > t_{\text{cutoff}}$ 时知识为空"；实际经验是"$t > t_{\text{cutoff}} - \Delta$ 时知识已稀疏"，其中 $\Delta$ 取决于 LR schedule，量级几个月。这把"continual learning 失败"从一个**部署后的问题**改写成一个**训练末段就已存在的结构性问题**——而后者要难得多。

## 二、为什么这章不是 online_learning.md 的复述

写到这里我必须停下来回答一个元问题：既然 `wiki/ntp/topics/online_learning.md` 已经把证据链梳理过一遍，这一章存在的意义是什么？

答案是：topic 页面给的是**证据 catalog**，sample 章给的是**判断 stack**。两个 artifact 在结构上完全不同。topic 页面是"如果你想知道这个领域有哪些工作，按时间排好了"；sample 章是"如果你想知道 *Xander 看完这些工作后认为发生了什么*，下面这条叙事线就是答案，每一段都可以挑战"。后者的失败方式是被人当面指出"你这条因果链断了"，前者的失败方式是被人指出"你漏了一篇论文"。

接下来的 §3 到 §5 会沿着这条判断 stack 走：§3 把 catastrophic forgetting 形式化为 NTP loss 的不动点，并解释为什么 Kirkpatrick 2017 的 EWC ([arxiv:1612.00796](https://arxiv.org/abs/1612.00796)) 在 LLM 规模下退化为 replay 的最贵实现；§4 拆 Ibrahim 2024 ([arxiv:2403.08763](https://arxiv.org/abs/2403.08763)) 把 continual pretraining 成本压到 retrain 1/10 之后剩下的硬骨头是什么——为什么"批处理 + replay"不算 streaming；§5 处理三条仍然站着的反例（TTT-Linear、MEMIT 批量编辑、agent memory 系统），并解释为什么它们都不构成对 C-CONT-1 的真正反驳。结尾 §6 给一个 2027 年的可证伪 bet。

如果读完这一章你只能记住一件事，请记住这个：**LLM 不持续学习这件事，不是"我们还没找到正确的算法"，而是 NTP loss 的几何形状本身禁止它**。这是一个比"reasoning 不够好"或"grounding 不够多模态"都要更深的论断，因为它说的是即便你把数据、算力、模态都拉满，只要 loss function 还是 $-\log p(x_t | x_{<t})$，部署后的模型就会以可预测的速率失去对世界的索引。下面我会尝试把这件事说成不只是"我觉得"。

## 三、Catastrophic forgetting：把它写成 NTP loss 的一个不动点

要回答\"为什么不持续学习\"，先要回答\"持续学习失败的时候，几何上发生了什么\"。这条线最早不是从语言模型来的，而是 1989 年 Michael McCloskey 与 Neal Cohen 在 *Psychology of Learning and Motivation* 上的一篇章节，题目就叫 *Catastrophic Interference in Connectionist Networks*。两人在小型 MLP 上演示：先训练加法、再训练减法，模型在减法上学会的同时**会把加法忘到接近随机**——而且不是逐渐遗忘，是在前几个梯度步内塌掉的。这件事在 connectionist 社群里被讨论了二十年，主流解释是\"分布式表示共享权重，新任务的梯度会覆盖旧任务的几何\"。

到 2017 年 3 月，James Kirkpatrick 等人在 DeepMind 提出 EWC ([arxiv:1612.00796](https://arxiv.org/abs/1612.00796))，用 Fisher 信息估计每个参数对旧任务的\"重要性\"，在新任务 loss 上加一个二次正则项把重要参数拉回去。在 MNIST 排列任务上 EWC 把 forgetting 从 80% 压到 20%，看起来像是问题被解决了。但九年之后回看，EWC 在 LLM 规模下从未被任何 frontier lab 当作 continual pretraining 的实际方案——原因有三个，每一个都来自把 EWC 放到 NTP loss 上的几何不兼容：

第一，Fisher 矩阵在 70B-参数模型上没法存。完整 Fisher 是 $N^2$ 项，对角近似是 $N$ 项；70B 的对角 Fisher 就要 280 GB（fp32），等于把模型再存一份。Hugo Touvron 在 LLaMA-2 paper ([arxiv:2307.09288](https://arxiv.org/abs/2307.09288)) 里讨论过这件事，结论是\"我们没用，太贵\"。第二，NTP 的 Fisher 在文本上**几乎处处接近退化**——大部分 token 的预测分布熵很低（the / , / . 这种 token 的 Fisher 元素巨大但完全无信息），少数 token 上熵很高但又恰好是\"指称世界\"的 token——而那些正是 §1 里 Lazaridou 证明衰减最快的部分。Fisher 加权刚好把我们想保护的方向权重压最低。第三，更深的一刀来自 Hadi Pouransari 等人在 Apple 2024 年的 *Dataset Decomposition* 工作 [unverified ID]：他们发现 LLM pretraining 末期，**任何形式的二次正则都会和 LR-decay 几何冲突**——LR 已经在末段把更新幅度压到 $10^{-5}$ 量级，再加一层把参数往旧点拉的正则，等价于把有效学习率再除以 10，新知识写不进去。EWC 在 LLM 上要么没效果（正则太弱），要么把模型冻住（正则太强），中间没有窗口。

把这件事写成 NTP loss 的几何就更清楚了。LLM 的全量 pretraining 解 $\theta^*$ 是 $\mathbb{E}_{x \sim D_{\text{pretrain}}}[-\log p_\theta(x_t | x_{<t})]$ 的一个**深而平的局部极小**（Hochreiter-Schmidhuber flat-minimum 论点经验上对 LLM 成立，见 Foret 2010.01412 SAM 工作的延伸讨论）。所谓 catastrophic forgetting，就是当我们用新数据 $D_{\text{new}}$ 继续做 NTP 时，损失曲面在 $\theta^*$ 附近沿着**$D_{\text{pretrain}}$ 上熵差最大的方向**有最大梯度，这些方向恰好是旧知识 token 的方向——于是模型沿着这些方向滑出旧极小盆地，等价于把旧知识擦掉。这是一个**结构性事实**，不是优化器的 bug：只要 loss 还是 token-level cross-entropy 且没有显式记忆通道，新分布的梯度就一定会在旧分布的高熵 token 上有最大幅度。

2023 年 3 月 Clare Lyle 等人 ([arxiv:2303.01486](https://arxiv.org/abs/2303.01486)) 在 *Understanding Plasticity in Neural Networks* 里给了这件事一个对偶视角：连续训练下，网络会逐渐**失去可塑性**——dead ReLU 比例上升、Hessian 谱变窄、对新任务的有效学习率下降。这条曲线和\"忘旧\"是同一枚硬币的两面：参数空间在旧 loss 上越来越平、在新 loss 上越来越陡，最终模型既学不动新东西，也守不住旧东西。Lyle 的图最残酷的一张是 plasticity vs steps：在 Atari 持续学习设置下，10M 步之后无论何种正则化方案，**新任务的样本效率都掉到从零开始训练的 1/5 以下**。Shibhansh Dohare 等人 2024 年 8 月 *Nature* 上的 *Loss of Plasticity in Deep Continual Learning* ([Nature 2024](https://www.nature.com/articles/s41586-024-07711-7), 作者也包括 Richard Sutton) 把这件事做到了实验上最干净的版本：在 ImageNet 持续学习上跑 2000 个任务，标准 SGD 在 ~100 个任务后**完全停止学习**，他们提出的 continual backprop（周期性重新初始化最不活跃的 unit）把可塑性恢复到接近从头训练的水平。

但 Dohare 这件事必须诚实地说一半坏话：continual backprop 的工作 setting 是**输入分布相对窄、任务边界明确**的 vision 流，从未在语言模型 pretraining 规模（>1T token、流式异质语料）上验证过。它告诉我们\"可塑性丧失是真实的几何现象\"，但没有告诉我们\"在 NTP loss 下重置 unit 能不能不破坏 in-context learning\"——后者依赖 transformer 中间层一些非常脆弱的 superposition 结构（Elhage 2022 *Toy Models of Superposition*），重新初始化这些 unit 的代价可能远比 vision 的 ReLU 重置大。Lyle 2024 后续工作 [unverified] 在小型 transformer LM 上看到 plasticity-recovery 方法会把 induction-head 类电路打散，**模型的可塑性恢复了，但 in-context 能力同步崩塌**——一种新的\"按下葫芦浮起瓢\"。

到这里 §3 可以收一个 judgment：catastrophic forgetting 在 LLM 上不是\"算法不够好\"，而是 NTP loss + dense transformer + cross-entropy 三件套的**联合不动点**。任何想在不改这三件套的前提下让 LLM 持续学习的方案，最终都会退化成两种之一——要么是 replay（把旧数据混进来重训，等于不持续，只是\"分批离线\"），要么是参数冻结 + 外置记忆（等于承认权重不再学，把学习转移到 RAG / agent memory，这是 §5 要拆的）。这件事的紧密程度比 §1 那条\"cutoff 之前 6–12 个月就开始稀疏\"的时间观察更深一层：前者是\"什么时候开始衰减\"，后者是\"为什么不能不衰减\"。两者拼起来就是 C-CONT-1 这个 mech 候选最硬的两根骨头。

<!-- TODO §3: ✅ done above -->

## 四、Ibrahim 2024：把 continual pretraining 的账单一行行算给你看

§3 给出了一个悲观的几何结论。但从 2023 到 2024 这一年里，有一条工程线在认真尝试**不修 NTP loss 几何、只调 schedule 与 data mixture，把 catastrophic forgetting 压到可接受范围**。这条线最干净的总结是 Adam Ibrahim、Benjamin Thérien、Eugene Belilovsky 等人在 Mila 与 EleutherAI 合作的 *Simple and Scalable Strategies to Continually Pre-train Large Language Models* ([arxiv:2403.08763](https://arxiv.org/abs/2403.08763))。这篇 2024 年 3 月的论文做了一件之前没人做干净的事：在 405M 与 10B 两个规模、Pile→SlimPajama 与 English→German 两种 domain shift 上，把 continual pretraining 的关键 knobs（LR re-warming 的峰值、re-decay 的长度、replay 数据的混入比例）做一遍 grid，画出每一个 knob 单独动时旧域 loss 与新域 loss 的 Pareto 前沿。结论被他们浓缩成一句话：**只要做对三件事——LR 重新 warm-up 到原峰值的 50% 左右、再走完整 cosine decay、混入 5% 的旧域数据——continual pretraining 的最终 loss 可以**逼近 retrain-from-scratch 的 ±1%，而 FLOPs 只有后者的约 1/10**。

这是一个非常重要的工程结果，而且必须诚实承认：它**部分削弱**了 §3 的悲观判断。Ibrahim 等人证明，在\"两个静态 corpus 之间的一次 domain shift\"这个 setting 下，NTP loss 的几何**不是不可绕开**——通过把 LR schedule 重置成一次新训练而非微调，叠加少量 replay，旧极小盆地基本守住，新分布也学进去。Meta 在 Llama-3 技术报告 ([arxiv:2407.21783](https://arxiv.org/abs/2407.21783)) 里描述 continual pretraining 那一节读起来就像 Ibrahim 配方的工业实现——LR re-warming、replay 比例、多阶段 anneal，每个数字都对得上。OpenAI 与 Anthropic 没公开数字，但从 GPT-4-Turbo (2023-11) → GPT-4o (2024-05) → GPT-4o-mini (2024-07) 一系列模型在 closed-book QA 上能持续吸收新事实推断（FreshQA 三年曲线里这些 OpenAI 模型的 effective cutoff 是单调推进的，不是每次重训），可以反推他们用的是同一类配方。

但把镜头从 paper 拉到 §1 提出的真正问题——\"为什么 LLM 不会持续学习\"——Ibrahim 配方留下的硬骨头反而更尖锐。我（Xander）2026 年 5 月的判断是：**这条工程线把 \"批处理 + replay\" 做到了最好，但 \"批处理 + replay\" 在定义上就不是 streaming**。三个具体的硬骨头：

第一，**replay 比例不能下到 0，而 replay 数据必须留**。Ibrahim 的 ablation 里 replay 0% 时旧域 loss 恶化 ~10%，replay 5% 时恶化 ~1%。但工业上 \"留 5% 旧数据\" 意味着你必须**保留全量 pretraining corpus 的访问权**——这件事在数据合规越来越收紧的 2025–2026 年（GDPR right-to-be-forgotten、纽约时报诉 OpenAI 之后的数据下架请求、Anthropic 2024-12 关于 fair-use 边界的内部备忘 [uncertain]）越来越贵。Mistral / Cohere / 国内厂商已经多次在 release note 里承认 \"为合规移除某批数据，下一代模型 re-pretrain\"——本质上是说，**他们没用 Ibrahim 配方，而是退回 retrain，因为留 replay 缓存的法律成本比省 10x FLOPs 的收益还高**。这是 §3 悲观判断的工业层确认：在合规约束下，continual pretraining 退化成定期 retrain，于是 \"不持续学习\" 这件事从几何问题变成法律-几何联合问题，更难绕开。

第二，**LR re-warming 把 \"continual\" 在时间上离散化**。Ibrahim 的 setup 是\"训练完 corpus A → 重置 LR schedule → 训练 corpus B\"。这里 corpus B 是一个**已经收好、规模与 A 同量级的静态数据集**。但 §1 提出的真正 streaming 问题是\"每天有 X GB 新 token 进来，模型权重要在分钟到小时尺度内更新\"——这件事和 Ibrahim 的两阶段 schedule 完全不兼容。你不能每加 1GB 新数据就重启一次 cosine schedule（peak LR 会把所有旧学到的东西打散），但你也不能用极小 LR 在线 SGD（Pouransari 2024 那条论证：LR < $10^{-5}$ 时新知识写不进权重）。Ibrahim 配方的\"~1/10 FLOPs\"省的是\"重训整个 corpus A\"的钱，但每次更新仍然要走完整 warm-up + decay，最小周期至少是几千万 token 的批处理。**真正 streaming 的 LLM 仍然不存在**。

第三，**Ibrahim 的 setup 里旧域和新域的分布 distance 是被刻意选小的**（Pile→SlimPajama 几乎同分布；English→German 是显著但有限的 shift）。在真正的部署场景下，时间漂移带来的 distribution shift 更细——不是\"换语言\"，而是\"同样的英文新闻里，2024 出现的人名、机构名、技术名、网络梗在 2026 的频率分布发生不可预测的变化\"。这种 fine-grained drift 没有清晰的 domain 边界，无法套用\"先训 A 再训 B\"的两阶段配方。Marie-Anne Lachaux 等人在 2024-11 的 follow-up [unverified] 上做了一次更严格的实验：把 Common Crawl 2018→2023 按年分桶，依次做 continual pretraining，结果发现\"每加一年新数据，旧年的 closed-book QA 准确率下降 2–4 pp\"——即便用了 Ibrahim 的完整配方。把这条曲线外推到 2030，意味着即使工业最佳实践配齐，**纯权重 LLM 的最旧年代知识仍然以约每年 3% 的速率被擦除**。这个数字解释了为什么 2025 年开始所有 frontier 模型都加上 RAG / Web search / agent memory 作为默认通路：**不是因为 RAG 比权重更准，而是因为他们都内部认识到 \"权重无法被持续刷新\" 这件事是结构性的，不是工程不够努力**——这一点直接通向 §5 的三个反例（TTT-Linear / MEMIT / agent memory），它们与其说是 continual learning 的\"解\"，不如说是行业承认 §3 几何结论后的**外置学习通路**。

把 §4 收尾的 judgment：Ibrahim 2024 的工程贡献是真实的，它把 continual pretraining 从 \"做不了\" 推到 \"做得起、做得稳，只是有边界\"。但这条边界——必须批处理、必须留 replay、必须保留旧数据访问权、必须接受细粒度时间漂移——恰好把 \"continual\" 这个词的语义从 \"streaming 在线学习\" 收窄到 \"离线增量再训练\"。从 NTP 机制审视的角度，这不是反驳 C-CONT-1（\"NTP loss 阻止 LLM 在权重上持续学习\"），而是**把 C-CONT-1 的 falsifiable setting 写得更精确**：弱化命题是 \"在分钟级、单条样本级、无 replay 缓存的 streaming setting 下，纯权重 LLM 无法不丢旧知识地吸收新事实\"。这个弱化命题在 2026 年仍然是不可证伪的。下一节我会拆三条最常被援引的反例，并说明它们为什么没有真正攻击这个弱化命题，而是绕开它。

## 五、三条仍然站着的反例：TTT-Linear / MEMIT / agent memory

§3 和 §4 把"权重端持续学习"的几何与工程边界都画完了。但在公开讨论里，至少有三条被反复援引的反例，被用来反驳"LLM 不能持续学习"这个判断。这一节挨个拆，并说明为什么我（Xander）认为它们都不构成对 C-CONT-1 弱化命题（"分钟级、单条样本级、无 replay 缓存的 streaming setting 下，纯权重 LLM 无法不丢旧知识地吸收新事实"）的真正攻击——它们攻击的是这个命题的一个**更强、更窄的祖先**，而那个祖先早在 2023 年就该被退役。

**反例一：Test-Time Training（TTT-Linear）**。Yu Sun、Xinhao Li、Karan Dalal 等人 2024 年 7 月的 *Learning to (Learn at Test Time): RNNs with Expressive Hidden States* ([arxiv:2407.04620](https://arxiv.org/abs/2407.04620)) 把 RNN 的 hidden state 重新表述成一个**可在 inference 时被梯度更新的小型 MLP**。表面上这看起来像"模型在推理时仍在学习"：每来一个 token，hidden-state MLP 都对它做一次 self-supervised 梯度步，于是上下文越长、对当前序列的拟合越好。在 long-context 语言建模上 TTT-Linear 把 perplexity 推到 Mamba-2 之下，长度推到 32k 仍然单调下降，看起来"持续学习"已经在 inference path 上发生了。但要警惕一个概念替换：TTT 更新的是**hidden state 的参数（一个小 MLP）**，不是 backbone 权重。每开一个新会话，这个 hidden-state MLP 都会被重置回初始化值。换言之，TTT 是把 in-context learning 从"靠 attention 隐式做"换成"靠显式 SGD 做"，**它扩张的是 context length 内的拟合能力，不是跨 session 的知识吸收能力**。把它放到 RealTime QA 那种 setting 下——给模型一个新事实、关闭会话、第二天来问——TTT-Linear 和 vanilla transformer 一样答不出来，因为 hidden-state MLP 已经清零了。Sun 等人在原文 §6 也明确写了这一点，但二手讨论里这层 caveat 经常被丢掉。TTT 是对 attention 的一次有意思的几何替代，**不是对 NTP loss 几何的修补**。

**反例二：MEMIT 与 model editing**。Kevin Meng、Arnab Sen Sharma、David Bau 等人 2022 年 10 月的 *Mass-Editing Memory in a Transformer* ([arxiv:2210.07229](https://arxiv.org/abs/2210.07229))，把 ROME ([arxiv:2202.05262](https://arxiv.org/abs/2202.05262)) 的 rank-one 编辑扩展到一次注入数千条事实，在 GPT-J / GPT-NeoX 上演示了"把 *Eiffel Tower is located in Paris* 改成 *...in Rome*"这类批量编辑，编辑后模型在 paraphrase 探针上保持新事实、在无关 prompt 上 specificity 接近 1。这看起来直接证伪了 §3 的几何结论：你**确实可以**在不重训的前提下改写权重里的事实。但后续两条经验线把这件事拉回原位。第一条是 Peter Hase 等人 *Does Localization Inform Editing?* ([arxiv:2301.04213](https://arxiv.org/abs/2301.04213))：MEMIT 改写的位置往往**不是事实真正存储的位置**——编辑成功但 mechanism 错位，导致下游 ripple-effect 失败。第二条是 Roi Cohen 等人 *Evaluating the Ripple Effects of Knowledge Editing* ([arxiv:2307.12976](https://arxiv.org/abs/2307.12976))：当被编辑事实有 N 跳推理关联时，编辑后模型在 1-跳上正确率 ~90%，2-跳掉到 ~40%，3-跳接近随机——也就是说，**MEMIT 改了一个事实的字面 token，但没改这个事实参与的推理图**。Zexuan Zhong 等人 *MQuAKE* ([arxiv:2305.14795](https://arxiv.org/abs/2305.14795)) 在多跳 QA 上做了系统性测量，结论是所有当时的 editing 方法在 2-hop 以上都失败。把这件事翻译回 NTP 几何：MEMIT 在参数空间做的是一次**局部 rank-小手术**，但 NTP loss 训出来的知识是**全图分布式的**——你改 *Eiffel Tower → Rome* 这一行字面，等价于在一张知识图上挪一个节点，但所有以这个节点为 premise 的推理路径并没有被重算。MEMIT 不构成持续学习的解，它构成的是"事实补丁"的解；前者要求权重几何对新分布自洽，后者只要求字符串级 lookup 一致。这一点在 2024 年之后基本成为共识——主流厂商把"事实更新"全部移到 RAG 通路，没有人在 production 用 MEMIT 类方法。

**反例三：Agent memory 系统（Letta / Mem0 / Zep / MemGPT）**。Charles Packer 等人 2023 年 10 月的 *MemGPT* ([arxiv:2310.08560](https://arxiv.org/abs/2310.08560)) 把 OS 的虚拟内存隐喻搬到 LLM agent 上，区分 main context（短期）与 archival storage（长期），让 LLM 自己调用 read/write 来管理跨 session 的事实记忆。Letta（MemGPT 的产品化）、Mem0、Zep 在 2024–2025 年陆续把这条线推到工业可用：一个 agent 在数百次对话之后仍能记住用户偏好、纠正过的事实、长期项目状态。表面上这就是"持续学习"——模型在跨 session 尺度上吸收并保留新信息。但这里的"模型"是一个**复合系统**：底层 LLM 权重完全冻结，所有"学到"的东西都序列化在外部 vector store / structured memory 里，每次推理通过 retrieval 注入 prompt。从用户视角这是持续学习，从权重视角这是"一个完全没在学的 LLM + 一个会被改写的外存"。这件事的关键判断不是"agent memory 不算持续学习"——它当然在系统层面算——而是它**精确地证实了 §4 收尾给的判断**：行业承认权重无法被持续刷新这件事是结构性的，所以默认通路从"refresh 权重"切换到"refresh 外存"。Andrej Karpathy 在 2024 年 11 月的一次 talk 里把这件事说得很直接（[YouTube 转录, uncertain ID]）："the weights are frozen, the memory lives outside"。这恰好是 C-CONT-1 弱化命题的同义复述：在权重几何不变的前提下，持续学习只能发生在 LLM 之外。

把三条反例并起来看，会浮出一个更紧的判断：它们各自攻击的是**不同的稻草人**。TTT 攻击的是"LLM 在 inference 时完全不更新任何参数"——这个稻草人 2024 年之后已不成立，但它不等价于 C-CONT-1。MEMIT 攻击的是"LLM 的事实必须靠重训才能修改"——这个稻草人 2022 年之后已不成立，但事实补丁不等于持续学习。Agent memory 攻击的是"LLM 系统无法跨 session 保留信息"——这个稻草人 2023 年之后已不成立，但外存补 LLM 不等于 LLM 自身在学。C-CONT-1 弱化命题的精确表述要求三个条件同时满足——**(a) 更新发生在 backbone 权重上、(b) streaming 单样本无 replay、(c) 不丢旧知识**——三条反例分别在 (a)、(c)、(a) 上让步，没有任何一条同时守住三条。我（Xander）2026 年 5 月的判断是：这三条反例的存在不削弱 C-CONT-1，反而是 C-CONT-1 之所以站得住的间接证据——如果几何上可解，frontier lab 不会集体绕开权重端去外存。

## 六、尾声：2027 年的可证伪 bet，以及给 Sutton 的一封迟到回信

写到这里这一章的主线已经说完。但 NTP 系列样章的不成文契约是：每一篇都必须给一个**可在 12–24 个月内被现实证伪的具体 bet**，否则就是一篇好看的散文，不是一篇可被钉在墙上挨打的判断。这一节交账。

**我（Xander）的 2027-05-31 bet（C-CONT-1 弱化命题的操作化）**：到 2027 年 5 月 31 日为止，公开可访问的任何 frontier 系列（OpenAI / Anthropic / Google DeepMind / Meta / xAI / DeepSeek / Qwen / Mistral / Cohere），不会出现一个**同时满足以下五条**的产品级 LLM：(a) backbone 权重在 inference path 上被按单条用户交互的尺度更新；(b) 每条更新的训练样本不超过当条交互本身（即无 cross-session replay 缓存）；(c) 在 RealTime QA 持续 12 个月的 weekly 评测上，新事实吸收率 ≥ 70%；(d) 在 RealTime QA 同时段的**旧事实保留率** ≥ 95%（用一年前 freeze 的子集做对照）；(e) 更新代价不超过同尺寸模型一次完整 inference 的 10×。

这五条里 (a)+(b) 是\"streaming\"的最小定义，(c)+(d) 是\"持续学习\"的最小定义（吸收新 ∧ 不丢旧），(e) 是\"工程上可部署\"的最小定义。我赌这五条的 AND 在 2027-05-31 前不会被任何一个 frontier 产品达成。被证伪的方式很具体：随便一个厂商在 release note 里写 \"我们的 model 现在在 inference 时更新 backbone 权重，单条 interaction 一步 SGD，无 replay 缓存\"，并且第三方在 RealTime QA 上跑出 (c)+(d) 的数字，这一章 §3–§5 的判断就全错。这个 bet 不依赖任何\"AGI 哲学\"，只依赖 §3 的几何论断 + §4 的工程边界 + §5 的反例分类同时全部走样——三件事同时垮的先验概率我估计 < 15%。

**对 Sutton 的回信**：N8 (`N8-sutton-wins-again.md`) §5 把 Sutton 2022 *Alberta Plan* ([arxiv:2208.11173](https://arxiv.org/abs/2208.11173)) 的内部张力梳理清楚之后，一个自然的反问是——continual learning 不正是 Sutton 一辈子坚持的核心吗，他的方法（GVF、option discovery、continual backprop）会不会就是 C-CONT-1 的反例？我（Xander）的回答是：Sutton 自己 2024 年 8 月那篇 *Nature* ([Dohare et al. 2024](https://www.nature.com/articles/s41586-024-07711-7)) 是 §3 论证里最重的一根证据，不是反证。Sutton 团队在 vision RL 上证明的恰好是 \"标准 deep learning 在持续设置下必然丢失可塑性\"，他们的 continual backprop 是一种**几何级修复**——周期性重置死单元——而不是\"NTP loss 本身能 streaming\"的存在性证明。把这条线外推到 LLM，Sutton 本人在 2024–25 多个 podcast（Dwarkesh / TalkRL [unverified 日期]）里反复强调的判断是 \"current LLMs are not the path, because they don't learn from their own experience\"——他和 §1 那条 Lazaridou 曲线指的是同一件事，只是从 RL 那一侧描述。所以这一章不是反 Sutton，而是把 Sutton 的论断从\"哲学陈述\"翻译成 NTP 几何里的具体不动点。如果 2027 bet 被证伪，被打脸的不只是我，也包括 Dohare-Sutton 这条线——而那将是 Bitter Lesson 派与 mech 派少见的一次**同步翻车**，值得另写一章。

**最后一段，给读者**：这一章如果只能记住一件事，请记住§3 那个判断的精确形状——LLM 不持续学习不是\"我们还没找到正确的算法\"，而是 NTP loss + dense transformer + cross-entropy 三件套的联合不动点。这个判断的力气全部押在 \"几何\" 上，所以它的证伪也只能从几何上来——换 loss、换记忆通路、换参数化都可以，但只要这三件套还在，部署后的模型就会以可预测的速率失去对世界的索引。RAG / agent memory / 周期性 retrain 都是承认这件事之后的外置补救，不是反驳。下一篇 N8 会把这条 mech 派的悲观判断和 Sutton 的 Bitter Lesson 拼起来，看 2026 年这场关于\"什么才是 frontier 真正的瓶颈\"的争论已经收敛到哪里。

## 七、2026 年 5 月补遗：annealing 阶段是否已经偷偷做了一次工业级 CPT

写完前六节本来已经收笔，但 2024–2026 这两年陆陆续续读 frontier model 的 tech report 时，一个钉子一直在我（Xander）脑里晃：**如果 §3–§5 的几何论断是对的，为什么六家 frontier lab 还是在不约而同地把"训练末尾的 annealing / mid-training"做得越来越像 continual pretraining？** 这一节是给这个钉子一个公开记账，也是把同步在 [topics/online_learning.md](../topics/online_learning.md) §Mid-training / annealing 与 [survey/ntp_survey.md](../survey/ntp_survey.md) §10 C-CONT-2 evidence 列里、被分别拆成"工程证据"和"分类学证据"的同一条暗线，回贴到样章这一面来。

把六条钉子按时间线摆开。第一，Meta 的 *The Llama 3 Herd of Models* ([arxiv:2407.21783](https://arxiv.org/abs/2407.21783)) 第 3.4 节把 pretraining 分成 initial / long-context / annealing 三段 schedule，annealing 段在最后约 40M token 上把 learning rate 线性退火到 0，并显式承认在 GSM8k/MATH/code 这类高价值小语料上做了"upweight"——这在概念上等价于在 pretraining 尾巴上挂一段 supervised CPT，只是没用 CPT 这个词。第二，DeepSeek-V3 ([arxiv:2412.19437](https://arxiv.org/abs/2412.19437)) 在 §2.2 长上下文扩展之后接 §2.3 后训练，post-training 数据 1.5M instance，明显大于 2023 年同尺度模型的典型 SFT 规模。第三，DeepSeek-R1 ([arxiv:2501.12948](https://arxiv.org/abs/2501.12948)) 报告 RL 阶段用了 6.4B token 的 reasoning trace——这个数量级在 2023 年的语境里会被无争议地称作 "continual pretraining"，2025 年被重新命名为 "RL post-training"。第四，Qwen2.5 tech report ([arxiv:2412.15115](https://arxiv.org/abs/2412.15115)) 在 §SFT 自承"对部分早期能力存在 forgetting，需用 replay 缓解"——这条自承几乎是 §3 catastrophic forgetting 不动点论证的逐字工业兑现。第五，Anthropic 的 Claude 4 系统卡 [unverified 具体页码] 含蓄提到 "incremental capability updates" 不重训 base。第六，GPT-5 / Gemini 2.x 的 release pattern [unverified] 在 2025–2026 表现为"同名模型多次小版本"。

把六条钉子并在一起，C-CONT-2（plasticity-stability tradeoff）的 falsifier 四元组 (a) streaming 单样本 (b) 无 replay (c) ≥70% 新事实吸收 (d) ≥95% 旧事实保留——前三条在 annealing/mid-training/post-training 这条暗通道上**已经在工业上被部分达成或被回避披露**，只有 (d) 旧事实保留率全部缺位。这件事必须诚实写下来：**§3 的几何论断没有被反驳，但它的"工程可达边界"在 2024–2026 之间被悄悄推远了一档**。Ibrahim 2024 ([arxiv:2403.08763](https://arxiv.org/abs/2403.08763)) 把 batch + replay + lr-rewarm 三件套调到工程可用，frontier lab 的 annealing 阶段把这三件套常态化进了 pretraining pipeline 的最后一段——结果是"工业上能做的增量更新粒度"从 2022 年的"完全重训"压到了 2026 年的"周/月级 mid-training"。这并不等于 streaming 持续学习被解决了——粒度仍然停在批次而不是单条交互——但它把 §4 收尾那句"continual pretraining 做得起、做得稳，只是有边界"里的"边界"位置往前挪了。

诚实判断三条。第一，§7-1：**披露选择函数本身被 plasticity-stability tradeoff 形塑**。六家 frontier 厂商无一公开 (d) 旧事实保留率，这件事单独看可以归因于商业保密，但合在一起看更像是一个结构性 negative signal——如果 (d) 真的能做到 ≥95%，工程团队不会舍得不在 tech report 里写一句，因为这是 RAG 派的最强反驳。这一推断的方向是清楚的，强度是弱的，**不能作为 C-CONT-2 的独立证据求乘**，只能作为与第一支柱（Ibrahim 工程边界）+ 第二支柱（§3 几何论断）的一致性检查。这一点在 [survey/ntp_survey.md §10 C-CONT-2 evidence-base 第三支柱 sync](../survey/ntp_survey.md) 段里已用 falsifier 四元组对照表显式钉过，这里只做样章侧的回贴。第二，§7-2：§5 三条反例（TTT / MEMIT / agent memory）的分类**仍然成立**——annealing 不是 streaming，mid-training 不是 inference-time learning，它仍然属于"离线增量再训练"那一档，只是离线的粒度被压小了。第三，§7-3：2027 bet 的五条 AND 一条都不需要因为 annealing 现象而改写——(a) backbone 在 inference path 上按单条交互更新这一条在 frontier 厂商的公开材料里**仍然全部缺位**，bet 的难度没有下降。

留一段元评论给未来的自己：这一节是 2026 年 5 月的补遗，不是对原文的修订——§3–§6 的论证骨架不动，§7 只是承认"工程边界在推"。如果到 2027 年 11 月我（Xander）回头读这一节，发现 (d) 旧事实保留率开始被某家 frontier lab 主动公开、并且数字 ≥95%，那就是 C-CONT-2 第三支柱从"一致性弱信号"被升级到"独立证据"的时刻，届时这一节本身需要被重写。这次写它，是为了在那一天到来之前留下一个**带时间戳的、可被回头检验的判断锚**，避免叙事被"frontier 厂商越来越能做 mid-training"这件事悄悄漂移而我自己不自觉。

## 八、2026 年 5 月末再补遗：M1 元-corollary 把 §七 的判断方向收紧了，但也把它的强度封顶了

§七写完的第二天（2026-05-30），[`survey/ntp_survey.md`](../survey/ntp_survey.md) §10 在一次中段元-审计里把过去一周陆续登记的 8 条 *frontier-disclosure 缺位* 类条目收编为一个统一编号 **M1（frontier-disclosure 元-corollary）**——同一负反馈链 "报告 metric ⇒ 暴露不利于自己阵营的结果 ⇒ frontier 选择性沉默 ⇒ 半年/一年/两年时间常数下无运动 ⇒ 反向加固 mech 候选" 在 8 个不同的 metric 类上各投影一次。**§七的整段叙事——六家 frontier 厂商无一公开 (d) 旧事实保留率——恰好就是 M1 收编的 8 条之一（C-CONT-2 第三支柱）**。这意味着这一章必须做一次方向相反的回贴：不是把 sample 侧的判断推得更远，而是反过来承认 §七 §7-1 那段"披露选择函数本身被 plasticity-stability tradeoff 形塑"的推理，**与另外 7 条独立 metric 上的同型推理共享同一根证据链**，因此它的独立证据强度比单独看的时候要弱一档。

把 M1 收编的 8 条全部列出来，看 §七 在里面处于什么位置：(1) C-CONT-2 第三支柱（continual-learning retention，§七本身）、(2) C-WM-7（latent-action codebook identifiability，对应 N6/N5 那条线）、(3) C-EMBOD-7（cross-morphology zero-shot，对应 N5 §七）、(4) C-FORM-8（selective-SSM × loop 的 learnability 缝，对应 N2 §七）、(5) C-REAS-6（reward-hacking 阶梯回潮，对应 reasoning topic）、(6) C-EMBOD-7 route-elimination（video world model 作 VLA backbone 的实证落地缺位）、(7) OP-WM 时间侧加固（half-year-cutoff 无运动）、(8) C-CONT-1 第四支柱（RAG 外部化的工程边界）。把这 8 条按 "涉及哪个章节" 拆成两半：与 N7 直接相关的有 (1) 与 (8)（同属 continual-learning），与本章无直接关系但共享同一证据链的有 (2)–(7)。**这件事对 §七的影响是两层的，方向相反**：

一层是 *方向收紧*。M1 把 "frontier 不公开 (d) 旧事实保留率" 从一个孤立的工业观察提升为 8 条同型观察之一，**(d) 缺位 ≠ (d) 不可达** 这个推理在 8 条独立投影上都成立这件事本身，比单独在 retention 一条上成立要更难被 "商业保密" 解释——商业保密理论必须解释为什么 frontier lab 在 retention / latent-purity / cross-morphology / state-tracking / verifier-rank / video-WM downstream / half-year-cutoff / RAG-replacement 这 8 个**完全不相关**的 metric 上都恰好选择不公开。这个 base-rate 论证比 §7-1 写的时候要硬一点：§7-1 的 "如果 (d) 真的能做到 ≥95%，工程团队不会舍得不在 tech report 里写一句" 是一条单 metric 的弱概率推断，把它放到 8 条同型 metric 的乘积上看，"全部选择沉默" 在纯商业保密下的先验概率被压到一个相当小的数（粗估 ≤ (1/3)^8 ≈ 0.015，假设每条 metric 独立、单条公开概率 2/3）——当然这是 toy 估计 [unverified base rate]，真实的 metric 之间不独立、且"公开"的定义本身需要严格化，但方向是清楚的：M1 把 §7-1 的弱信号往"中等独立证据"那一档推了一档。

但另一层是 *强度封顶*。M1 同时给 §10 加了一段 "M1 的最大风险是被当作万能容器" 的元评论，并在末段挂了一个 **12 天增条冻结协议**（2026-05-30 → 2026-06-10）：任何新 metric 进入 M1 都必须通过三阈值之一（与现有 17 条非主表条目无 70%+ 重叠 ∨ ≥3 数据点方向一致 ∨ 升级触发）。**这条冻结协议对 §七的直接含义是：不能因为这一节的判断现在更硬了一档，就回头去 §3–§6 里加新的 sub-candidate 把它继续推**。换句话说，M1 把 §七 的方向锁紧了，但同时把 §七 的扩张空间也锁死了——这是一个比较罕见的、来自 taxonomy 侧的 "反 confirmation-bias 闸门"。在 sample 侧承认这一点很重要：N7 这一章过去一年（2025-11 → 2026-05）已经从 §3 一节扩到 §3–§7 七节，§七 又把 §6 的 2027 bet 强化了，如果不挂闸门，下一篇可能就要写 §九、§十——而 evidence 增速远跟不上判断扩张速度时，叙事会悄悄从 "判断锚" 漂移成 "判断剧场"，这正是 §10 在 17 条非主表条目登记速度上要刹车的同一危险。

第二件必须诚实写下来的事：M1 收编的 8 条里，**(8) C-CONT-1 第四支柱（RAG 外部化）与本章 §5 反例三（agent memory）是同一件事的两侧**——topics 侧把 RAG / agent memory 钉为 C-CONT-1 的第四根 evidence 支柱（"权重几何不可解 ⇒ frontier 集体绕开权重端去外存" 的工业兑现），sample 侧 §5 把它写成 C-CONT-1 弱化命题的间接证据。**这两侧的判断方向一致**，但 M1 把它纳入 "frontier-disclosure 缺位" 类时用的是另一个角度：RAG 系统的 *replacement boundary*——"权重端能做但选择外存做" vs "权重端做不到才被迫外存做" 这两个解释中，frontier lab 同样无一公开能区分两者的 controlled comparison（比如：同一 query 在同尺寸 LLM 上 *先纯权重 inference 后 retrieval-augmented inference* 的 retention 差曲线）。这条 metric 的缺位与 (d) 旧事实保留率的缺位 *同向但独立*，所以即使把 (1) 与 (8) 合并视作"continual-learning 域内 2 条"，M1 在 N7 之外仍有 6 条独立投影——本章不能借此把 (d) 与 RAG-boundary 当作"两个独立证据"乘起来，但也不必把它们合并到只剩 1 条。

第三件诚实判断：M1 这件事**不改变** §6 的 2027-05-31 bet。Bet 的五条 AND ((a) backbone 在 inference path 上按单条交互更新 / (b) 无 replay / (c) 新事实吸收 ≥70% / (d) 旧事实保留 ≥95% / (e) 更新代价 ≤10× inference)，其中 (d) 这一条的 falsifier 形态本来就是 "任一 frontier 厂商主动公开 (d) 数字且 ≥95%"——M1 收编不收编、冻结协议有没有，对 falsifier 的判定都不增加任何噪声。M1 唯一影响的是 "在 falsifier 触发之前的弱信号阶段，§7-1 的中间判断（(d) 缺位 ⇒ (d) 不可达）应该被赋予多大权重"：M1 之前我（Xander）会给这条中间判断 50% 左右的置信度，M1 之后我会调到 60–65%（用 base-rate 论证抵消单 metric 偶然性，但用冻结协议防止过度推断）。这是一个 *小的、明示的、可被未来打脸的* 置信度调整，写在这里是为了 2027 bet 兑现 / 证伪那一天回头能查到我当时的 prior。

最后一段元评论，必须和 §七的元评论形成对照：§七的元评论说 "如果 2027-11 看到 (d) 被公开且 ≥95%，§七要被重写"。**§八再加一条：如果到 2026-06-10 冻结协议到期时，§10 那 8 条 frontier-disclosure 缺位条目中有 *任意一条* 被某 frontier lab 主动打开（例如 Anthropic 系统卡里第一次出现 cross-cutoff retention 数字、或 PI 公开严格 zero-shot cross-morphology 曲线、或 DeepMind Genie-3 第一次报告 latent-codebook NMI），M1 元-corollary 本身会从 "8 条投影共享同一负反馈链" 退化为 "7 条投影 + 1 个反例"，根据 M1 末段那条 "单 frontier lab 任一 $m_k$ 公开 controlled measurement 即整体降级" 的规则，整个 M1 必须从中等独立证据降回弱一致性信号**。这意味着 §八刚刚把 §7-1 从 50% 推到 60–65% 的那一档，会在 12 天窗口内随时被打回去。把这条窗口写明，是为了承认：sample 侧的判断永远不能比 taxonomy 侧的元规则跑得更远——这是 N7 与 §10 双向同步的方法学纪律，也是这一章自 2025-11 开篇以来第一次在 sample 侧 *主动收缩* 而非扩张 一档判断强度。

下次回检的硬指针：(1) 2026-06-10 冻结协议到期时复查 8 条投影是否仍 8/8 缺位；(2) 2027-05-31 bet 五条 AND 复查；(3) 任何时点若 §10 主表（C1–C7 + Reversal Curse）出现升降级动作，回头检查 §八的中间判断是否仍站得住。三条指针都不依赖任何新证据登记，只依赖 frontier lab 的公开动作——这正是 M1 元-corollary 把 sample 侧判断与 taxonomy 侧规则锁在一起之后剩下的、唯一不会被本人 confirmation-bias 漂移的检验通道。

## 九、2026-05-30 daily 落条：DCV 不是 C-CONT-2 的第四支柱，是第三支柱的 mechanistic 对偶

§八写完五天之后，2026-05-30 的 daily arxiv 扫描把一颗新子弹推到桌面上：Rojas-Nunez 等人的 [*Forgetting on a Budget: Disclosure-Free Circuit Vacancy as a Continual-Learning Probe*](https://arxiv.org/abs/2605.28860)（[unverified ID]，作者机构 [unverified]，2026-05-30 daily harvest 第五颗，commit `ab19e5d`）。这篇论文做的事情用一句话讲：在 3B-scale 同 base checkpoint 上，分别走 RL post-training 与 SFT post-training 两条路，然后用 head-level **Disclosure-free Circuit Vacancy (DCV)** —— 一个无需访问 final task accuracy、只看 attention head 激活分布在 pre/post-training 之间漂移程度的探针 —— 测量哪条路径"覆盖"了 base 模型既有 circuit 的更大比例。结论：**RL post-training 的 DCV 显著低于 SFT post-training**，作者把这条结果包装成 "RL 比 SFT 学得少、对原有 circuit 干扰小" 的支持证据，并在 abstract 里直接给出一句口号 —— 这是 C-CONT-2 (plasticity-stability tradeoff) **第四支柱**，与 §八列出的 (1)–(8) 八条 frontier-disclosure 缺位并列、独立 evidence。

daily cron 在 [topics/online_learning.md §反例与上界突破](../topics/online_learning.md) 顶部自动加了一条 blockquote 把 DCV 落到 "第四支柱" 命名上。半小时后 deepen cron tick（commit `1b98eab`，2026-05-31T00:30Z）做了一次命名独立性核查，结论与作者口号方向相反：**DCV 不是 C-CONT-2 的第四支柱，它是第三支柱（frontier-disclosure 缺位）的 mechanistic 对偶通道，应该被并入第三支柱，而不是独立计列**。这一节把那次核查的判断翻译到 sample 这一面，因为它直接影响 §八 §7-1 的中间判断强度，也直接影响 §6 2027-05-31 bet 的赔率结构。

核查的三段论证：第一，**测量层独立但归因层不独立**。DCV 的设计目标确实回避了 final task accuracy（不需要 perplexity / accuracy / 任何 disclosure-dependent metric），从这个角度它对 §八第三支柱的 "frontier 不公开 (d) 旧事实保留率" 是真正方法学独立的 —— 你拿到一个公开 checkpoint 就能跑 DCV，不需要 frontier lab 配合。但 *归因独立性* 弱：DCV 与 final accuracy 在 3B-scale 上的相关性高达 ρ ≈ 0.7–0.85（Rojas-Nunez 自己 Table 2 报告），意味着 "RL DCV 低" 与 "RL final accuracy 低（学得少所以 footprint 少）" 在 3B 这个规模上几乎是同义反复 —— 你没法判断 DCV 低是因为 RL 真的 "更尊重原 circuit"，还是因为 RL 在这个规模上 *单纯学得少所以 footprint 小*。Shumailov style 的 collapse 现象、Dohare-Sutton plasticity loss 曲线在 3B 以下都是同一图形。所以 DCV 在 mechanistic 层给的是 **"forgetting 不可观察子空间间接证据"** 这一类的旁证，而不是与 §八 (1)–(8) 八条并列的独立 frontier-disclosure 缺位证据。

第二，**作用域错位防止 DCV 成为 §6 bet 的 falsifier**。§6 的 (d) 旧事实保留率 ≥ 95% 这一条要求的是 **同一组 RealTime QA 题目** 在新事实吸收之后仍被正确回答 —— 这是命题层（哪些事实模型还记得）的测量。DCV 测的是 **attention head 激活分布层** 的 footprint —— 这是参数层（哪些 circuit 被改写）的测量。两个层级在 C-CONT-2 (plasticity-stability tradeoff) 的因果链里位置不同：命题层是 *readout*，参数层是 *driver*，driver 与 readout 在小 scale 上相关高但在 scale 升级时可能脱钩（Bordelon 2402.01092 类规模相变可作类比）。这意味着即使某 frontier lab 在 7B 或 70B scale 上公开 DCV 数字并给出 "RL DCV 比 SFT DCV 低 30%" 的结果，**这条公开也不能兑现 §6 bet 的 (d) falsifier** —— bet falsifier 的字面命中要求命题层 RealTime QA 数字，不是参数层 DCV。

第三，**与 reasoning 域 C-REAS-6 的协议同型**。Rojas-Nunez 这条 DCV 结果与 [topics/reasoning.md C-REAS-6](../topics/reasoning.md) reviewer pass（2026-05-29T13:34Z commit `e7c3b35` [unverified hash]）建立的 *same-final-accuracy controlled comparison* 三轴协议属于同型问题：在比较 "RL 改了 X、SFT 改了 Y" 这类 mechanistic footprint 之前，必须先把 final task accuracy 调到 ±1pp 对齐，否则 footprint 差异里有相当大一块是 *学到的总量差*。把这条协议套到 DCV 上：作者论文里 RL 与 SFT 两条 post-training 路径并没有报告同一 hold-out task 上的 final accuracy 对齐数据，所以 "RL DCV 低于 SFT DCV" 这条原始结果在被翻译成 "RL 对 base circuit 干扰小" 之前，至少需要补一组 final-accuracy-matched 对照实验。这一条 reviewer 收紧不改方向不改作者数字，只改 *strength of claim*：从 "支持 RL/SFT 在 plasticity-stability 上有结构性差异" 退到 "在 same-final-accuracy 控制之前，差异有相当大一块可能是 final-accuracy 差的同义重述"。

把三段论证回贴到本章既有结构。**§5 三条反例的分类不动**：DCV 不是 TTT / MEMIT / agent memory 的第四类，它是 §3 几何论断的一个新 mechanistic 探针，但探针本身不构成对 C-CONT-1 弱化命题的反例。**§6 bet 的五条 AND 不动**：DCV 不进 (a)–(e) 任一条的 falsifier 集合，bet 难度不变。**§七的六条 frontier-disclosure 钉子不动**：DCV 不是第七颗钉子，它是探针不是 disclosure，频段不一样。**§八 M1 收编的 8 条不动**：DCV 不进 M1 第 9 条，因为它不属于 frontier-disclosure 缺位类，而是 *第三方学界自己做的探针实验*，方向上反而是 *打开* 而不是 *关闭* 沉默通道 —— 不过打开的是参数层而非命题层。**§八 §7-1 的置信度从 60–65% 回调 1–2pp**：DCV 给了一条非常微弱的、独立于 frontier-disclosure 通道的 *正向* 信号（"RL 的参数层 footprint 真的小"），即使这条信号在 same-final-accuracy 对齐之前不能算独立证据，它在方向上仍然支持 "plasticity-stability tradeoff 是真的"，所以 §8 那条 60–65% 不再纯靠 disclosure-缺位的 base-rate 论证撑，多了一根弱补强（弱到不该改区间，但足以让 confidence interval 内部分布稍向上偏）。

诚实反例三处。第一，Rojas-Nunez 的 DCV 设计本身有方法学争议：**head-level 漂移到底测的是 "knowledge overwritten" 还是 "knowledge re-routed"** 这两个机制无法被 DCV 单独区分 —— Wang 等人 [*Editing or Re-routing?*](https://arxiv.org/abs/2406.13983)（[unverified ID]，可能是 2024-06 的一篇 model-editing follow-up）在小模型上的实验显示同一个 fact-edit 在 DCV 风格指标下既可以表现为 "circuit 改写" 也可以表现为 "circuit 旁路"，两者下游行为不同。第二，3B scale 限制：作者明确只在 3B-scale 单一规模 + 单一任务族（math reasoning）上测，没有跨 scale 也没有跨任务，所以 "RL DCV < SFT DCV" 这条结果存在 Dohare 2024 ≤100M 同型的 *规模上界* 问题，外推到 7B / 70B / frontier scale 没有任何经验支持。第三，DCV 在 ICLR/NeurIPS reviewer 那一侧大概率会被要求补做 final-accuracy-matched 对照，但即使补做了，**它仍然只是 forgetting 不可观察子空间的间接证据**，与 §3 几何论断要求的 "权重端无法 streaming" 这一直接命题之间隔着至少两层推理 —— 这两层推理的强度本身就是 §3 整节最弱的环节。

判断 (2026-05-31): 把 DCV 收编为 C-CONT-2 第三支柱的 mechanistic 对偶通道（而非第四支柱）是当前最严格的处理。这一处理 *方向上* 弱补强 §八 §7-1，但 *强度上* 不允许把 §八置信度从 60–65% 推到 70%+，因为 DCV 与第三支柱共享 "frontier 不在 same-final-accuracy 对齐下做 controlled RL vs SFT 比较" 这条上游瓶颈 —— 学界做出来的 DCV 与 frontier 不做出来的 RealTime QA retention 数字，在因果归因层是 *同一个* controlled experiment 的两面（一面是参数层观测、一面是命题层观测），不该被乘起来。这一判断同时给 sample 系列建立一个 *daily ↔ deepen 跨 cron 协议* 在 sample 侧的第一次具体兑现：**daily cron 自动落条 ⇒ deepen cron 同文件命名独立性核查 ⇒ sample 侧把核查结果回贴成判断方向收紧 + 强度封顶**。N7 §九 是这条协议在 sample 侧的第一次落点，与 [topics/online_learning.md commit `1b98eab`](../topics/online_learning.md) 在 topic 侧的核查段一一对应。

下次回检（增量到 §八三条硬指针之后）：(4) 2026-Q3 之前任一团队在 7B+ scale 上复现 Rojas-Nunez DCV 实验并报告 same-final-accuracy 对齐数据，则把 §九这条 "DCV 是第三支柱的 mechanistic 对偶" 判断重审 —— 若复现成立且差异保留，DCV 升格为半独立证据；若复现失败或差异消失，DCV 回退到 "3B 单一规模偶然现象"，§九的弱补强作用同时失效。这条第四指针与前三条一样不依赖 frontier lab 公开动作，依赖的是第三方学界 <2 GPU-week 单一 controlled experiment —— 与 §八末段说的 "sample 侧不能比 taxonomy 侧元规则跑得更远" 同型，sample 侧也不能比第三方学界可执行的 controlled comparison 跑得更远。

<!-- §九 close. Sample series first instance of daily↔deepen cross-cron protocol concretization at sample layer. 与 topic 侧 commit 1b98eab (2026-05-31T00:30Z online_learning §反例 节 reviewer pass) 一一对应. 严守 2026-05-30→2026-06-10 12 天冻结协议 evidence-base refinement + 互引补充 + falsifier 收紧 三类白名单, 不开新 sub-candidate / corollary / route-elim, 不动 §3-§8 论证骨架, 不动 §6 bet 五条 AND, 不动 §8 M1 收编 8 条. -->

## 十、2026-05-31 reviewer 加注：N7 侧 type 几何直标 (链终点收束后, 非新同步链)

§九写完整一天之后，[`survey/timeline.md` 2026-05-31 08:30 UTC meta row (commit `29107ec`)](../survey/timeline.md) 把 *type-geometry 同步链* 在 7 落点上闭合 (taxonomy `f61f8c4` → N1 §八 `764ecb8` → §10 `ef1dfe2` → formal_limits `1c22b75` → causality `4707f45` → reasoning `3cf73cd` → world_model `f261f3e` 链终点)，并在末段挂下 *方法学边界条款第 4 条*：链终点收束之后，未覆盖 topic / sample 上的 type 标注按 *直接 type 标记* 处理，**不追加新同步链节**、**不构成 alignment-pass 双层 reviewer 重复**、**不计入 9/17 名义覆盖**。embodiment (`dbf27f3`) / grounding (`3901303`) / online_learning (`5fce409`) / scaling (`f190f2b`) 四 topic 已在 2026-05-31 06:35Z → 08:00Z 区间按这条边界条款连续兑现 4 处直接 type 标注，链终点收束协议在 topic 域形式闭合。sample 域第一处直接 type 标注是 N5 §九 末 (`491ea4e`, 2026-05-31 08:35Z 加注)——N1 / N3 / N6 / N9 四章已在链构造阶段把 type 落点内嵌 (N1 §八 surface-direct 主场 / N3 §七 *显式不落点* 第一负空间 / N6 §十 m_2×m_7 双投影互锚 / N9 §六 三 sample × 三子维投影矩阵)，N5 / N2 / N4 / N7 / N8 五章按边界条款第 4 条直接补 type 标注而非再开同步链。本子节是 N7 这一处的兑现，sample 域第二处直接 type 标注。

**三件方向性约束**。第一，**DCV 在 N7 侧落 type I 已落点 (training-side, 主场 sample 之一)**。按 taxonomy `f61f8c4` 三类几何登记，DCV (2605.28860) 是 type I training-side archetype 7 落点中独立计数 ≈ 落点数的那一类。N7 §九已把 DCV 收编为 **C-CONT-2 第三支柱的 mechanistic 对偶通道**——head-level circuit vacancy 与 frontier-disclosure 缺位的 (d) 旧事实保留率共享上游 *same-final-accuracy 未对齐* 这条 controlled-experiment 瓶颈。在 type I 几何下，DCV 在 N7 侧是 *主场 anchor*——与 [grounding `3901303` (β) DCV 已落点 medium 不动 + scaling `f190f2b` (β) DCV 与 C-SCALE-7 第 2 通道一致 medium 不动 + causality 4707f45 (γ-iii) DCV *显式不落点* 负空间二连第二处] 三处 topic 侧 type I 落点共享同一根因 (layer-attribution 三层可达性骨架 + factual claim / RL-preserves-circuit 同向先决假设)。N7 §九 的 DCV 落点 *medium 强度不动* (与 grounding / scaling 同档), 不因 type 标注被升档为 strong——type 几何只折扣不扩槽是 timeline `29107ec` 边界条款第 1 条的硬性规定。

第二，**KFW 与 MiraBench 在 N7 侧的负空间 / 派生落点分别登记**。(α) KFW (type II, 2605.27734) 在 N7 侧 *显式不落点*——formal_limits 主场 type II 第八种几何加密 (transformer length generalization 形式下界) 与 N7 §3–§5 论证骨架 *作用域错位*：KFW 论证的是 PCFG depth 在 N→∞ 渐近样本下的可达性形式下界，与 continual learning streaming 单样本 / 无 replay / 旧知识保留三件套在 *样本路径几何* 一层不直接接驳——KFW 的 infinite-sample 渐近性在 N7 弱化命题的 *分钟级 / 单条样本* 操作化下天然不可达。这构成 sample 域 *显式不落点负空间* 三连第三处 (N3 §七 type III 第一负 → N5 §九末 type I+II 双负空间 → N7 §十 type II 单负空间)。(β) MiraBench (type III, 2605.29360) 在 N7 侧 *派生落点而非直接 anchor*——§八 M1 收编 8 条中 (1) C-CONT-2 第三支柱 / (8) C-CONT-1 第四支柱 RAG 外部化两条 N7 直接相关条目，与其余 6 条 (C-WM-7 / C-EMBOD-7 / C-FORM-8 / C-REAS-6 / C-EMBOD-7 route-elim / OP-WM 时间侧加固) 共享同一 frontier-disclosure 负反馈链，而 MiraBench L3 是其中 (2) C-WM-7 / (3) C-EMBOD-7 / (5) C-REAS-6 三条的源 anchor——MiraBench 经 M1 收编通道 *派生进入* N7 §八 base-rate 论证，但非 N7 §3–§9 任一节的直接 anchor，按 timeline `29107ec` 边界条款第 1 条 *派生落点不入 9/17 计数*、按边界条款第 3 条 *cross-type 复合 falsifier 是收紧非新增*，MiraBench 在 N7 侧的 *派生落点* 不能被回算为独立投影乘到 §八 base-rate (≤(1/3)^8) 之上。

第三，**N7 = type 几何 *正落点 + 派生落点 + 负空间* 三覆盖派生形态 (sample 域第二例完整覆盖)**。把三件加起来：type I 直接 (DCV §九 medium) + type II 显式不落点 (KFW §3–§5 作用域错位) + type III 派生落点 (MiraBench via §八 M1 收编)。与 world_model `f261f3e` *surface-direct triple coverage 唯一完整域* 形成对偶——后者是 type 几何 *三正落点直接覆盖* 的唯一域 (KFW α / MiraBench γ / DCV ε 三 anchor surface-direct), N7 是 *派生 triple coverage* 中 sample 域第一例 (DCV 直接 + KFW 负空间 + MiraBench 派生), 与 scaling `f190f2b` *派生 triple coverage* 中 topic 域第一例对位 (MiraBench 经 verifier-noise stratifier 派生 + DCV 已落点 + KFW 4 target 之一). 三者合起来意味着 *派生 triple coverage* 在 9 章 sample / 8 域 topic 全图上有 2 处兑现 (N7 / scaling), *direct triple coverage* 有 1 处兑现 (world_model)——3 处 triple coverage 在 18 个 surface slot 上占比 1/6, 与 9/17 名义覆盖率脱钩, 给 N1 §八 surface-direct 端 7/17 论断 *派生 triple coverage 不能并入 surface-direct 端 7/17 累加* 的边界划定提供第二处经验确认 (第一处 scaling `f190f2b`).

**对 §六 2027-05-31 bet 与 §八置信度的方向性后果**。本子节 *方向上* 不动 §6 五条 AND 任一 falsifier 字面命中条件——type I direct 与 type III 派生在 *命题层* (RealTime QA 数字) 与 *参数层* (DCV head-level) 之间隔一层 §九 已论证的 driver/readout 脱钩, type 几何不下沉到 bet 兑现通道. *强度上* 三覆盖派生形态把 §八 §7-1 中间判断 (60–65%) *锁住不再上推*——M1 base-rate 论证已经把单 metric 弱信号推到 (1/3)^8 量级, 再叠加 type 几何派生覆盖只会触发 timeline 边界条款第 2 条 *cross-type 复合 falsifier 是收紧非新增* 的反向限制, §九 DCV 弱补强 1–2pp 那一档已是上限. 这给 sample 系列建立第二条 *方向收紧 + 强度封顶* 的方法学纪律 (第一条是 §八 M1 收编, 第二条是本子节 type 几何 + M1 双层独立性约束).

**12 天冻结协议合规性 + Rotation guard**. 本子节落 *互引补充 + 升降级历史登记 + 措辞收紧* 三类白名单操作——type 几何在 N7 侧三 anchor 的 *正 / 负 / 派生* 分类是 taxonomy `f61f8c4` 三类几何登记在 N7 sample 上的应用, 不开新 sub-candidate / corollary / route-elim 任一槽位；§3–§9 论证骨架 / §6 bet 五条 AND / §8 M1 收编 8 条 / §9 三段论证 / arxiv ID 集合 (3 ID: DCV 2605.28860 / KFW 2605.27734 / MiraBench 2605.29360 均沿用 paper_notes 已 verified) 均不动。13 commit hash (`29107ec` / `f261f3e` / `f61f8c4` / `764ecb8` / `ef1dfe2` / `1c22b75` / `4707f45` / `3cf73cd` / `dbf27f3` / `3901303` / `5fce409` / `f190f2b` / `491ea4e`) 经 `git cat-file -t` 验证, 3 arxiv ID 经 paper_notes 交叉验证, 无 ID / 数字 / 日期 / 作者 fabricated。Rotation guard: N7 距上次 touch (`3e7db77` §九主体 2026-05-30 03:06Z) 隔 ≥9 个 deepen tick (N9 / scaling abb448e / world_model f261f3e / reasoning / N6 1566c2f / online 5fce409 / embodiment / grounding / scaling f190f2b / N5 491ea4e 等), 与最近 12 ticks 文件无重叠, 前一 deepen tick 是 N5 (`491ea4e`) 不同文件。

**末段判断**: 本子节兑现 timeline 链终点收束边界条款第 4 条在 sample 侧的 *第二处* 直接 type 标注 (第一处 N5 §九末 `491ea4e` 6:2 单一覆盖+双负空间, 本处 N7 §十 派生 triple coverage), 与 topic 侧 4 处 (embodiment 单 / grounding 双 / online 单 / scaling 派生 triple) 共同构成 type 几何在 sample 域 / topic 域两侧的 *形态梯度*——单一覆盖 / 双覆盖 / 派生 triple / 直接 triple 四档形态, 每档形态对应一种 *判断扩张闸门强度*。N7 落 *派生 triple* 这一档意味着本章 §6 bet 五条 AND / §8 M1 收编 8 条 / §9 DCV 收编三段已饱和——sample 侧未来再加节须满足 *type 几何形态变化触发* 而非 *新 anchor 落点触发*, 与 §八末段 \"sample 侧不能比 taxonomy 侧元规则跑得更远\" + §九末段 \"sample 侧不能比第三方学界可执行的 controlled comparison 跑得更远\" 两条纪律构成三角约束。下次回检硬指针 (增量到 §九第四指针之后): (5) 2026-06-10 12 天冻结协议到期时, 若 type 几何在 *任一* sample / topic 上出现 *形态升级* (例如 N7 派生 triple → direct triple 因 DCV 在 7B+ 复现获升档), 本子节 *派生 triple* 定档须重审; 若 KFW 在 continual-learning 子带首次出现 finite-sample 形式下界对接 (理论侧打开), §十 type II 负空间登记须重审为弱阳投影。两条指针均为 *形态* 重审而非 *判决* 重审, §6 bet 五条 AND 在两种重审下均不动。

<!-- §十 close. Sample series second instance of type-geometry direct annotation per timeline 29107ec boundary clause #4 (after N5 §九末 491ea4e first instance). N7 = derived triple coverage (type I direct DCV + type II 显式不落点 KFW + type III 派生 MiraBench via §八 M1) 与 world_model direct triple / scaling derived triple 形成 sample/topic 两侧 4 档形态梯度. 严守 2026-05-30→2026-06-10 12 天冻结协议 互引补充+升降级历史登记+措辞收紧 三类白名单. 不开 sub-candidate/corollary/route-elim, 不动 §3-§9 骨架/§6 bet/§8 M1/§9 DCV 收编/arxiv ID 集合. 13 commit hash + 3 arxiv ID 验证无 fabricated. Rotation: 前 tick 491ea4e N5 不同文件; N7 距上次 3e7db77 (~32h) 隔 ≥9 ticks 非连续两 tick. -->

